> **🟢 D-COMPLETE-FIRST DECIDIDA E REGISTRADA (26/09 ~04:2x, lane paridade — regra de escolha da mantenedora; 4 frentes rule-6 destravadas).** Chat: "me da soluções idiomaticas, nada de desistir ou assumir gap" + "muito menos stub" + "é ela q vc segue" + "defina isso como regra" → **`D-COMPLETE-FIRST`** entra em `DECISIONS.md` EN+PT: a forma completa idiomatica é A decisão; desistir/gap-aceito/stub/fachada NÃO são opções. Consequências: **(1) O-02×N-02 RESOLVIDO — Fase 3 UNLOCKED** (passe de ownership/lifetime no pipeline + emissão MEM001/MEM002/MEM005 nos 4 alvos, pacote único, sem literal null — plan+spec EN+PT atualizados); (2) X2 = pacote oficial `interop` completo (marshalling tipado, motor real, sessão, erros nomeados, E2E por alvo); (3) X8 fatia 3 = tag no primitivo `test` + runner inteiro; (4) kof.ui = auto-unsub determinístico no unmount com trava de leak. Voto futuro só entre opções genuinamente completas. **NEXT STEP (re-trigger lê isto):** implementar Fase 3 fatia 1 — passe de análise de ownership/último-uso na frente do kof-compiler + emissão MEM001 (use-after-move) no JVM com E2E golden (válidos byte-green obrigatório); files: pass em `dev.kof.compiler.memory`, wiring no pipeline, `MemorySafetyE2ETest` novo por alvo. Depois: X8 tag, UI unsub, X2 interop (fila).

