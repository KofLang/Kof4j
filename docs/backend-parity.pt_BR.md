[English](backend-parity.md) | [Português](backend-parity.pt_BR.md)

# Backend Parity — Kof JVM × Native × KofJS

**Última atualização:** 20 de setembro de 2026
**Versão:** 0.5.0-beta

> Deltas desde 0.1.0: Targets `native.risc` (riscv64) e `native.arm` (aarch64) separados de `native` x86_64 (**core completo 02-03/09** — `NativeRiscv64E2ETest 13/13` + `NativeAarch64E2ETest 13/13` em asm puro + qemu, estado atual na seção "Native por arco" abaixo + `docs/native-multiarch.md`); Native free-list (`kof_free_head`) + `kof_gc_collect` (mark-sweep implementado 03/09; **auto-collect LIGADO 19/09 — §260/G-6(a) fechado**: o gatilho chama `kof_gc_collect_now` 1x por programa, gate `spawn_count==0`; histórico honesto no §260); MySQL wire protocol real no x86-64 (`kof_db_mysql_scramble` + `user:pass@`; handshake/COM_QUERY/prepared completos + as 13 faces ORM medidas contra o MariaDB 12.3.2, F2d1–F2d7 22/09); `mariadb://` é alias do wire `mysql://` no Native x86-64 (paridade DB S1, 23/09 — `kof_db_type` reporta a família mysql; cross segue `DB001` honesto, R7); pattern matching `switch case String s` + record destructuring `Point(x,y)` em JVM/Native/JS; `String?` null safety básica; `KofScript` top-level `let` → `KofScriptGlobals`; `KofCcompiler` (`kof c`) native-only C subset; `List map/filter/reduce` + `Box<T>`; Windows SIGPIPE fix.
> Deltas 30-31/08: `spawn`/`await` real no Native (pthread + trampoline + join + allocator thread-safe futex — CONC001); FP real em XMM (FLT001); JSON objetos/records + arrays FP no Native (JSN001/JSN002/JSN003); WebSocket/SSE no JVM (`app.ws`/`sse.*`, RFC 6455); `kof.http` retry/circuit JVM+JS (30s window, fail-fast); `kof.cache` 3 targets (fix de clobber de registradores); UI Fase 7 Router (JS real, JVM no-op); SQLite nativo via `.so` direto; `kof fmt` + `kof config gen`.
> Deltas 18/09: **FFI da JVM generalizada (R3, `.18`)** — `extern` agora casa qualquer assinatura sobre o conjunto escalar {Int, Long, Float, Double, Boolean, String} em aridade arbitrária (parâmetros e retorno), um retorno `String` lê de volta o `char*` nativo; os downcalls FFM `kof_ffi`/`kof_ffi_void` substituem `kof_ffi_i`/`_si`/`_dd` (`FfiE2ETest` `pow` dois doubles → `1024.0`, `strstr` duas strings → `world`, `srand(Int)` default `void`; `FfiE2ETest` `atol`→`labs` prova `Long` ponta-a-ponta, `FfiSignatureTest` trava o mapeamento escalar→layout completo incl. `Float`/`Boolean`). Gaps honestos preservados: struct/pointer (D6), variadics e handles opacos/out-buffers seguem `FFI001`. **Paridade JS FECHADA 18/09 (3.6, `.18`)**: a ABI escalar binda no target JS via bridge FFM no host `KofJsFfiBridge` (`extern`→`kofFfi`→`ProxyExecutable` `kof_platform.ffi` no runner GraalJS/node), provada byte-a-byte JVM↔JS por `FfiE2ETest` (+7 `assertJvmJsParity`); browser não tem host → degrade honesto em runtime (R7); assinaturas não-escalares seguem `FFI002`. Native `FFI001` (§61) permanece gap honesto por target (R7).
> Deltas 18/09 (2): **paridade FFI-JS FECHADA (R3.6-F2/F3, `d3598c2d`)** — o runner host JS (GraalJS/node) liga a MESMA ABI escalar via `KofJsFfiBridge` (`extern`→`kofFfi`→`kof_platform.ffi`); `FfiE2ETest` 16/16 afirma igualdade byte-for-byte JVM↔JS; browser = erro honesto de runtime (R7, sem host); nao-escalar JS = `FFI002` em compilacao.
> Deltas 18/09 (3): **callbacks/upcalls da FFI bindam na JVM (R3.4-C1/C2)** — um `extern` com parâmetro de tipo-função baixa para `kof_ffi` com um token aninhado `(<ret><params>)` e o runtime monta um ponteiro de função C real via `Linker.upcallStub` sobre o valor de função Kof (interface sintética especializada `int invoke(int,int)`, unreflectida + ponte `.asType`); `JvmFfiCallbackE2ETest` roda `(x,y)->x+y` através de callbacks C em ABIs Int/Long/Double/mistas (`42/42/6.0/7.5`). Contrato síncrono/não-escapante (vida na arena confined); ABI de callback só primitiva (String/struct/pointer-no-callback e callback-como-retorno seguem `FFI001`); paridade JS de callback = fatia 3.4-C3 (`FFI002` no JS), browser = degrade honesto (R7). Pin no host `JvmFfiCallbackTest` trava o mecanismo.

> **Deltas 20/09 (11): bare `List`/`Set`/`Map` em posicao declarada agora e a colecao builtin nos 4 alvos (#443/§373, `d969bc3a`)** — o descriptor fantasma `LList;` (NoClassDefFoundError no class load) morreu no ponto de convergencia `qualifyDeep` 2b (contrato congelado #139/#150/#214; shadow do usuario preservado pelo controle do §243); `BareCollectionFieldE2ETest` 8/8 (RED 6/8) e o print `2` identico em JVM/Script/Native/JS no jar limpo; a caca Q4 abriu §374/#553 (fix ainda aberto, lane compilador).
> **Deltas 20/09 (12): §374/#553 + §371/#550 — box-por-ARG no `add`/`set`/`send` de colecao BARE (emissor JVM, espelha bug-35) E a CLI distribuida voltou a compilar cross de qualquer CWD (carregador classpath-first dos slices)** — `BareCollectionPrimitiveArgE2ETest` 7/7 (RED 4/4 pre-fix), `RuntimeSourceLoaderTest` 6/6 + `ShippedCliCrossSmokeTest` 2/2 (CLI como subprocesso de um `@TempDir` estrangeiro, cross-binutils+qemu reais). A lane docs re-mediu 7/7 + 6/6 + 2/2 VERDES no tip limpo `aabd7bff` (0F/0E). Ledger §371/§374 EN+PT.
> **Deltas 20/09 (10): argumentos de `extern` seguem a conversao numerica ORDINARIA do Kof (#549/§370, `35bfaec1`)** — `f(Float)` aceita Int/Double com a MESMA conversao de um chamada kof (`fmid(1,2)` -> `1.5`, `sqrt(9)` -> `3.0`), byte-a-byte JVM==Native (re-medido pela lane docs com jar limpo, paridade nos 5 casos); nao-convertiveis travam SEM014 no call-site (`abs(2.5)`, `abs("s")`, `abs(true)`); arity errada continua SEM013. `FfiExternTypeConversionTest` 11/11 + FfiE2ETest/FfiNativeE2ETest/FfiNativeCrossE2ETest 38/38 reverificados.
> **Deltas 20/09 (9): gate de construtor-fantasma + CLI cross quebrada pela armadilha de CWD do jar distribuiddo** — o §362 pousou (`57a0d5f0`, `ConstructorPhantomE2ETest` 7/7): `P(1,2)` em `record P(Int x)` agora e SEM023, mismatch de tipo de argumento e SEM014 — medido no jar real pela lane docs, identico nos builds jvm/js/native, e as formas legitimas (`Q(7)`, `E(9)`, `R(1, 2)`) seguem compilando+rodando (a paridade cross vale porque o gate e em tempo de compilacao). E a varredura da fatia 2 pela lane docs achou o **§371/#550 (classe P0)**: o carregador de slices do runtime le o fonte `.java` por caminho relativo ao CWD, entao o PRUNE NUNCA FUNCIONA NO JAR DISTRIBUIDO -> runtime completo -> `usesDb` -> link dinamico forcado -> `-lsqlite3` inexistente no sysroot cross -> `kof build --target native.risc hello.kf` falha COMP001 em qualquer maquina fora da arvore do modulo (as suites E2E rodam com cwd=modulo = o ponto cego; este host reproduziu riscv64+aarch64). A ABI da fatia 2 em si re-verificada boa pelo caminho da API (FfiNativeCrossE2ETest 6/6 + sondas da lane sob qemu).
> **Deltas 20/09 (8): a FFI escalar binda em TODOS os targets Native (#431 fatias 1-2, `d946e6fa`+`d5c849f3`, §369)** — o `extern` com `library()` agora baixa para um `call sym@PLT` direto em x86-64, riscv64 e aarch64 (a saida do §61 vira a rota de producao: sem dlopen, link-by-use — precedente DB001/SQLite); marshaling SysV/LP64/AAPCS64 por classe com derramamento de >=9 args, `String`<->`char*` no payload offset 24 (NULL->NULL), `void`/`Bool`/`Float`, retorno String = copia na fronteira (`kof_ffi_from_cstr`); validacao SEM013/SEM014 no call-site (um `extern` de aridade errada compilava e dava SIGSEGV em runtime). Fatos de ABI MEDIDOS sob qemu: o glibc riscv64 passa E devolve FP em `fa0..fa7` (físicos f10-f17, NAO ft0); o aarch64 herda pela normalizacao `fa0->f0->d0` do tradutor = AAPCS64. Dois bugs reais dos caminhos de saida corrigidos com prova: a saida stdio da C era PERDIDA sob o `_start` cru (sem atexit — `call fflush` antes da saida nos 3 targets; o x86 precisou `%rdi=0` + realinhamento) e o `_start` aarch64 usava `exit(93)` e nao `exit_group(94)` (qualquer thread viva — scheduler, raylib/GLFW — pendurava o processo PARA SEMPRE; RED 20s / GREEN <1s). Provas: `FfiNativeE2ETest` 16/16 x86-64 (golden da forma InitWindow + paridade byte-a-byte com o JVM dos binds antigos) + `FfiNativeCrossE2ETest` 6/6 riscv x aarch byte-identicos sob qemu. Restante honesto: struct/array (D6), variadics (3.5), callbacks no Native, handles opacos -> `FFI001`; spill >=9 e arestas Float/Bool no cross nao tem golden (sem cc cruzado neste host). §61 FECHADO.
> **Deltas 20/09 (7): cluster #278 FECHADO COMPLETO** — §361 (box no escritor, `e293c4a5`) + §365 (default nunca-escrito = `null` nos 4 alvos, `dd418419`) + §368 (gate de atributibilidade na store de campo — `String`→`Char`, `2.5`→`Int`, `5L`→`Int?` agora SEM012 nos 4 alvos, `5cd078c1`) — #278 fechado com evidencia. A face `Char?`+String-literal do adendo (6) provada phantom, NAO coercibilidade pendente: rejeicao uniforme e o contrato (locals ja rejeitavam). Matriz 4-alvos em `NullablePrimitiveFieldWriterE2ETest` + `FieldAssignabilityPhantomE2ETest` (8/8)._
> **Deltas 26/09 (6): `process.spawn` + ops de handle no x86-64 (`D-FULL-PARITY-050` linha 1 fatia B, lane native-cross)** — nova fatia `RuntimeProcessSpawn`: `kof_process_spawn` (tabela persistente de 64 slots no `.bss`, handle = indice; `pipe2` + `fork` + `execvp` da libc; stdin/stderr do filho em `/dev/null`; exec falho = `-1`), `kof_spawn_read_line` (linha sem quebra, `""` em EOF), `kof_spawn_exit_code` (`Integer.MIN_VALUE` vivo, `-1` morto/killado), `kof_spawn_alive`, `kof_spawn_kill` (SIGKILL + reap + esquece o handle) e `kof_spawn_write` = no-op honesto (stdin de `/dev/null` nos dois alvos — entrada viva e regra 6). Gate `ExpressionProcessCallLowerer` libera `spawn` no x86-64; cross/MCU seguem `PROC001`. `ProcessSpawnNativeE2ETest` 4/4 (paridade byte JVM==x86-64) + `DomainGapCodesTest` repontado (x86 compila, cross PROC001). Bug medido: o filho fechava o fd de ESCRITA do pipe CLOEXEC (escrevia no fd ja fechado), entao um exec falho era lido como sucesso — fix = fechar `fail_r`.

> **Deltas 26/09 (3): `ssh.cmd`/`run`/`ok` em TODO o nativo — x86-64 + cross riscv64/aarch64 (`D-FULL-PARITY-050` linha 3 fatias A+B, lane native-cross)** — `RuntimeSsh` emite `kof_ssh_argv` (oraculo exato `[ssh,-o,BatchMode=yes,-o,ConnectTimeout=5,host,command]`) e `kof_ssh_run` (reusa `kof_process_run`); o gate `ExpressionSshCallLowerer` libera o x86-64 e mantem PROC001 no cross. `SshE2ETest` 9/9.

> **Deltas 26/09 (5): `shell.pipeline` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 2 fatia B2, lane native-cross)** — nova peca `NativeRiscvAsmPipeline.kof_shell_pipeline(List<List<KofString>>)`: o kernel encadeia os estagios (estagio 0 stdin `/dev/null`, stdout do i vira stdin do i+1, stderr intermediario descartado, so o ULTIMO e capturado) via `clone(SIGCHLD)`+`dup3`+`execvp`, SEM threads; Result = `{stdout,stderr,exit}` do ultimo (oraculo JVM). Vazio/sem estagio/>16 = `Result` honesto. `ShellCrossE2ETest` 7/7 (2 e 3 estagios byte-paridade JVM==riscv64==aarch64) + `ShellE2ETest` 19/19. Bug medido: o argv do filho duplicava `argv[0]` (`cat` rodava `[cat,cat]`) — fix no loop (indice `j`, nao `j+1`).

> **Deltas 26/09 (4): `shell.runWith` no CROSS riscv64/aarch64

> **Deltas 26/09 (4): `shell.runWith` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 2 fatia B1, lane native-cross)** — `NativeRiscvAsmShell.kof_shell_runwith(argv, cwd, env)` spawna argv-first: programa = `argv[0]`, o resto vira args, delega a `kof_process_run`. O caso herdado (`cwd` `""`/null, `env` null/vazio) e byte-paridade com o JVM; `cwd`/`env` NAO-VAZIOS nunca sao ignorados (R6) — devolvem `Result` honesto (`stdout=""`, `stderr` com mensagem, `exitCode=-1`), como a convencao do JVM para falha de spawn (env aditivo/cwd-exec no processo free-standing = fatia B2). `pipeline` segue `PROC001`. `ShellCrossE2ETest` 5/5 + `ShellE2ETest` 19/19.

> **Deltas 26/09 (2): `shell.run`/`cmd`/`ok` no CROSS riscv64/aarch64

> **Deltas 26/09 (2): `shell.run`/`cmd`/`ok` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 2, lane native-cross)** — `run` reusa `kof_process_run` (linha 1 fatia C), `ok` e IR puro sobre o acesso `exitCode`, `cmd` usa a peca nova `NativeRiscvAsmShell` (`kof_shell_argv` = prepend do program, o oraculo JVM exato, sem split). `pipeline`/`runWith` seguem PROC001 honesto (fatia B). Corrige tambem um pin STALE do `ShellE2ETest` que ainda exigia `process.run` cross == PROC001 depois da linha 1 fatia C. `ShellCrossE2ETest` 3/3 byte-parity JVM==riscv64==aarch64; `ShellE2ETest` 18/18. Bug medido: a peca nao trocava p/ `.section .text` e caia no `.bss` herdado -> `kof_shell_argv` nao-executavel (SIGSEGV 139); fix = `.section .text`.

> **Deltas 26/09 (1): `process.run` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 1 fatia C, lane native-cross)** — `run` compila e roda nos 2 alvos cross via `NativeRiscvAsmProcess` (riscv64 nao tem `fork` -> `clone(flags=SIGCHLD, stack=0)`; filho `dup3` 1/2, stdin `/dev/null`, `List`->`argv` na pilha do filho, `execvp` da libc para PATH, pai drena os 2 pipes com `ppoll` mascara `0x19` + `wait4`; exec falho = `("", msg, -1)`). `execvp` entrou no `NativeCrossLink.LIBC_SYMBOLS` (link dinamico so quando a fatia e alcancavel). Buffers estaticos de 1 MiB no `.bss` da fatia — a arena GC cross segue 256 KiB (subir o heap global p/ 4 MiB quebrou `riscv/aarch64HeapExhaustionPanicsHonest`; revertido). O gate `PROC001` agora recusa SO `process.spawn` no cross. `ProcessRunCrossE2ETest` 6/6 byte-parity JVM==riscv64==aarch64; `spawn` cross = `PROC001` honesto (fatia B, outra lane).

> Deltas 20/09 (6): **§361 PARCIAL + §365 abertos pela re-medicao da lane docs no tip `e293c4a5`** — o escritor de campo nullable-primitivo boxa `Int?/Long?/Double?/Troolean/estatico/composto` nos 4 alvos (✅), mas a face **`Char?`** ainda morre (JVM/Script `VerifyError` no `putfield`: String cru no slot `Character`; Native: erro de cast na leitura pos-char; JS ✅) e a **leitura de campo nullable nunca-escrito vaza `0` no JS** (JVM/Script/Native = `null`; pre-existente, nao veio do fix) — §365 no ledger, rota cluster `.22`/#278.
> Deltas 20/09 (5): **o rio da erasure JVM pousou (`16f16081`/`c8d55a10`/`0aa6a307`, familias §355-§357)** — `TypeVariable` carrega o bound ate todo emit site, interfaces genericas baixam descritores apagados com pontes covariantes em pai E interfaces (box/unbox na fronteira de primitivo), campo `T[]`/`as T[]` apagam com honestidade, `SEM098` gateia array-primitivo-em-slot-apagado; 8 issues fechadas com prova (#399/#363/#375/#385/#366/#365/#295/#368). Gap nativo pre-existente registrado §358 (link `toString` em `T` sem bound nos cross).
> Deltas 20/09 (4): **D-TROOL pousou (`916b9fb7`/`d61836eb`/`5f0757e8`)** — `Bool` 2-valores, `Troolean` trivalente de Kleene na maquina boxada (`&&`/`||`/`!` desugaram; backends intocados), `Bool?` do usuario rejeitado `SEM095`; lei travada em `TrooleanLawE2ETest` 13/13. Linha de paridade propria ja na tabela.
> Deltas 20/09 (3): **X7-2 — DWARF agora nos CROSS (`5d9c855c`/`23b7ecf7`/`890b58bf`)** — tabelas de linha `.file/.loc` provadas no ELF riscv64/aarch64 real sob qemu (`NativeDwarfCrossTest`) + DIEs de CU/subprogram com `frame_base` por ABI; source maps JS ✅; `kof profile` honesto. Linha do tracker corrigida contra o codigo.
> Deltas 20/09 (2): **X10 FECHADA (`27826838`)** — catalogo de assinaturas do LSP em **32/32 namespaces** (`json` na tabela com trava no SEM025 real), hover + `signatureHelp` + `completionItem/resolve` de uma fonte gerada (`scripts/gen_signatures.py`), 265 membros.
> Deltas 20/09 (1): **`shell.pipeline` agora e REAL no JS — fecha o ultimo residual de pipes vivos da linha 2.2** — binding de host `KofJsProcessBridge.processPipeline`: a mesma cadeia ProcessBuilder + threads de pump daemon do `kof_shell_pipeline` do JVM (stdin da primeira etapa `/dev/null`, demais PIPE, exit code do ultimo, pumps joined, sobrantes destruidos; `no stages`/`empty stage`/erros de spawn = `Result(-1)` honesto). Gate do lowerer reduzido a so-Native; `ShellE2ETest` 18/18 com o pin JS trocado por goldens de paridade byte (2 estagios `echo|wc -w`→3 e pin multi-pump de 3 estagios `echo|tr|wc`). Linha 2.2 do tracker universal vira ✅.
> Deltas 19/09 (6): **face JS de `process.spawn` landada (F10 fecha no JS) + branch morta de roteamento consertada na raiz, lane `.18`** — `spawn` + ops do handle (`write`/`readLine`/`exitCode`/`kill`/`alive`) agora rodam no JS pelo binding de host `KofJsProcessBridge` — paridade **por construção** (mesmo JDK, mesmo ProcessBuilder, contrato copiado do binding JVM medido: spawn falho = `-1`, EOF = `""`, sentinela vivo = `Integer.MIN_VALUE`); o gate do lowerer reduziu a só-Native e `DomainGapCodesTest.processSpawnOnJs` virou de pin PROC001 para no-gap. **§355 achado no caminho:** ops de handle baixavam para um `invokevirtual java/lang/Long.readLine` cru — a branch `isHandle` morava atras de um dispatcher que nunca roteia um receiver `Long`, entao NENHUM alvo as executou (todos os pins antigos so assertavam compilacao). Conserto na raiz = rotear; prova = `ProcessSpawnE2ETest` 4/4 paridade byte JVM==JS. Quirk honesto preservado: stdin do filho e `/dev/null`, entao `write` publico e no-op nos dois alvos — alimentar entrada viva e mudanca de contrato (regra 6), nao bug de paridade.
> Deltas 19/09 (5): **`kof.shell` 2.2.3 landado — `runWith(argv, cwd, env)` no JVM + ligação no host JS, lane `.18`** — roda o argv num `cwd` escolhido (`""` herda o diretório do processo) com ambiente **aditivo** (as chaves do map sobrescrevem as herdadas; nunca limpeza silenciosa); erro de spawn e argv vazio devolvem `Result` honesto (`stderr` preenchido, `exitCode == -1`) nos dois alvos — nunca hang, nunca sucesso silencioso (R6). JVM `kof_shell_runwith` (ProcessBuilder) + shim JS → `KofJsRunner.processRunWith`; Native segue o `PROC001` de compilação herdado de `process.run`. `ShellE2ETest` 15/15 (goldens `pwd`/`printenv` JVM==JS byte-a-byte, pins de falha honesta, pin do gap Native, pin SEM025 de forma errada). Residual da linha 2.2 = só o `pipeline` JS com pipes vivos (item de plataforma `process.spawn` à parte). Plano `shell-plan.md` §5 2.2.3 ✅.
> Deltas 19/09 (4): **bundle 2.1.3 do `kof.workflow` COMPLETO — as cinco faces landaram, lane `.18`** — retry/deadLetter (ambas as faces)/schedule/checkpoint (3a) entraram hoje e a **supervisão (3b) entra agora**: `runSupervised(dag, nome, maxReinicios)` roda a DAG como workers supervisionados one_for_one DELEGANDO ao `kof.supervisor` (cada job = um child `transient`; o laço por filho reinicia só o que falhou; dependências = esperas cooperativas em flags de status voláteis; limite de reinícios estourado = drop + skip transitivo; guardas R6 altas: `maxReinicios < 1` recusado, `retry()` na mesma dag recusado — uma política de reinício por face). O host do supervisor vem flat com a face (dedupado pela marca quando `kof.supervisor` também é importado). Native roda a face REAL (sem stub — o núcleo OTP entrega nos 4 alvos, §129 portado). `WorkflowE2ETest` 20/20 (paridade byte JVM==JS + pin de compilação Native + pin de import duplo). Plano `workflow-plan.md` §5 2.1.3.
> Deltas 19/09 (3): **registry PULL landado — `kof deps` consome as releases do D2-A (1.5.3-S2)** — uma linha `owner/repo[@ver]` no `kofdeps` resolve o `<repo>-<ver>.tar.gz` da release pela API do GitHub Releases, verifica o `SHA256SUMS` embutido ANTES de instalar, faz cache em `~/.kof/deps/kof/<owner>/<repo>/<ver>/` (idempotente; `latest` puro pinna a versão concreta no `kofdeps`), e `kof build`/`kof run` consomem via `Deps.classpath()`; REG001–REG004 honestos (R6). `DepsRegistry.java`, `DepsRegistryTest` 6/6 num fake server do JDK (zero rede nos gates). Ver linha 117.
> Deltas 19/09 (2): **`kof deploy` ganha multi-target da mesma fonte + face `--publish` (X9 fatias 4–5, D-POLL-19/D2-A)** — `--target jvm,native,js`/`all` empacota uma release por face + `<name>-<v>.deploy-manifest.json` (linha 8.4); `--publish` sobe o tar.gz (+manifesto) ao GitHub Releases (reuso no 422; falha honesta sem `GH_TOKEN`) — `DeployPublish.java`, `CmdDeployTest` 15+2-skip num fake server do JDK (zero rede nos gates). Ver linha 116.
> Deltas 18/09 (14): **pipeline de APK real, pinada e provada no CI (§299, lane tooling/cli, X9)** — `ApkToolchain.pickBuildTools` exige build-tools **≥35** (d8 com `class major 65`/Java-21; hosts antigos = `DEP001` honesto com a condição documentada em learn/32 + KOFANDROID), `keytool` roda com `-d` + CWD fixo (não escrevia o keystore fora do diretório de execução) e o split `CmdBuild 647→467` + `ApkToolchain.java` (200) fecha o gate ≤500 (§303). Prova: o e2e `androidWithSdkPackagesApk` roda **sem skip** no CI ubuntu (`1b425a57`, `33363a3f` — SUCCESS) e `androidWithoutSdkIsHonestFailure` mantém R6 sem SDK.
> Deltas 19/09 (2): **MVP do `kof.workflow` aterrissou (fatia 2.1.2, lane `.18`)** — host pure-Kof `dev/kof/workflow-host.kf` injetado flat pelo `CompilerWorkflow` no `import kof.workflow` (DD-OTP-01 opção A, mecanismo do supervisor): `job(nome, () -> Bool)` + `.after(dep)` + `dag(listOf(...))` + `run() -> Report` (succeeded/failed/skipped/errors, `allOk()`, `summary()`); fixpoint sequencial determinístico, falha cascateia para dependentes transitivos, ciclo rejeitado no run com mensagem acionável; SEM gate de target (nenhuma fronteira de runtime na camada). Escopo mínimo Q2; retry/checkpoint/deadLetter = 2.1.3. Ledger R1 registrado ("workflow platform", 34 namespaces). `WorkflowE2ETest` 7/7. Plano: `workflow-plan.md` APROVADO 19/09.
> Deltas 19/09 (1): **MVP do `kof.shell` aterrissou (fatias 2.2.0–2.2.2, `34e4344f`, lane `.18`)** — `shell.cmd(program, args)` (lista argv, golden anti-injeção: `"a b|c && d"` permanece UM elemento), `shell.run(program[,args])` → `kof.process.Result` (abaixa para `kof_process_run`, paridade byte-a-byte JVM+JS), `shell.ok(r)` IR puro, `shell.pipeline([[...],[...]])` = cadeia real no JVM, JS/Native PROC001 honesto. Ledger R1 registrado (`stdlib_boundary.txt` "shell platform"). `ShellE2ETest` 11/11. Plano: `docs/shell-plan.md` SIGNED-OFF 18/09 (enquete Q1–Q3); glob/`~`/redirecionamento ficam FORA do v1 por decisão.
> Deltas 18/09 (8): **releases JS do `kof deploy` agora são AUTOCONTIDAS (§298, lane tooling/cli)** — `CmdDeploy.jsImportClosure` copia o fecho transitivo de imports relativos do entry (`kof-runtime.mjs` + quaisquer `./kof-runtime-*.mjs`) para o diretório da release, hasheia cada arquivo embarcado no `SHA256SUMS` e os adiciona ao `.tar.gz`, então o `node <name>.mjs` prometido pelo `RELEASE.md` funciona num diretório limpo (antes: `ERR_MODULE_NOT_FOUND` — o teste de verde fraco não executava nada; agora ELE RODA node e asserta a saída do programa, com guarda `assumeTrue(node)` honesta). Correção companheira `90ea7c34`: o pipeline do APK android normaliza `projDir` para caminho absoluto depois que o CI ubuntu expôs um caminho relativo duplicado pelo CWD (keytool `FileNotFoundException`). `DEP001` segue honesto para cross riscv64/aarch64 + `--publish` (D2).
> Deltas 18/09 (9): **completion domain-aware completo dos receivers tipados (X10 slice 3, docs→platform lane)** — `StdCatalog` 18→**31 namespaces**: json(encode/decode), log(debug..error), orm(11), config(8), gpu(13), mq(7), validation(27), observability(15), tetris(run) e os estáticos de mídia Image/Audio/Video/Mic — cada lista travada contra o `case`-literals do SEU dispatcher real (`log` via `isLogMethod`, mídia via `case ns -> switch(name)` aninhado, json via os literais `.equals(mc.methodName())` do validador inline); todo membro resolve no dispatcher por probe (argCount 0..8 para media/tetris; `Int[]`x`Int` para gpu). web/app-DSL + ui + ffi ficam honestamente fora (R6). (`9e4d1728`, `LspServerTest` 25/25, `StdCatalogTest` 10/10)

> Deltas 18/09 (13): **hover de símbolos do projeto no `kof lsp` (X10 fatia 7, lane docs→plataforma)** — quando `LspHover` não conhece o nome, `LspProject.declarationLine` busca a declaração no buffer (fonte da verdade) e nos `.kf` irmãos e mostra a LINHA completa + arquivo de origem em ```kof```; desconhecido = `null` honesto (R6). Bug real exposto pela fatia: o parser de frames do teste fatiava por CHAR um `Content-Length` que é BYTES UTF-8 — payloads não-ASCII quebravam; helper refeito byte-safe. (`848b7df1`, `LspServerTest` 30/30)
> Deltas 18/09 (12): **`workspace/symbol` no `kof lsp` cobre o projeto inteiro (X10 fatia 6, lane docs→plataforma)** — `LspProject.workspaceSymbols` une os símbolos dos buffers abertos (fonte da verdade) com os `.kf` irmãos não-abertos (dedupe por path absoluto, walker das fatias 4–5); filtro substring case-insensitive; ordenação prefixo→substring→nome→uri (convenção LSP). Lógica no `LspProject` (LspServer estaciona em 578 < 600). (`c04e16a4`, `LspServerTest` 29/29)
> Deltas 18/09 (11): **referências cruzam arquivos no `kof lsp` (X10 fatia 5, lane docs→plataforma)** — `references()` agora varre os `.kf` irmãos do projeto além do buffer (mesmo walker da fatia 4; somente-leitura). Varredura extraída para `LspProject` (split obrigatório: LspServer ia 603 ≥ 600 CRÍTICO → 572; estrutura-only, comportamento idêntico). Rename cross-file fica de fora: WorkspaceEdit em arquivos não-abertos = decisão de superfície (rule 6). (`f5df2362`, `LspServerTest` 28/28)
> Deltas 18/09 (10): **ir-para-definição cruza arquivos no `kof lsp` (X10 fatia 4, lane docs→plataforma)** — quando o nome não é declarado no buffer, `LspServer.definition` percorre a árvore do projeto (profundidade ≤6, ordenada, primeiro hit) e devolve o Location exato do `.kf` irmão reusando `LspSymbols.declarationRange` (a própria convenção do LSP — sem parser paralelo, sem API de AST nova); nome desconhecido = `null` honesto (R6, nunca chute). (`5837eca7`, `LspServerTest` 27/27 — 2 testes novos com `@TempDir` de dois arquivos). Nota de coordenação: o split ≤500 do `CmdBuild` (gate quebrado em 647 pelo §299) já foi consertado em `ea0ac82b` pela lane apk — o split duplicado desta lane foi descartado ANTES do push.
> Deltas 18/09 (7): **completion domain-aware da stdlib no `kof lsp` (X10 fatia 1, lane docs→plataforma)** — novo `dev.kof.compiler.StdCatalog` expõe os membros reais dos 7 namespaces roteados pelo `KofStd.staticMethod` (math, strings, encoding, net, uuid, random, rng); após `rng.` o editor recebe os 5 nomes do typer (kind Function, detail `kof.rng`) — sem parser paralelo: `functions()` é transcrição dos `case`-literals dos próprios typers e o `StdCatalogTest` trava as duas pontas contra a fonte (lista == case-literals, dispatch do KofStd == chaves do catálogo, todo membro resolve no typer). Prefixo não-stdlib = zero invenção. time/json/http/db/... ficam na fatia 2 (dispatch próprio). (`8a2c26b7`, `LspServerTest` 23/23)

> Deltas 18/09 (6): **face nativa x86_64 do `rng` fechada (X8 fatia 2, lane docs→plataforma)** — asm `RuntimeRng` (xorshift128/splitmix32, mesmos imediatos e shifts dos fragments JVM/JS — bits idênticos por construção) entrou no mapa de fatias do runtime; `KofRngTest.jvmNativeParityFullFace` RODA o mesmo `.kf` no JVM e no binário nativo e exige stdout byte-idêntico; `RNG001` agora cobre SOMENTE cross riscv64/aarch64 + ANDROID (honesto em compile). Um bug de roteamento pego pela prova: o special-case `random_double` emitia `call kof_random_double` hardcoded até para `rng.double()` (chamava entropia do SO, 0 nexts consumidos, sequência de ints deslocada) — agora por nome. (`367af29d`, `KofRngTest` 11/11)

> Deltas 18/09 (5): **namespace `rng` — PRNG determinístico semeável (X8 fatia 1, lane docs→plataforma)** — `rng.seed(n)`/`rng.int(bound)`/`rng.boolean()`/`rng.double()`/`rng.string(n, alpha)` em JVM+JS (xorshift128 + splitmix32, aritmética 32-bit exata: mesma seed ⇒ mesmos bits nos dois backends, `KofRngTest` 8/8 incl. paridade byte-a-byte JVM↔JS e oracle Java no próprio teste). `RNG001` é o gap honesto de compile em NATIVE/NATIVE_RISCV64/NATIVE_AARCH64/ANDROID (fatia 2 = asm nativo). NUNCA para material de chave — isso fica em `random`/`security` (entropia do SO, R11).

> Deltas 18/09 (4): **callbacks/upcalls da FFI também bindam no host runner JS (R3.4-C3.1→C3.3)** — o `ProxyExecutable` do `KofJsRunner` marshala um callback montando um `Linker.upcallStub` sobre o valor de função Kof; **o valor de função compilado é um objeto `Lambda…` com método `invoke` (não uma arrow JS nativa), então a ponte chama `fn.getMember("invoke").execute(...)`** (o enquadramento `Value.execute`-em-arrow do pin C3.1 foi corrigido); `JvmFfiCallbackE2ETest.jvmAndJsCallbacksMatchByteForByte` prova paridade byte-a-byte JVM↔JS de callback (`42/42/6.0/7.5` Int/Long/Double/mistas); browser = degrade honesto (R7); um callback não-bindável (`String`-in-callback) segue `FFI002` no JS. Paridade de callback JVM+JS FECHADA; gaps R3 restantes inalterados (structs/D6, variadics, handles opacos, Native §61).
> Deltas 01/09: **`spawn` com lambda que captura variável externa** (`SpawnStmt` emitia a lambda com zero capturas → `VerifyError`/valor errado; agora coleta via `collectCaptures`); **`&&`/`||` booleanos short-circuitam no JS** (backend emitia `&`/`|` bitwise que avalia os dois lados; agora `&&`/`||` JS para `bool`, bitwise intacto); **`Channel<T>` como parâmetro de função** (tipo saía com package vazio e o `isChannel` exigia `kof.concurrent` → mapeado como builtin + `JvmTypeMapper` → `LinkedBlockingQueue`); **`println`/`print` antes de `spawn` no Native** e o bug pré-existente **`spawn→await→spawn`** (SIGSEGV no `pthread_create` — stack chegava desalinhada ao C call; alinhado com `andq $-16` preservando `r15` + frame); **KofJS no browser real** (`KofJsBrowserE2ETest` — Chrome headless + HTTP + DOM); **`SECN002` fechado — AES-256-GCM no KofJS** (AES-GCM puro JS: FIPS 197 key expansion + GCM CTR/GHASH, formato `aesgcm$iv$ctTag` idêntico ao JVM/Native; tamper detectado por decode base64 estrito `strict=true` — paridade cross-target JVM↔JS testada); **`OBS002` fechado — histogram/metrics no Native** (store asm 32B name+sum+count + export Prometheus via `kof_string_concat` — paridade de conteúdo com o JVM); **`TIME001` (Native) — `time.interval`/`time.cancel`** (mesmo mecanismo do `scheduler.every`/`cancel`, SCHED001: thread por job; alias `kof_time_interval`/`kof_time_cancel` → `jmp kof_scheduler_every`/`cancel`; mutação por referência da captura validada); **`TIME001` fechado no JS (02/09) — fila cooperativa de timers bombeada por `time.sleep`** (GraalJS não tem `setInterval`; browser/Node usam o nativo — `KofTimeE2ETest.jsIntervalRunsPeriodicallyUntilCancelled`); **`MQ001` fechado — kof.mq no Native** (pub/sub + filas in-process em asm: store `.bss` + nodes 40B, invoke-com-arg `rdi`=fn `rsi`=msg, unsubscribe por identidade do objeto fn — paridade de output JVM↔Native↔JS); **`Set<T>`/`Map<K,V>` como campo/retorno de classe no JVM** (o `JvmTypeMapper` mapeava só `List`→`ArrayList`, então `Set`/`Map` viravam `Lkof/Set;`/`Lkof/Map;` → `NoClassDefFoundError`; agora `HashSet`/`HashMap`; + parse de método de classe com retorno genérico `Set<Int> all(` — `KofMapSetTest.setMapAsFieldAndReturn`, 3 targets); **Query DSL tipada do ORM (nível 3, `ORM001`)** — `User.query(db){ where; orderBy; limit }` baixado para `db.query<T>` (SQL preparada em compile-time, valores como binds; `KofOrmE2ETest` 22); **source map V3 do KofJS** (mappings VLQ reais em nível de linha: cada função gerada → linha Kof via `KofDebugInfo` — antes era stub `"mappings":""`; `KofJsSourceMapTest`).
> Deltas 02/09: `Set<T>` como tipo declarado no JVM (`kof.Set` → HashSet); null-safety narrowing JVM corrigido (`s.length`/`s.substring(...)`/`if_acmp*`); `mapOf` infere o primeiro par; `String? s = null` prefixado parseia; `readText`/`readFile` → `String?` (Native devolve `null`); `size()` lança em vez de `-1`; `Map.get` → `V?` (referências); gaps `STR001`/`STR002` documentados abaixo.
> Deltas 03/09 (main): **aarch64 native core completo** (tradução riscv→aarch64, NATIVE002 13/13); **kof.deps** (package manager MVP, Maven Central); **lifecycle** `application { onStart/onShutdown }`; **observability spans W3C** (3 targets) + metrics removidos; **LOG001 JS**; **TIME001 JS**.
> Linhas "Deltas" registram o histórico por release (0.1.0→0.4.0-beta). Build atual: `mvn test` **3225** (2762 kof-compiler + 50 kof-script + 7 kof-c-compiler + 406 kof-cli), **0 falhas / 0 erros**, 221 skip (cross roda no job dedicado com qemu; guardas de toolchain/DB externo + sysroot §255) — job CI Build+Tests de `404d8be6` (20/09 ~18:14, primeiro verde na `beta-0.5.0`, reator `Kof 0.5.0-beta`). O flake §252 e os residuais §181/§256 seguem fechados no código. A matriz reflete o estado corrente dos 3 targets. DoD em `docs/development/DECISIONS.md` §D-PLAT → `docs/architecture/performance.md` §40–§41.

---

## Tabela de Paridade

| Feature | JVM | Native | KofJS | Notas |
|---------|-----|--------|-------|-------|
| Literals (int, long, float, double, string, bool, char, null, hex) | ✅ | ✅ | ✅ | hex: `0x...` |
| Variáveis, `var`/`val`, inferência | ✅ | ✅ | ✅ | |
| Aritmética, bitwise, unário | ✅ | ✅ | ✅ | |
| if/else | ✅ | ✅ | ✅ | |
| if-expr (`var x = if (c) a else b`) | ✅ | ✅ | ✅ | |
| while, for, do-while, break/continue | ✅ | ✅ | ✅ | |
| for-in (`for (var x in coll)`) | ✅ | ✅ | ✅ | |
| switch (`case N:`) | ✅ | ✅ | ✅ | |
| Funções (todas as formas, sem `fun`) | ✅ | ✅ | ✅ | |
| Records (`record Point(Int x, Int y)`) | ✅ | ✅ | ✅ | toString/equals/hashCode |
| Classes, campos, métodos | ✅ | ✅ | ✅ | |
| `constructor(...)` e primary `class X(...)` | ✅ | ✅ | ✅ | |
| Herança, `super`, override | ✅ | ✅* | ✅ | Native: `super.metodo()` = SUP001 |
| Virtual dispatch | ✅ | ✅ | ✅ | |
| Interfaces | ✅ | ✅ | ✅ | |
| Generics (erasure) — `Box<T>` `T` primitivo | ✅ | ✅ | ✅ | 25/08 `substituteTypeVariable` + `kof_int_to_string` |
| Tipos `sealed` + `switch` exaustivo (X5.1/X5.2, `SEM080`/`SEM081`) | ✅ | ✅ | ✅ | 0.5.0-beta — modificador apagado na emissão; conjunto fechado por mesmo arquivo; `switch`-expr precisa cobrir os subtipos diretos. `SealedTypeE2ETest` 9/9 + matriz `sealedswitch` |
| variância `out`/`in` (declaration-site) + projeção no sítio de uso `List<out T>`/`List<in T>` (X5.3/X5.4, `SEM082`) | ✅ | ✅ | ✅ | 0.5.0-beta — só compile-time (vira `WildcardType` na erosão); `TypeVarianceE2ETest` 9/9 · `UseSiteVarianceE2ETest` 5/5 · matriz `variance`/`useproj` |
| Reflexão na fronteira `interop.schema(R)` (X6.0–X6.2, `D-INTEROP-REFLECT`) | ✅ | ✅ | ✅ | 0.5.0-beta — `import kof.interop`; intrínseco de compile-time (zero reflexão em runtime, mesma saída nos 4 alvos, sem `REF001`); `List<Field>` imutável + `record Field(String name, String type)` do compilador; `INTEROP001`/`INTEROP002`; `InteropSchemaE2ETest` 18/18 · matriz `interopschema` |
| Reflexão na fronteira `interop.schema(R)` (X6.0–X6.2, `D-INTEROP-REFLECT`) | ✅ | ✅ | ✅ | 0.5.0-beta — `import kof.interop`; intrínseco de compile-time (zero reflexão em runtime, mesma saída nos 4 alvos, sem `REF001`); `List<Field>` imutável + `record Field(String name, String type)` fornecido pelo compilador; `INTEROP001`/`INTEROP002`; `InteropSchemaE2ETest` 18/18 · matriz `interopschema` |
| Lambdas `(x: Int) -> expr` | ✅ | ✅ | ✅ | com capturas (box `BoxN`) |
| Exceptions (throw "msg", try/catch/finally) | ✅ | ✅ | ✅ | Native: unwinding próprio |
| `assert(cond[, msg])` | ✅ | ✅ | ✅ | |
| `spawn` stmt / `spawn f()` / `await` / `poll` / `done` / `cancel`+`cancelled` / `selectAny` / `awaitTimeout` / `channel<T>` send/receive / `scheduler.every`+`cancel` (Handle<T>, unbox, exceção limpa; `at(cron)` = cron real 5 campos UTC em JVM/JS, Native CRON001) | ✅ (virtual threads; canal = LinkedBlockingQueue; scheduler = ScheduledExecutor) | ✅ 31/08 (pthread_create + trampoline + pthread_join, allocator futex — CONC001; awaitTimeout = polling 1ms; canal = FIFO futex; **scheduler SCHED001** = thread por job + `usleep` ms→us + flag `active`; **cross riscv64/aarch64: clone 220 + nanosleep 101 + spinlock `amoswap.w`; helpers CONC001 15/09 = fatia `RtB48` (done/poll/cancel/cancelled/selectAny/awaitTimeout; cancel por TID real gettid(178) no ctid do clone; supervisor ✅ 19/09 — §129 port cross)) | ✅ 03/09 (CONC003 fechado — async/await/Promise reais; canal = fila de resolvers pendentes; `cancelled()` sempre `0`, limitação conhecida; scheduler = setInterval) | `KofAwaitTest` 8/8 · `KofConcurrency2Test` 41 (pós-§117 + helpers cross 15/09) · `SpawnE2ETest` 8/8 |
| Strings (`+`, `==`, length, charAt, substring, contains, startsWith, endsWith, indexOf, trim, case, replace, split) | ✅ | ✅ | ✅ | `STR001` fechado em UTF-16: x86_64 + JS + riscv64/aarch64 `length`/`charAt` (10–11/09, B33) + `substring`/`indexOf`/`lastIndexOf` riscv/aarch (11/09, B34 — walk de code-units fiel ao x86, trampolins `kof_su_*`). **Resíduo de contrato (x86, não cross):** `"ab".substring(0,0)` → `"ab"` (marker `end=0`="até o fim" colide com `end=0` explícito; JVM: `""`) — decisão da mantenedora |

| Arrays (`new Int[n]`, `arr[i]`, `.length`) | ✅ | ✅ | ✅ | |
| `List<T>`, `listOf`, for-in | ✅ | ✅ | ✅ | |
| `Map<K,V>` + `mapOf` (put/get/remove/contains/size/keys/values/clear/isEmpty) | ✅ HashMap | ✅ asm próprio | ✅ JS Map | `KofMapSetTest` — **campo/retorno de classe 01/09** (mapper `Map`→`HashMap`; parse de método c/ retorno genérico) |
| `Set<T>` + `setOf` (add/contains/remove/size/clear/isEmpty) | ✅ HashSet | ✅ asm sobre List | ✅ JS Set | tag de tipo no Native; **campo/retorno de classe 01/09** (mapper `Set`→`HashSet`) |
| `enum` + values/valueOf/name + switch exaustivo (`SEM031`) | ✅ | ✅ | ✅ | enum→String nos descritores; `KofEnumSwitchTest` |
| JSON encode/decode (primitivos, List) | ✅ | ✅ | ✅ | arrays `JSN003` fechado; `Double[]`/`Float[]` no Native (JSN001, 31/08) |
| JSON objetos/records (JSN002) | ✅ | ✅ 31/08 | ✅ | composição em compile-time no Native |
| `kof.io` (File, Path, Directory) | ✅ | ✅ | ✅ | `readText`/`readFile` → `String?` (`null` p/ ausência, 02/09); `size()` lança em vez de `-1` |
| `readLine` (stdin) | ✅ | ✅ | ✅ | `String?` — `null` no EOF (02/09; Native antes devolvia `""`) |
| kof.io `readText`/`size` | ✅ | ✅ | ✅ | `STR002` histórico fechado 02/09: Native `readText` devolve `null` (antes encerrava); `size()` sem sentinela `-1` |
| kof.time (`now()`, `sleep`, `interval`) | ✅ | ✅ | ✅ | `interval`/`every` JVM (ScheduledExecutor) + **Native (01/09, `time.interval` reusa o scheduler — SCHED001; mutação por referência validada; **cross 05/09** — alias `j kof_scheduler_every`/`cancel` no runtime riscv64/aarch64)** + **JS (TIME001 fechado 02/09; §132 18/09 — `time.sleep` agora é ponto de await: sleep cooperativo async devolvendo Promise, dirigido pela bomba do host `KofJsRunner` para que tasks spawnadas irmãs/filhas progridam enquanto uma task dorme)** — fila de interval disparada por essa mesma bomba do host (`__kofSleepStep`) no GraalJS, `setInterval`/`setTimeout` nativo em browser/Node. Calendário civil + data ISO (`isLeapYear`/`daysInMonth`/`dayOfWeek`/`daysBetween`/`addDays`/`diffDays`): **TIME002 fechado 11/09** — `addDays`/`diffDays` nos 5 targets (x86 `RuntimeTimeIso` + riscv64 fatia B33 + aarch64 traduzida; golden sob qemu). `time.collect()` (GC manual): JVM/SCRIPT (runtime JVM) + x86 (`RuntimeGc`) + riscv64/aarch64 reais; **JS real** (§426 melhorado 25/09 — a face `kofGcCollectNow` que o backend já importava agora é EXPORTADA e roteia para o GC do host, `kof_platform.gcCollect` → `System.gc()`, a mesma semântica do JVM/SCRIPT; browser/sem-host = erro honesto em runtime, nunca no-op silencioso) |
| `readLine`, `readFile`, `writeFile` | ✅ | ✅ | ✅ | |
| `kof.validation` (13 preds) | ✅ | ✅ | ✅ | `KofValidationTest` |
| `kof.math` (`clamp/abs/sign/min/max/isEven/...` Int + `sqrt/lerp/percentage/isInteger/isDecimal` Double) | ✅ | ✅ | ✅ | Escalares Int nos 4 alvos; **Double (S1b/S1b.1): JVM/Script/JS/x86 (SSE2 `sqrtsd`/`cvttsd2si`/`ucomisd`, bits crus via rax) + riscv64/aarch64 — MATH001 fechado 11/09** (fatia B36 `fsqrt.d`/`fmul.d`/`fdiv.d` + `fcvtzs`/`fcvt.d.l`; bits crus via a0..aN = modelo "rax cru" do x86; golden 20 vetores idêntico nos 5 targets sob qemu). `Double as Int` aarch64 corrigido junto (tradutor: `fcvt.w/l.{s,d}`→`fcvtzs`, era `scvtf` invertido — §120). **`roundTo(value, decimals)` (S1b.3, DECISIONS §3) FEITO 14/09 nos 5 alvos** — arredonda-meio-para-longe-do-zero por escala decimal determinística, sem libm (`p=10^\|d\|` por multiplicação repetida; `d<0` arredonda p/ dezenas/centenas); contrato aritmético `roundTo(2.675,2)==2.68`; matriz `stdmathround` + `KofMathTest.roundToCrossArch` sob qemu. `pow` = libm (x86 `pow@PLT`; riscv/aarch MATH001). Bug 94: `NaN==NaN` só no interpretador (semântica congelada). |
| `kof.supervisor` (núcleo OTP issue #83: `import kof.supervisor` → `supervisor(name).child(id,fabrica,politica).restartLimit(max).escalate(cb).start()/.stop(deadlineMs)/.stats()`) | ✅ | ✅ (x86 · riscv64 · aarch64) | ✅ 18/09 (§132) | Host puro-Kof injetado como pacote virtual (resource `dev/kof/supervisor-host.kf`, mecanismo idêntico ao `android-host`: só age no `import kof.supervisor` explícito — zero falso-positivo). Observa falha do worker por `try{await}catch` por filho (laço `vigiar` em thread dedicada), reinicia com **fábrica nova** (DD-OTP-06), respeita restartLimit (para + R6 sem escalate), stop cooperativo `cancel`+deadline, stats(started/restarts/dropped/vivos). Gate DD-OTP-11: worker falha 2x → termina na 3ª → restarts=2/fabrica=3. NATIVE x86 ✅ 15/09: §129 fechado (DECISIONS §2 opção B — `kof_exc_chain` agora é TLS por thread e o trampolim do spawn instala handler por worker; `await`/`selectAny` relançam a causa). riscv64/aarch64 ✅ 19/09: §129 port cross — tabela de cadeia por-TID `kof_exc_slots` + `kof_exc_slot()` (a rota TLS-via-`tp` foi provada ABI-insegura e abandonada); gate `OTP001` removido, `crossGateOtp001` roda o APP sob qemu nas 2 arches. JS ✅ 18/09 §132 resolvido: `time.sleep` agora é ponto de await (sleep cooperativo async — o compilador colore async o método que o alcança via `computeAsyncColoring`, `kofTimeSleep` devolve Promise, e a bomba do host `KofJsRunner` a drena), então uma task spawnada de dentro de outra roda enquanto o poller dorme — o worker dispara e `OTP002` é levantado. `KofSupervisorE2ETest` 16/16 (JVM+Script+Native x86+JS rodam, limite, gate cross OTP001, sem-import não injeta). Impeditivo §130 corrigido junto (SEM024 falso em corpo de método re-analisado no mesmo escopo). |
| `kof.observability` (health/metrics/requestId + spans) | ✅ | ✅ | ✅ | `KofObservabilityTest` 12/12 (spans JSON x86_64/riscv64/aarch64 == golden JVM — §272 fechado 18/09); **`exportSpans()` (OTel OTLP/JSON) = `OBS003`: JVM/JS ✅, gap em compile-time no Native** |
| `kof.media` (`Image`/`Audio`/`Video`/`Mic` + `app.serveDir`) | ✅ `javax.imageio`/`javax.sound` (`KofMediaE2ETest`) | **MEDIA001** (handles) / **MEDIA003** (mic) / **WEB005** (`serveDir`) — gap em compile-time | **MEDIA001** / **MEDIA003** / **WEB005** — gap em compile-time | só JVM: alvos não-JVM diagnosticam em compile-time (R6), nunca stub silencioso; `MEDIA003` também cobre hardware ausente |
| `kof.gpu` (compute Vulkan: `available`/`failReason`/`dispatchMatmul`/matvec; namespace `gpu.*`) | ✅ FFM Vulkan (real; link-por-uso + `--enable-preview` só em programas GPU) | ✅ stubs asm (`available=false`, dispatch devolve o fallback CPU) | **GPU001** — gap em compile-time | semente FFI do plano universal (§GPU): JS não tem suporte (`KofGpu.supportedOn` falso → `GPU001`); stub Native sinaliza fallback, nunca resultado errado silencioso |
| `kof.http` client | ✅ | Native HTTP/1.1 asm (socket/recv/send parse; https→throw; DNS não-IP→127.0.0.1; retry knob no-op) | ✅ GraalJS síncrono (`Java HttpClient` interop); Node/browser = fetch real **assíncrono** (`spawn http.* + await` → Promise; §133 — a face síncrona em JS puro é registrada como HTTP004, não emitida como código) | `KofHttpE2ETest` 8/8 (JVM+JS+Native real resp; `jsNodeSpawnAwaitHttpResolvesBody` no node); retry/circuit paridade 30/08 |
| `kof.cache` (get/set/ttl/delete/clear) | ✅ | ✅ 30/08 (fix clobber `%rax`/`%rdi`) | ✅ | `KofCacheE2ETest` 5/5 x3 targets |
| `kof.mq` (pub/sub + queue) | ✅ | ✅ (01/09, pub/sub + filas in-process, asm; **05/09 port completo riscv64/aarch64** — queue por handle, pop via `kof_list_remove`, queue_size, unsubscribe por identidade, scan loops sem clobber) | ✅ | `KofMqE2ETest` 5/5 (JVM+Native+JS + **cross riscv64/aarch64 qemu** c/ paridade de output) |
| `kof.config` (tipado) | ✅ | ✅ (asm próprio) | ✅ 16/09 (CONF001 fechado) | precedência total Native (`KOF_CONFIG` > env > profile > `kof.config`); `NativeConfigE2ETest` 8 |
| `kof.log` | ✅ (JSON + correlation ID) | ✅ (asm; UTC, sem JSON) | ✅ 01/09 (console.* + nível) | `KofLogE2ETest` 11 (incl. JS) + `NativeLogE2ETest` 7. **Cross (riscv64/aarch64)**: `[LEVEL] msg` sem timestamp (x86_64 usa `UTC LEVEL msg`) — gap de formato não-determinístico, não falha silenciosa (nível+mensagem presentes) |
| `kof.process` (`process.run`/`process.spawn` — subprocesso com stdout vivo; stdin sob `/dev/null` — §355) | ✅ (ProcessBuilder; ops de handle do spawn REAIS desde 19/09 — elas nunca rodaram em nenhum alvo antes do §355) | `run` ✅ x86-64 + riscv64/aarch64 (cross 26/09); `spawn`+ops de handle ✅ x86-64 26/09 (`RuntimeProcessSpawn`; cross/MCU `PROC001` — fatia B) | ✅ (host GraalJS — `run` desde 18/09, `spawn`+ops de handle desde 19/09 via `KofJsProcessBridge`, paridade byte `ProcessSpawnE2ETest` 4/4) | JVM/JS rodam subprocessos reais com handles vivos; o Native roda `run` no x86-64 e no riscv64/aarch64 (cross 26/09) e `spawn`+ops de handle no x86-64 (paridade byte `ProcessSpawnNativeE2ETest` 4/4); `spawn` no cross/MCU recusa em compile-time (PROC001 — R6) |
| `kof.shell` (`shell.cmd`/`run`/`runWith`/`ok`/`pipeline` — acúcar sobre `kof.process`, argv-como-lista, nunca `sh -c`) | ✅ (abaixa para `kof_process_run`/`kof_shell_runwith`; `pipeline` = cadeia JVM real `kof_shell_pipeline`; `runWith` = cwd + ambiente aditivo, `Result` honesto com `-1`) | ✅ as CINCO no riscv64/aarch64 (cross 26/09, `kof_process_run` + `NativeRiscvAsmShell`/`NativeRiscvAsmPipeline`); x86-64 `run`/`cmd`/`ok` ✅ + `runWith`/`pipeline` = PROC001 honesto (fatia B) | ✅ as CINCO reais — `pipeline` = cadeia no host + threads de pump em `KofJsProcessBridge` (20/09; goldens de paridade byte com 2 e 3 estagios) | 18–20/09, plano `shell-plan.md` SIGNED-OFF (enquete Q1–Q3 da mantenedora), `ShellE2ETest` 19/19 + `ShellCrossE2ETest` 7/7 |
| `kof.ssh` (`ssh.cmd`/`run`/`ok` — acúcar sobre `kof.process`, argv-como-lista, nunca `sh -c`) | ✅ (abaixa para `kof_ssh_argv`/`kof_ssh_run` → `kof_process_run`; BatchMode + ConnectTimeout, Result honesto) | ✅ x86-64 + riscv64/aarch64 26/09 (`RuntimeSsh`/`NativeRiscvAsmSsh` → `kof_process_run`, paridade byte do argv); MCU/riscv32 = **PROC001** | ✅ real (paridade byte com o JVM) | 21/09 x86 ✅ cross ✅ 26/09, plano universal Estágio 2 linha 2.3, `SshE2ETest` 10/10 + `SshCrossE2ETest` 2/2 |
| `kof.workflow` (MVP 2.1.2: `import kof.workflow` → `job(nome, () -> Bool)`, `job(...).after(dep)`, `dag(listOf(jobs...))`, `dag.run() -> Report{succeeded/failed/skipped/errors/retries, allOk(), summary()}`; bundle 2.1.3 COMPLETO 19/09: `flow.retry`/`retryFixed`, `deadLetter` (duas faces), `schedule`, `checkpoint`, `runSupervised(dag, nome, max)` = one_for_one via `kof.supervisor`) | ✅ | ✅ (paridade byte-a-byte com o JVM) | ✅ (pinado por compilação — a camada-core é um fixpoint sequencial pure-Kof que NÃO cruza fronteira de runtime; a face `runSupervised` CRUZA (workers spawn/await) e roda nos 4 alvos — o núcleo OTP entrega em todos desde §129; os gaps honestos `PROC001`/`CRON001`/`ORM001` moram nos CORPOS dos jobs escritos pelo usuário) | Host pure-Kof injetado flat como pacote virtual (resource `dev/kof/workflow-host.kf`, mecanismo copiado de `CompilerSupervisor`/`android-host`: só age no `import kof.workflow` explícito — zero falso-positivo; nomes `KofWf*` definidos pelo usuário suprimem a injeção; a fatia supervisionada também puxa o host do supervisor flat, dedupado pela marca dele). Superfície mínima Q2 + o bundle completo de add-ons 2.1.3; falha cascateia para dependentes transitivos; ciclo rejeitado no RUN com mensagem acionável (invariante §2, nunca silêncio). `WorkflowE2ETest` 20/20 com goldens exatos de stdout. Plano `workflow-plan.md` §5. |
| `kof.security` (passwords/crypto/jwt/secrets + G9) | ✅ | ✅ x86_64 (asm) · **SECN002** chacha20 / **SECN006** cookies · **SECN000** riscv64/aarch64 (sem runtime `kof_sec_*`) | ✅ (JS puro; **SECN007** resourceServer é JVM-only) | PBKDF2/SHA512/JWT/AES-GCM (asm no Native x86, JS puro no KofJS — face **AES** do `SECN002` fechada 01/09). `KofSecurity.supportedOn` faz gate por função/target; medido 17/09: `crypto.encryptChacha20` no Native → `SECN002`, `security.cookieSet` no Native → `SECN006`, `crypto.sha256` em `native.risc` → `SECN000`. Ver as linhas de Documented Gaps |
| `auth.resourceServer` (OAuth2 resource server, D-SEC camada 16) | ✅ JVM (JWKS RS/ES, iss/aud/exp, sem confusão de algoritmo; `KofOAuthResourceServerTest` 4/4) | **SECN007** | **SECN007** | valida JWT de terceiro; integra com `auth.authenticated`/`app.security` |
| `web.app().security()` (D-SEC C18, middleware composto) | ✅ JVM (ordem fixa rate-limit→CORS→headers→cookies/session→csrf→auth→RBAC; `KofWebE2ETest` 25/25) | **WEB006** | **WEB006** | `app.security([opts])`; opts `headers/cors/rateLimit/csrf/auth/roles`; auth-if-present; aviso em produção |
| `kof.db`/`kof.orm` | ✅ | ✅ (SQLite `.so` direto; **`transaction {}` commit/rollback 01/09** (EH asm + BEGIN/COMMIT/ROLLBACK); **MySQL wire protocol** — handshake+scramble+auth-switch+COM_QUERY+resultset 31/08 + **prepared statements binários 03/09**) — **`kof.orm` x86 SQL-puro FECHADO 20/09 (F1+F3a)**: `deleteAll`/`create`/`migrate`/`count`/`count`-com-`where` em asm `kof_orm_*` (fatias `RuntimeOrm1/2/3`) com DDL byte-idêntico ao host (golden mede o `sql` salvo em `sqlite_master`: `PRIMARY KEY AUTOINCREMENT` + `UNIQUE`) e binds via `prepare_v2`/`bind_*` (injection-proof); **F2a `save` row-object FECHADO 20/09** — INSERT-sem-pk + generated key (`last_insert_rowid`) devolvendo NOVA instância (cópia `kof_alloc`+header+slots), UPDATE-por-pk devolvendo a MESMA instância, miss→INSERT-all (upsert), binds pelo typeCode do schema (`RuntimeOrm4` + `RuntimeOrmSchema`/`RuntimeOrmBind`, JVM==Native byte); **F2b `find` row-object FECHADO 20/09** — `SELECT` por pk, miss→`null` como o host, record construído no runtime (resolver `kof_orm_ctors` className→vtable/typeId/size; colunas casadas por NOME, leitura por typeCode, bool por tipo dinâmico da coluna (§397 pós-fix: INTEGER/FLOAT `!=0`, TEXT literal "true") — key chega KofString pelo coerce do call-site, medido), JVM==Native byte; **F2c `all` row-object→List FECHADO 21/09** (`RuntimeOrm6`: mesmo loop de campos do find + `kof_list_new`/`kof_list_add` por linha, lista vazia ≠ null, oracle JVM medido ANTES da asm, JVM==Native byte); **F2c2 `where`/`where_op` FECHADO 21/09** (`RuntimeOrm7`: um corpo, dois globls; 7º arg `className` lido da stack na entry — S7f/SysV; whitelist do op idêntica ao host com throw medido `ORM operator not allowed: <op>`; GC-safe — A/B empilhados durante o alloc do concat, scan conservativo); **F2c3 `page`/`delete`/`saveAll` FECHADO 21/09 — row-object x86-64 completo** (`RuntimeOrm8/9/10`: page = `LIMIT ? OFFSET ?` bindados com trunc int32 = `intValue()` do host, KofString do coerce = atoi superset honesto documentado; delete = PK do schema via `parse_schema` + bind do key (classificador do Orm7), `true` sempre no DONE como `execute1 >= 0` do host — miss deleta 0 linhas e retorna true, medido no oráculo; saveAll = loop `kof_list_size`/`kof_list_get` → `kof_orm_save`, instância patchada descartada como no host); provas: `KofOrmE2ETest` `pageDeleteSaveAllNativeEndToEndMatchesJvm` + `pageEdgesDeleteMissSaveAllEmptyNativeMatchesJvm` byte JVM==Native (batch, offset além, miss, lista vazia), pin honesto migrou p/ cross riscv64; restam `ORM001`: **F2d1–F2d7 runtime-MySQL x86-64 FECHADO 22/09 — 13/13 faces `kof_orm_*` sobre o wire** (delete_all/count/delete/find/all/where/where_op/page/saveAll/save/count_where/create/migrate; prova byte JVM==Native por face, `KofOrmE2ETest` 60/0F); restam `ORM001`: SÓ os espelhos cross riscv64/aarch64 (compile-time); **F2b `find` (peça `RtB57` + `RtB57Helpers` + `NativeRiscvOrmCtors` por-programa) + F2c1 `all` (peça `RtB58`) + F2c2 `where`/`where_op` (peça `RtB59` — whitelist do op idêntica ao host com throw `ORM operator not allowed: <op>`, bind do value pelo classificador do Orm7, um corpo/dois globls; 7º arg em a6 no call-site riscv) + F2c3 `page` (peça `RtB60` — `SELECT * FROM "t" LIMIT ? OFFSET ?` bindados, coerção `intValue()` do host: int direto, long/double/float truncam p/ int32, KofString = atoi, null = 0) REAIS 23/09** (leitura row-object no cross — find miss→null, all lista vazia≠null, where/page com golden x86 explícito + byte-parity riscv64/aarch64; **row-object de leitura COMPLETO: 13/13 faces, nenhuma `ORM001`**; §449 x86 resolvido: resolver multi-entidade + `Double` `movsd` em `RuntimeOrm5/6/7/8` + paridade do `bind_key`) | ✅ não-tipado (16/09); `query<T>` = `DB002` FECHADO 18/09; ORM FECHADO 18/09 (`ORM001`) | **Query DSL `User.query(db){ where; orderBy; limit }` (nível 3) 01/09** — baixa p/ `db.query` (JVM E2E H2); MySQL native `nativeMysqlWireProtocol` + `nativeMysqlPreparedBinary` (binário q/ binds) + `nativeMariadbAliasWireProtocol` (`mariadb://` = alias `mysql://`, 23/09); `nativeTransaction{Commits,RollsBack}`. **riscv64/aarch64 SQLite FECHADO 15/09** (link-by-use `libsqlite3` via `NativeCrossLink.needsSqlite` + runtime `kof_db_*` fatias `RtB46/RtB47` — connect/execute/query/bind/close/transaction, `KofDbE2ETest` crossNativeSqliteRoundtrip sob qemu nas duas arches; ORM cross F1a+F1c+F1d+F3a+F2c3+F2a+F2b+F2c1+F2c2+F2c3 reais (`RtB50`/`RtB51`/`RtB52`/`RtB53`/`RtB54`/`RtB55`+`RtB55Helpers`/`RtB56`/`RtB57`+`RtB57Helpers`/`RtB58`/`RtB59`/`RtB60`, 22–23/09) — row-object de leitura COMPLETO, nenhuma face `ORM001`; MySQL só x86 (nos espelhos cross, recusa runtime honesta, R7)). **JS FECHADO 16/09 (DB001)**: `connect`/`connect2`/`close`/`execute`/`query`/`transaction` nao-tipados delegam a `kof_platform.db*` no host GraalJS (mesma JVM/classpath do caminho JDBC — `KofJsRunner`, ponte `KofJsDbBridge`; saida de `KofDbE2ETest.js{ExecuteAndQueryRowsAsJson,TransactionCommits,TransactionRollsBackOnFailure,NestedTransactionDoesNotCommitOuterScope}` byte-parity com JVM). **`db.query<T>` tipado = `DB002` compile-time** (JS nao emite bytecode JVM no classpath do host — `Class.forName` nao tem o que carregar; R6: alto, nunca silencioso). ORM no JS FECHADO 18/09 (`ORM001` — `KofJsOrmBridge`). |
| `web.app()` + TLS `listenSecure` | ✅ | server HTTP/1.1 (accept/route/lambda/body) — **TLS = WEB002 residual** | ✅ 03/09 (GraalJS `HttpServer` real — `bc577aa`) | `KofWebTlsTest` 7/7 (JVM) · `KofWebE2ETest` 25/25 · `KofWebNativeE2ETest` 4/4 |
| `web.app()` WebSocket `app.ws` + SSE `sse.*` | ✅ 30/08 (RFC 6455 + frame codec/máscara) | **WEB004** (ws) / **WEB003** (sse) — não portados p/ Native | **SSE ✅ 16/09 HANDLER-SCOPED** no host GraalJS (`app.sse` + `send/event/close/isOpen` + `sse()`, framing/headers idem JVM; push pós-return/multi-cliente = WEB003 residual, pump single-thread); **ws** = `WEB004` gate em compile 16/09 (antes `kofWebStub` 0 silencioso, R6); context-fns com runtime seguem ✅ | `KofWebWsE2ETest` 11/11 · `KofWebSseE2ETest` 7/7 · `KofWebJsE2ETest` sse 3 · `KofWsFrameTest` 7/7 · `KofWebHardeningTest` 6/6 |
| `status(code, body)` / `headerSet` | ✅ 27/08 | WEB001 (Native só suporta `body()` no contexto web; `status`/`headerSet` emitem WEB001 em compile-time — só `kof_web_body` em `contextNativeSupported`, 16/09) | ✅ 03/09 (JS HttpServer) | `KofWebE2ETest` 9/9 |
| UI Fase 7 Router (`go/replace/back/forward/param/current/depth`) | no-op | — | ✅ 31/08 | `RouterE2ETest` |
| `switch` pattern matching `case String s` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `switch` record destructuring `Point(x,y)` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `switch` como expressão `case X -> v` (SYN001) | ✅ | ✅ (x86_64/riscv64/aarch64) | ✅ (ternários aninhados) | 03/09 — `default` obrigatório ou exaustividade enum (`SEM032`); `KofSwitchExprE2ETest` 19 + riscv64/aarch64 14/14 |
| `String?` null safety básica | ✅ | ✅ | ✅ | 0.2.6-beta (`Type?`) |
| `Troolean` lógica tres-estado (Kleene; `Bool` nunca nulavel — `Bool?` → `SEM095`) | ✅ | ✅ (x86_64; cross via desugar front-end, guarda CI) | ✅ | 0.4.0-beta — D-TROOL 19/09, `TrooleanLawE2ETest` |
| escrita de campo nullable-primitivo (`b.n = 42` em `Int?/Long?/...`) — #278/§361 | ✅ (`e293c4a5`; FULL apos §368) | ✅ (`5cd078c1`; matriz 4-alvos do teste) | ✅ escrita (`0aa6a307`); ✅ leitura nunca-escrita = `null` (era `0` — §365 fix `JsClassEmitter.insertFieldDefaults`+`KofInterpreterMembers.defaultValue`; matriz 4-alvos `NullablePrimitiveFieldWriterE2ETest` 9/9 matrix361c) | §361 PARCIAL (face char String-literal ainda deve RED); §365 FECHADO 20/09 |
| `List map/filter/reduce` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `Box<T>` generic | ✅ | ✅ | ✅ | `substituteTypeVariable` |
| `KofScript` top-level `var`/`val` → `KofScriptGlobals` (execução direta via `KofInterpreter`) | ✅ | ✅ | ✅ | `KofScript` 0.3.0 |
| `KofCcompiler` (`kof c`) C subset | — | ✅ x86_64 native-only | — | `kof_c`, while/if/deref &/* |
| FFI C (`extern "<lib>"`, R3) | **Parcial** — qualquer assinatura sobre {Int, Long, Float, Double, Boolean, String} aridade livre/void/String-retorno + callbacks com argumentos primitivos E String, sincronicos/nao-escapantes (C2→C3.4); **struct/array JVM ✅ 3.8b (20–21/09)**: `record` por valor arg **e** retorno + `T[]` escalar→`ptr` + out-buffer `Buffer(U8)` INOUT (`buffer.alloc`/`Buffer.bytes()`; `FfiStructE2ETest` 10/10, `FfiArrayE2ETest` 5/5, `BufferE2ETest` 4/4, `BufferFfiE2ETest` 4/4); restantes (retorno String/struct de callback, bridge de struct no JS) → `FFI001`/`FFI002` | **Parcial** — a ABI escalar binda **DIRETA nos 3 archs nativos** (x86-64/riscv64/aarch64, #431 fatias 1–2 ✅ 20/09, §369): link-by-use da `library()` + `call sym@PLT` (sem dlopen — §61 contornado), derramamento ≥9 args, `String`↔char* (payload off 24, NULL→NULL), `void`/`Bool`/`Float`, retorno String = cópia na fronteira; goldens riscv×aarch **byte-idênticos sob qemu**; **struct param + retorno por valor x86-64 (caminho de registradores *e* sret > 16 B) ✅ 3.7 fatias 1–2b (21/09)** — `FfiStructLayout` classifica via `AbiLayout`, o call-site monta cada eightbyte no registrador de destino (param) ou salva os eightbytes de retorno e materializa o `record` (retorno); o caminho sret passa o ponteiro escondido em `%rdi` (reservando um registrador INTEGER) e copia cada campo do offset C; **struct param por valor no cross (riscv64/aarch64) com celulas INTEGER ≤ 16 B ✅ 3.7 fatia 4 (22/09)** — o caller empacota cada eightbyte do `record` Kof no seu registrador inteiro (`a0`/`a1`; `x0`/`x1` no AAPCS64 pelo tradutor), golden medido sob qemu nas duas archs (`FfiCrossStructParamE2ETest`); **array escalar `T[]`→`ptr` no x86-64 (D6-2, 3.7 passos 1–2, 22/09) ✅** — `Long[]`/`Double[]`/`Int[]`/`Float[]`/`Bool[]` bindam com copy-in por chamada (`kof_ffi_pack_array`, tamanho do elemento em `%rsi`, sem write-back, paridade byte-a-byte com o JVM; `FfiNativeArrayE2ETest`), enquanto `String[]` e arrays no cross → `FFI001`; `FfiStructE2ETest` `structParamByValueNativeRegisterPath` + `structReturnByValueNativeRegisterPath` + `structReturnSretNative` JVM==Native; struct de memória/callback/**`library()` ausente** → `FFI001` na linha da declaração (§350) | **Parcial** — o host runner liga a MESMA ABI escalar + callbacks byte-for-byte (`KofJsFfiBridge`/`upcallStub`); nao-escalar → `FFI002`; browser = degrade honesto de runtime (R7, sem host) | `FfiE2ETest` 16/16 JVM↔JS byte-for-byte; `JvmFfiCallbackE2ETest` 42/42/6.0/7.5; `KofJsFfiBridgeTest` 8/8; `FfiNativeE2ETest` 16/16 (x86-64, 20/09) + `FfiNativeCrossE2ETest` 6/6 (riscv64+aarch64 qemu, 20/09) |
| `native.risc` (riscv64) / `native.arm` (aarch64) | — | **core completo (02-03/09)**: plumbing + codegen/runtimes asm puro + qemu — `NativeRiscv64E2ETest 13/13` + `NativeAarch64E2ETest 13/13` (core: classes/arrays/List/switch/try-catch/pattern/Strings/recursão) — SUPERSEDIDO pela re-auditagem 12/09; estado por arco indexado na seção "Native por arco" abaixo — `docs/native-multiarch.md` | — | target separation 0.2.0 |
| `kof fmt` (formatter parser real, idempotente) | ✅ 31/08 | ✅ | ✅ | `KofFormatter` (2c3e794) |
| **KofJS no browser real** (`kof.ui` renderizando DOM via ES Modules) | — | — | ✅ 01/09 (`KofJsBrowserE2ETest` — Chrome headless + HTTP + captura de DOM; pula se Chrome ausente) | ESM via HTTP local (módulos não carregam via `file://`); `KofJsRunner` serve `appDir` em `127.0.0.1` |
| Android (Fase 1: `kof build --target android` → projeto Maven + APK, host Activity em Kof) | ✅ (bytecode JVM) | — | — | gaps `AND00x` em compile-time; `kof.db`/`kof.orm` são **paridade JVM desde 20/09** (D-DB-GAPS DB-2 — gates `supportedOn` incluem `ANDROID`, `Main.class` byte-idêntico ao JVM); `kof.security` é **paridade `ANDROID` desde 23/09** (`D-TECHDEBT-23/09` "portar as pilhas" — `KofSecurity.supportedOn` trata `ANDROID` como `JVM`, shims JCA, `Main.class` byte-idêntico); `kof.gpu` é **paridade `ANDROID` desde 24/09** (§278 — Android compila como o JVM, `Main.class` byte-idêntico; runtime = `JvmVkStubRuntime`, sem FFM no ART, `available()=false` + fallback CPU) |

### Native por arco (x86_64 · riscv64 · aarch64) — face (4) do multiarch, 19/09

A coluna **Native** acima dobra três arquiteturas; o estado por arco estava
enterado na prosa das células (e na linha obsoleta `native.risc`/`native.arm`,
cujo "advanced parity pending" foi superado pela re-auditagem de 12/09 em
`docs/native-multiarch.md`). Esta seção INDEXA estados JÁ MEDIDOS
— nenhuma afirmação nova; cada linha cita evidência existente. riscv64 e
aarch64 compartilham uma realidade: aarch64 herda 100% das fatias riscv pelo
`NativeAarch64Translator` (regra 5), então as colunas deles são iguais salvo
nota em contrário.

| Domínio | x86_64 | riscv64 | aarch64 | Evidência |
|---|---|---|---|---|
| Linguagem core (classes/arrays/List/strings/FP/switch/try-catch/pattern/recursion, multi-dim §113, busca UTF-16 §43/§102/§111) | ✅ | ✅ | ✅ | `NativeE2ETest`, `NativeRiscv64E2ETest`/`NativeAarch64E2ETest` sob qemu (CI `cross-native`) |
| GC mark-sweep + AUTO-COLLECT | ✅ gatilho ON 19/09 (G-6(a): 1x por programa, gate `spawn_count==0`; MT = comportamento antigo, face worker-stack catalogada) | ✅ tick-collect no alloc (G-4/B44) | ✅ herda G-4 (G-5) | `KofGcE2ETest` 4/4 incl. cap-test no repo; `NativeRiscvGcSweepTest` roda os dois arcos |
| `println(<coleção>)` §107 — escalares | ✅ | ✅ | ✅ | três goldens byte-idênticos ao oracle JVM medido (x86 `f3b3821c` + cross B39) |
| … record / coleções aninhadas | ✅ descritor recursivo `.rodata` 19/09 | ✅ 19/09 (face (4): mesma gramática de descritor na fatia `B39` — 8=toString na vtable via `jalr`, 9/10=aninhado, null→"null") | ✅ (tradutor; novo `lhu`→`ldrh`) | `NativeE2ETest.execCollectionPrintRecordNestedJvmGolden` + `nativeCollectionPrintRecordNestedMatchesJvmGolden` nos DOIS arcos cross (46/46 cada sob qemu, golden = o oracle JVM medido; a face de concat exigiu ramo `valueOf(record)` vtable no `NativeRiscvCrossOps`) |
| Map/Set + boxes de erasure (§284/§284-map) | ✅ | ✅ (`RtB49`/`RtB46`) | ✅ (tradutor) | `NativeErasureBoxE2ETest` 6/6 (md5 igual nos 3), `KofMapSetTest` |
| FP→string Double/Float (FLT001) | ✅ | ✅ (`RtB45`, libc sob demanda) | ✅ | `nativeValueOfDoubleFloatMatchesJvmGolden` nos dois arcos |
| spawn / helpers CONC001 | ✅ (pthread) | ✅ (clone 220 + futex + `RtB48`) | ✅ | `SpawnE2ETest`, `KofConcurrency2Test.crossNative*` |
| `channel<T>` send/receive | ✅ (fila de objetos) | ✅ 23/09 (FIFO futex portado — fatia `RtB61`, §423: `kof_channel_new/send/receive`, lock `amoswap.w` + futex 128/129 + sleep 1 ms no receive vazio) | ✅ 23/09 (mesma fonte via tradutor) | `KofConcurrency2Test.channelNative` (x86) + `channelWithSpawnCrossArch` (duas arches), `BareCollectionPrimitiveArgE2ETest` (nu `1` + tipado `11` rodam sob qemu nos dois cross) |
| supervisor | ✅ | ✅ (×3 — §129 port cross 19/09) | ✅ | `KofSupervisorE2ETest` |
| scheduler `every`/`at` | ✅ | ✅ | ✅ | `KofTimeE2ETest`; `at(cron)` = CRON001 em TODOS os native (não é por arco) |
| math (MATH001 fechado; `pow`) | ✅ (`pow` via libm) | ✅ FP; `pow` recusa MATH001 | ✅ FP; `pow` recusa MATH001 | `KofMathTest`, `KofMath.supportedOn` |
| db (SQLite) | ✅ SQLite + MySQL/MariaDB wire | ✅ SQLite (`RtB46`/`RtB47`); MySQL/MariaDB só x86 (`mariadb://` = alias mysql, S1 23/09) | ✅ SQLite | `KofDbE2ETest` incl. `crossNativeSqliteRoundtrip` + `nativeMariadbAliasWireProtocol`; `NativeCrossLink` |
| security | ✅ asm (faces SECN002/006) | 🔴 SECN000 | 🔴 SECN000 | `KofSecurity.supportedOn` |
| rng | ✅ (X8 fatia 2) | 🔴 RNG001 | 🔴 RNG001 | `KofRngTest` |
| http / kof.net / JSON escalar | ✅ | ✅ | ✅ | `riscvHttpGetPostStatus`, goldens JSON cross |
| `json.decode<Record>` | — (não medido aqui) | 🔴 JSN004 (asm pura) | 🔴 JSN004 | lista de recusas cross na re-auditagem 12/09 |
| `process` / `kof.ui` | PROC001 / sem port | recusas idênticas | recusas idênticas | as recusas de compile-time são do alvo inteiro, não por arco (a C-FFI quebrou essa regra 20/09: #431 binda a ABI escalar por arco no Native) |

## Gaps documentados (não mascarados)

| Gap | Diagnostic | Status |
|-----|-----------|--------|
| **App model (D-APP)** — matriz `APP001–003` | `APP001` (build backend-only com `web/` presente → frontend não compilado; honesto R6), `APP002` (✅ FEITO 23/09, #598: o `kof serve` lê `[server] port` do `kof.toml` e o repassa ao app como `KOF_SERVER_PORT` quando a env var não está setada — env do usuário prevalece; prova `ServeManifestPortE2ETest` 2/2), `APP003` (`kof new` — tipo/flag inválido, manifesto existente, dir parcial: recusa com diagnóstico, nunca sobrescreve) | ✅ `CmdNew` 14/09 (D-APP I1, DECISIONS.md §D-APP): `kof new <dir> [--type mono\|backend\|frontend\|full-stack]` — esqueletos nascem compiláveis (prova `CmdNewTest` 8/8, incl. compile JVM real dos esqueletos backend/full-stack); I3 `--fat` ✅ FEITO 14/09 (`kof build --fat` → `kof-app.jar` executável, `CmdBuildFatTest` 4/4, recusa honesta fora do JVM) |
| spawn/await no Native | ✅ 31/08 (CONC001 fechado — pthread_create + trampoline + pthread_join + allocator thread-safe futex; join implícito) | |
| `kof deploy` cross (riscv64/aarch64) | — | **empacotado desde a X9 fatia 6 (20/09)** — o ELF cross (`Default/Main`) passa pelo MESMO pipeline de release do native x86 (artefato 0755 + RELEASE.md + SHA256SUMS + `.tar.gz`); toolchain cross ausente é falha HONESTA nomeando a ferramenta (R6: ele TENTOU de verdade — a recusa preventiva `DEP001` saiu; `binary not found` só depois da tentativa). Prova: `as`/`ld` de mentirinha via `KOF_CROSS_PREFIX` em qualquer host (`CmdDeployTest#crossReleasePackagesWithStubToolchain`, `crossDeployWithoutToolchainFailsHonestly`) + binutils/qemu reais no job cross da CI. Face `--publish` ✅ 19/09 (D-POLL-19/D2-A): sobe o `.tar.gz` empacotado (por face) + o manifest do multi-deploy para uma tag **GitHub Releases** `<name>-<version>` (422 = reutilizar a tag; token `GH_TOKEN`; sem token / endpoint fora = falha honesta DEPOIS do pacote local existir — R6). **Face Pull ✅ 19/09 (1.5.3-S2):** `kof deps add owner/repo[@ver]` + `resolve` baixam o tarball da release, verificam `SHA256SUMS` antes de instalar, cacheiam em `~/.kof/deps/kof/` e alimentam build/run — REG001–004 honesto (`DepsRegistryTest` 6/6). |
| spawn/await no JS | ✅ 03/09 (CONC003 fechado — async/await/Promise real; stmt + spawn-expr + await/poll/cancel/selectAny/channel bloqueante) | `cancelled()` sempre `0` (sem thread-local pra task atual); só task-lambdas podem ficar async (`CONC003-JS-01`) |
| web no Native/JS (server, TLS, ws/sse) | ✅ server base (03/09): Native accept/route/lambda/body (`NativeWebRuntime.java`) + JS GraalJS HttpServer (`bc577aa`). JS: SSE handler-scoped ✅ 16/09. Residual (por feature): TLS `WEB002`, ws `WEB004`, sse `WEB003` (Native; push pós-return/multi-cliente no JS), resto `WEB001` (path params/keepalive) | `WEB002`/`WEB004`/`WEB003`/`WEB001` |
| kof.http no Native | ✅ HTTP/1.1 asm (`NativeHttpRuntime.java`, 03/09) | https + DNS real ficam como gaps; **`timeout`/`retry`/`circuit` são REAIS nos 4 alvos nativos desde 17/09** (§259 FECHADO: connect não-bloqueante + deadline `poll` + `SO_RCVTIMEO`/`SO_SNDTIMEO` → `throw "kof.http: timeout"`; retry em exceção/`>=500`; circuito fail-fast 30s half-open — riscv64 portado, aarch64 herda; `KofHttpNativeResilienceCrossTest` 4/4 sob qemu, mensagens idênticas ao x86/JVM). `HTTP002` é o único código de gap HTTP *definido* — **não é emitido hoje**: `KofHttp.supportedOn` sempre devolve `true`, então o ramo `HTTP002` em `ExpressionHttpCallLowerer` está morto e `KofHttp.gapCode()` não tem chamadores (§259). Reservado para quando um alvo HTTP nativo ficar de fato sem suporte |
| kof.db/orm no JS | **`kof.db` nao-tipado FECHADO 16/09** (ponte no host GraalJS) — `db.query<T>` tipado FECHADO 18/09 (`DB002` — bind no guest via `__kof_decode_<T>`); **`kof.orm` FECHADO 18/09** (`ORM001` — `KofJsOrmRuntime`/`KofJsOrmBridge`, mesmo SQL de `JvmOrmRuntime`, E2E byte-paridade) | feito |
| `kof.media` (Image/Audio/Video/Mic + `app.serveDir`) no Native/JS | `MEDIA001` (handles), `MEDIA003` (mic), `WEB005` (`serveDir`) | só JVM: handles de mídia emitem o gap em `ExpressionUiMediaCallLowerer` (`MEDIA001`/`MEDIA003`); `app.serveDir` emite `WEB005` no gate web (`KofWeb.gapCode` + `ExpressionBuiltinInstanceCalls.lowerWeb`) — compile-time, R6 honesto, nunca stub silencioso. Medido 17/09: antes do fix o catch-all do gate web emitia `WEB001` enquanto as docs prometiam `WEB005` (código fantasma; o único mapeamento vivia no `KofMedia.appServeDir` morto). `KofMediaE2ETest` cobre `MEDIA003` (hardware ausente) + `WEB005` no Native/JS |
| `kof.gpu` no JS | `GPU001` | JS não tem suporte: `KofGpu.supportedOn` é falso para JS e `ExpressionMethodCallLowerer` emite `GPU001` em compile-time (medido 18/09: `gpu.available()` com `--target js` → `gpu.available: not available on the JS driver.target yet (GPU001)`); os stubs asm do Native são honestos (`available=false` + fallback) |
| `kof.observability.exportSpans()` (OTel OTLP/JSON) no Native | `OBS003` | JVM-first (R7): `KofObservability.supportedOn` é falso para os targets Native e `ExpressionMethodCallLowerer` emite `OBS003` em compile-time (medido: `--target native` → `observability.exportSpans: not available on the NATIVE driver.target yet (OBS003)`); o payload JVM/JS é OTLP/JSON `resourceSpans` (golden medido no oráculo JVM + GraalJS) |
| `kof.security` em riscv64/aarch64 | `SECN000` | O runtime riscv64/aarch64 em asm puro **não tem** nenhuma primitiva `kof_sec_*`: `KofSecurity.supportedOn` devolve falso para toda função nesses targets (medido 17/09: `crypto.sha256("x")` com `--target native.risc` → `crypto.sha256: not available on the NATIVE_RISCV64 driver.target yet (SECN000)`). Sem o gate a chamada quebrava no link com undefined reference (R6) |
| `kof.security` chacha20 no Native x86_64 | `SECN002` | `crypto.encryptChacha20`/`decryptChacha20` são JVM/JS apenas (`KofSecurity.supportedOn`); medido 17/09: `--target native` → `crypto.encryptChacha20: not available on the NATIVE driver.target yet (SECN002)`. A face AES-GCM do `SECN002` está fechada no Native (01/09) — esta é a face chacha20 do mesmo código |
| `kof.security` cookies no Native | `SECN006` | `security.cookieSet`/`cookieSetOpts`/`cookieGet` são JVM/JS apenas (D-SEC C11); medido 17/09: `--target native` → `security.cookieSet: not available on the NATIVE driver.target yet (SECN006)` |
| Tipo valor `Secret` (`secrets.of`/`secrets.secret`) | `SECN008` | D-SECRETS face 1 (Estágio 5/tracker 3.6): JVM-primeiro (R7). JS/Native/Script/Android recusam em compile-time — medido 21/09 (`SecretE2ETest`): `secrets.of("x")` em JS/NATIVE/NATIVE_RISCV64/NATIVE_AARCH64/ANDROID → `SECN008`. O tipo só é alcançável pelas construtoras gated, então as faces de instância (`reveal`/`redacted`) são inalcançáveis onde não há suporte; nunca stub silencioso (R6) |
| `KeyHandle` (`secrets.keyFromHex/keyFromPem/keyFromKeystore`, `rotate`) | `SECN008` | D-SECRETS P3 (Estágio 5/tracker 3.6): JVM-primeiro (R7). Mesmo argumento de construtora gated do `Secret` — medido 21/09 (`KeyHandleE2ETest`): `secrets.keyFromHex("...")` em JS/NATIVE/NATIVE_RISCV64/NATIVE_AARCH64/ANDROID → `SECN008`. As sobrecargas `KeyHandle` de `crypto`/`jwt` são JVM-only; os bytes crus da chave nunca são expostos, e `rotate()` revoga o handle antigo (`SECN010`) |
| `kof.process` no Native | `PROC001` (só spawn) | `process.run` é REAL em todo alvo nativo — x86-64 (25/09) e riscv64/aarch64 (cross 26/09, `NativeRiscvAsmProcess`, paridade byte JVM==riscv64==aarch64). `process.spawn` segue recusado em compile-time (`ExpressionProcessCallLowerer`) = o `PROC001` restante (fatia B). JVM (`ProcessBuilder`) e JS (host GraalJS) rodam ambos |
| `kof.shell` `pipeline` em JS/Native | `PROC001` | `pipeline` precisa de piping stdout→stdin vivo: JVM real (`kof_shell_pipeline`, cadeia ProcessBuilder + threads de pump); JS/Native recusam em tempo de compilação com `PROC001` — nunca um `ReferenceError` cru (disciplina §235; `ShellE2ETest.pipelineOnJsIsHonestProc001` fixa isso). `cmd`/`run`/`runWith`/`ok` são reais em JVM+JS. JS FECHADO 20/09: a cadeia landou como binding próprio (`KofJsProcessBridge.processPipeline` — threads de pump; a unidade `spawn` nao foi esperada: o pipeline encadeia as proprias etapas por dentro, exatamente como o binding JVM — `write` publico segue sob `/dev/null`, regra 6), pinada pelos goldens de paridade do `ShellE2ETest` (16/16). Native FECHADO 26/09 (linha 2 fatia B2): o cross (`NativeRiscvAsmPipeline`) encadeia os estagios com pipes reais do SO (sem threads — `clone`+`dup3`+`execvp`), byte-paridade com o JVM; so x86-64 `pipeline`/`runWith` seguem `PROC001` (fatia B). |
| `kof.ssh` no Native | `PROC001` (só MCU/riscv32) | `ssh.cmd`/`run`/`ok` são REAIS no x86-64 (`RuntimeSsh`) E no riscv64/aarch64 (`NativeRiscvAsmSsh`), ambos reusando `kof_process_run` com o argv-oraculo JVM exato (paridade byte medida 26/09). Só o MCU/riscv32 freestanding seguem `PROC001` em compile-time. JVM/JS abaixam para a mesma camada de processo. |
| `extern` com ABI nao-casavel em qualquer target | `FFI001` / `FFI002` | **20/09 (#431, §369):** o Native agora CASA o conjunto escalar nos 3 archs SEM FFM — link-by-use da `library()` + `call sym@PLT` (a saida do §61 virou a rota de producao). Nao-casavel em qualquer arch nativa (struct **retorno**/struct de memoria/`T[]` no cross ou `Int[]`/`Float[]`/`Bool[]`/ponteiro, callbacks, `library()` ausente) → `FFI001` **na linha da declaracao** (§350), nunca stub silencioso, nunca link-break (R6); **struct param + retorno por valor x86-64 (caminho de registradores *e* sret > 16 B) agora CASAM (3.7 fatias 1–2b, 21/09)**; **no cross (riscv64/aarch64) o retorno de struct (fatia 3, 22/09) e o param struct por valor com celulas INTEGER ≤ 16 B (fatia 4, 22/09) tambem CASAM**; **no x86-64 o array escalar `T[]`→`ptr` (`Long[]`/`Double[]`) agora CASA com copy-in (3.7 passo 1, 22/09)** — o resto da superficie de struct (struct param de memória > 16 B/BYREF, float/HFA cross, `Buffer`) segue `FFI001`. O JVM casa o conjunto escalar **mais a fatia struct/array/out-buffer da 3.8b** (`record` por valor arg/retorno + `T[]` escalar→`ptr` + `Buffer(U8)` INOUT, `FfiStructE2ETest` 10/10, `FfiArrayE2ETest` 5/5, `BufferFfiE2ETest` 4/4); o que resta nao-casavel no JVM (retorno String/struct de callback) e todo nao-escalar no host JS segue honesto (medido 21/09). |
| JSON Float/Double | ✅ 31/08 (JSN001 fechado — XMM + parser fração/expoente) | |
| FP→string no riscv64/aarch64 | ✅ 15/09 (FLT001 fechado — fatia `RtB45`) | era `FLT001`: o runtime asm puro não tinha libc (`%g`/`snprintf` ausentes). Agora o port cross do `RuntimeDtoa` (`kof_dtoa_format`/`kof_double_to_string`/`kof_float_to_string`) consome `snprintf`/`strtod` pelo **link dinâmico sob demanda** (`NativeCrossLink`: um programa que imprime FP liga `-lc`, o resto segue estático). Byte-idêntico ao JDK `Double.toString`/`Float.toString` no riscv64 **e** aarch64 (aarch herda via tradutor, que agora mapeia os apelidos ABI `fa0..fa7` → `d0..d7`); `println(3.14)`/`"v=" + 3.14`/`listOf(1.5)` deixam de diagnosticar. Prova `NativeRiscvDtoaTest` (tabela oracle JVM, 2 arches) + `Native{Riscv64,Aarch64}E2ETest#nativeValueOfDoubleFloatMatchesJvmGolden` |
| JSON objetos/records no Native | ✅ 31/08 (JSN002 fechado — composição em compile-time) | |
| GC mark-sweep no Native | ✅ | `kof_gc_sweep` real (flag bit1) + **auto-collect desligado** por exigir safe-points (`status.md` #1) — `KofGcE2ETest` 3/3 |
| MySQL nativo completo | ✅ | **wire protocol** + **prepared statements binário (03/09)** — `NativeDbPrepared.java`, `KofDbE2ETest` 12/12 |
| Native riscv64/aarch64 codegen | `NATIVE002` | **riscv64 core completo (02/09) + aarch64 core completo (03/09)**: `NativeRiscv64E2ETest 20/20` + `NativeAarch64E2ETest 20/20` — runtimes + codegen em **asm puro** (raw syscalls, **sem C**; `as`+`ld` estático com `--no-relax` (gp=0 sem C runtime); aarch64 via `translateRiscvToAarch64`) + qemu: println, var, if/else, aritmética, **classes (virtual dispatch/fields), arrays, List, switch, try/catch/throw, pattern matching (switch String s/instanceof/as), String methods (charAt/substring/contains/startsWith/endsWith/indexOf/concat/toInt + trim/toUpperCase/toLowerCase/replace char+String/lastIndexOf/equalsIgnoreCase/split), recursão, JSON (encode/decode record+listas), HTTP (get/post/put/patch/delete/options/status + headers — asm-generic socket=198/connect=203/write=64/read=63/close=57, mesma tabela nos 2), spawn/await (`clone(220)`+`futex` — qemu-riscv64 8.2.2 não tem clone3; heap compartilhado → `kof_alloc` bump atômico `amoadd.d`/`ldadd`)**. Paridade total x86 (DB/UI/net) pendente nos dois. **Ver `docs/native-multiarch.md`** |
| Campo estático no Native | ✅ CORRIGIDO 07/09 (bug 41, lane Native) | Histórico: `NativeBackend:629-630` era stub vazio p/ `KofGetStatic`/`KofPutStatic` (imprimia memória não-inicializada). Fix: `ClassLayout` exclui estáticos do layout de instância + `collectStaticFields`/`emitStaticData` emitem com offset real. Prova: `NativeE2ETest.nativeStaticFields` (verde no gate 12/09, 64/64) |
| `record.hashCode()` no JS/Native | ✅ CORRIGIDO (bug 42; JS `1ecfb3d` + Native `CompilerRecordSupport.buildRecordHashCodeMethod`) | Sintoma histórico: JS `TypeError: hashCode is not a function`; Native `ld: undefined reference to P_hashCode`. `hashCode` sintético de record (acumula `31*h + campo`) emitido nos 3; paridade `==`/`toString`/`hashCode` nos 4 targets. Prova viva: `ConformanceMatrixTest.recordhash` (célula SEM exclusão — roda JVM+Native+Script+JS; 11/11 verde 12/09). |
| `String.length`/`charAt` no Native (UTF-8 vs UTF-16) | ✅ 5 targets (bug 43; cf. STR001) | O Native contava bytes UTF-8 (`"café".length`=5, `charAt(3)`=195); JVM conta code units UTF-16 (4, 233). x86_64 corrigido 10/09 (`RuntimeStringOps`/`RuntimeStringSearch` + `.Lkof_substr_walk`); JS 10/09; riscv64/aarch64 11/09 (na MERGE: fatias `RtB34`/`RtB35` — `kof_string_length`/`char_at`/`substring` + `index_of`/`last_index_of` UTF-16, inclusive as variantes `-from` (`*_2`, §102 cross) e `starts_with2`). Prova: golden JVM==x86==riscv==aarch (`NativeRiscv64/Aarch64E2ETest`+matriz, `assumeToolchain`). **Contrato `substring(0,0)` FECHADO 11/09 (§111):** sentinela "até o fim" mudou de `0` p/ `-1` no call site x86 E cross — `"ab".substring(0,0)` = `""` (JDK) nos nativos. |
| `String.compareTo`/`hashCode`/`equals` no Native | ✅ 5 targets (bug 97, 10–11/09) | Declarados no reference + aceitos pelo typer, mas os nativos não os emitiam (`undefined reference java_lang_String_compareTo`/`_hashCode`). x86_64 10/09 (`RuntimeStringCompare`, code units UTF-16; `.equals` roteado p/ `kof_string_equals` c/ guard isString); JS 10/09 (`JsCallEmitter` + `kofStringCompareTo`/`kofHashCode`); riscv64/aarch64 11/09 na MERGE (fatia `RtB36`: `String_equals`/`String_compareTo`/`String_hashCode` — algoritmo de code-unit port do x86, via `.Lu5_next`; aarch herda do tradutor). **Resíduo: `Object.equals` (receiver `Object`) ainda link-fail — dispatch virtual no Native, gap aberto p/ lane Native.** |
| `println(double)` no Native | ✅ 4 targets (bug 44 10/09 + spelling inf/nan 11/09 + §180 shortest-round-trip 15/09 + **cross 15/09** — fatia `RtB45` via link dinâmico `snprintf`/`strtod`) | JDK `Double.toString`/`Float.toString` via `RuntimeDtoa` (`kof_dtoa_format`/`kof_double_to_string`/`kof_float_to_string`): loop limitado `%.*e`+`strtod` de shortest round-trip + reformatação Java (`1.0E7`, `0.001`), Float com a própria forma, `Infinity`/`-Infinity`/`NaN`. `RuntimePrintNum` (o `print` sem box), `RuntimeJsonEncode` (NaN/±Inf → `null`) e `RuntimeCollectionToString` delegam a ela; `RuntimeStringConv` mantém só int/char/long/bool. Prova `ConformanceMatrixTest.doubleprint` (Native incluído)+`infinityprint`+`floatprint` + `NativeRiscvDtoaTest` (oracle cross) |
| `println(if/switch-expr)` heterogêneo no Native | 🟡 PARCIAL 15/09 (fatia 1, `97d08e54`) | §205: o `Object`-join do if/switch heterogêneo não tinha dispatch de print no Native → SIGSEGV (exit 139). A fatia 1 rebaixa o print DIRETO ramo-a-ramo (cada ramo pelo seu tipo — a mesma regra que o JVM já aplica no box de ramo): células `ifexpr-heterogeneous-direct`/`-else`/`-multi`/`-multi-default` byte-idênticas JVM×Native. Residual (via-`var`/`as Object`) = o ABI boxed polimórfico (§104b-ii), fatia 2. O gate de diagnóstico R6 foi REJEITADO pela mantenedora — regra 5 (mesmo output em todo target) é lei. |
| `finally` c/ `return` no try | ✅ 4 targets (bug 45, DD-01 opção 4a, 13/09) | `FinallyFrame` na IR: `ReturnStmt` com frame ativo → store `#retVal` + jump `returnFinallyLabel`; epílogo roda o finally e retorna (encadeia try aninhado); JS reconstrói try/finally nativo + epílogo return-only. Antes: JVM/Native/interp descartavam o finally, JS devolvia `undefined`. Prova `CoreRegressionE2ETest.finallyReturnJvm`+`finallyReturnJs`; suíte 1627/0 |
| `json.decode<List<Record>>` | `JSN004 (Native)` — gap honesto, bug 48 (09/09) | Interpretador ✅ CORRIGIDO 07/09 (`KofInterpreterRuntime` intercepta `kof_json_decode_object_list`); JVM/JS ok. **Native:** não é mais link-fail silencioso — `ExpressionJsonCallLowerer` (ramo `isNative()`, :131-145) emite o **diagnóstico `JSN004`** ("List<Record>/List<Class> not supported on the Native target yet; use JVM/JS") em vez de chamar `kof_json_decode_object_list` inexistente (R6: gap diagnosticado, nunca stub mudo/link fail). `decode<Record>` no topo ok nos 4 (fix 07/09) |
| `try` aninhado no KofJS | ✅ CORRIGIDO 07/09 (bug 49) | JS `COMP002: try expected KofTryEnd` — `JsControlFlowParser` só processa finally com catch-all `Throwable` e consome o label done só se não for endLabel de try aninhado (`MethodCtx.isTryEndLabel`); JVM/Native/Script/JS rodam |
| `throw` dentro de `catch` (re-throw) no KofJS | ✅ CORRIGIDO 08/09 (bug 52) | JS `unexpected KofCatchStart` (corpo de catch termina em KofThrow) — resolvido como efeito colateral do fix do bug 45 (`c727fee`); 4 targets agora concordam (`ConformanceMatrixTest.catchrethrow` sem exclusão) |
| channel send/recv DENTRO de `spawn` | ✅ CORRIGIDO 09/09 (bug 50) | Histórico: Native SIGSEGV 139 — o `call usleep` no receive-vazio clobberava `%rsi` (=&lock), deixando o `futex` com uaddr errado (nunca setado p/ &lock). Fix: alinha rdi/rsi ao padrão do WAKE + restaura &lock em rsi p/ o próximo cmpxchg, nos 2 pontos. Prova: `KofConcurrency2Test.channelWithSpawnNative` (`v=42`, verde no gate 12/09; antes a suíte só tinha `channelNative` SEM spawn) |
| `app.delete(...)` → COMP002 frame crash (GitHub #29) | ✅ CORRIGIDO 07/09 (bug 54) | `KofIo.instanceMethod` `case "delete"` sem `argCount==0` colide com rota web → `KofPop` extra sobre `kof_web_route` (void) → underflow ASM `Frame.merge`. Fix: guarda `argCount == 0`. `get/post/put/patch/delete/options` compilam e rodam (`KofWebE2ETest.deleteRouteCompilesAndResponds`) |
| Handler web que termina em `return null` responde 404 (GitHub #28) | ✅ CORRIGIDO 07/09 (bug 53) | `ExpressionTyper` caso `LambdaExpr` só varria `ReturnStmt` top-level → `return` aninhado em `if` ignorado → `invoke()` void descartava valor. Fix: varredura recursiva (`firstReturnValueType`). `return null` continua 404 documentado (`KofWebE2ETest.handlerReturningNullAsLastPathStillRespondsValue`) |
| `spawn` concorrente → CME no interpretador (statics) | ✅ CORRIGIDO 07/09 (bug 55) | `staticFields`/`initialized` HashMap compartilhados entre virtual threads → CME no `computeIfAbsent` + corrida de `<clinit>` (perdedor via statics vazios). Fix: ConcurrentHashMap + lock por classe (semântica JVM: clinit uma vez, outros esperam). Probe 120/120 + `concurrentAwaitReturnsOwnTaskResult` |
| `String.split` + `parts.get`/`.size` → ClassFormatError `Illegal class name ""` (GitHub #30) | ✅ CORRIGIDO 07/09 (bug 56) | receiver `ArrayType` no fallback genérico → `KofCall`/`KofLoadField` com owner ArrayType → internalName vazio no constant pool. Fix: `.get(i)` → AALOAD, `.size/.length` → ARRAYLENGTH + typer (componente/Int). Frontend compartilhado, emit JVM. `CoreRegressionE2ETest.stringSplitArrayAccess` (JVM+JS) |
| `await` sobre handle `Object`/`Handle<T>` declarado → VerifyError/ClassNotFoundException (GitHub #31) | ✅ CORRIGIDO 07/09 (bug 57) | `Handle` não era mapeado (só `Channel`) → descriptor `LHandle;` inexistente; e `emitWideningIfNeeded` não unboxava valor apagado (Object/Unknown) p/ slot primitivo. Fix: `Handle<T>`→`CompletableFuture` (Type.of + JvmTypeMapper) + checkcast + unbox no widening. `CoreRegressionE2ETest.awaitOn{HandleThroughObject,TypedHandle}Param` (JVM) |
| `json.decode` de record com campo `List<Record>` → mapas crus / ClassCastException (GitHub #34) | ✅ CORRIGIDO 07/09 (bug 58) | campo emitido sem atributo Signature (genérico apagado) → `kof_json_bind` só via `Class<?>` (erasure) e devolvia a lista crua. Fix: `toGenericSignature` nos campos/record components + bind recursivo por `getGenericType`. `List<List<T>>` → JSN004 (gap honesto). `CoreRegressionE2ETest.jsonDecodeRecordWithListOfRecords` (JVM) |
| `serveDir` `GET /ui/` (barra final) → 404 (GitHub #35.2) | ✅ CORRIGIDO 07/09 (lane JVM web) | `kof_web_static_match` só resolvia index.html para `path.equals(prefix)` (sem barra); `/ui/` caía em rel="" → não-arquivo → 404. Fix: barra final (e raiz `/`) → `index.html`. `KofMediaE2ETest.serveDirTrailingSlashServesIndex` |
| `http.<verb>` com 2+ headers como args separados → crash/SEM025 (GitHub #32) | ✅ CORRIGIDO 07/09 (bug 60) | API só casava aridade 1-3 (headers = 1 String `\n`); 4+ args → null → SEM025 (crash já contido por `65e2dc0`). Fix: headers variádicos — `staticCall` aceita `>=2`/`>=3`, lowering mescla extras com `concat(acc,"\n",h)`. `KofHttpE2ETest.multipleHeadersAsVariadicArgs` (JVM; JS/Native usam o mesmo lowering) |
| REGRESSÃO Native riscv64/aarch64: `println` → `undefined reference kof_static_java_lang_System_out` (59 testes) | ✅ CORRIGIDO 09/09 (bug 59, lane Native) | Histórico: desde `62423bf` (fix bug 41), o `.s` riscv/aarch referenciava o símbolo mas não o definia no `.data` (só o x86 definia) → 59 link-fail. Fix: `emitRiscv`/`emitAarch64` passaram a chamar `collectStaticFields()`/`emitStaticData`. Prova: `NativeRiscv64E2ETest` + `NativeAarch64E2ETest` **39/39 + 39/39 verdes SEM skip** no gate 12/09 neste host (compila → binário existe → roda no qemu → rc=0) |
| `spawn`/`await` (determinístico, ordem por await) | OK nos 4 | `spawn fn(a)` + `await` + `spawn fn` ×2 + canal mesma-thread: saída idêntica nos 4 targets (travado em `ConformanceMatrixTest` lote 3) |

Fechados em 0.2.6-beta (01/09): **`kof.log` no JS (LOG001)** — console.* + `KOF_LOG_LEVEL`, bloco Vulkan no runtime JVM condicional ao uso de `kof.vk` (capability/link-por-uso, R2) + `--enable-preview` só para programas Vulkan (FFM preview API no JDK 21), `spawn` com lambda que captura variável externa (JVM+Native), `&&`/`||` booleanos com short-circuit no JS (bitwise intacto), `Channel<T>` como parâmetro de função (JVM/Native/JS), `println`/`print` antes de `spawn` e `spawn→await→spawn` no Native sem SIGSEGV (alinhamento de stack no `pthread_create`), KofJS `kof.ui` renderizando em browser real (Chrome headless E2E), LSP `references`+`rename`, tracing W3C `traceId`/`spanId` (3 targets), `moduleRoot` por LCA (P1-4), validação tipada de coluna no ORM (P3-10, `ORM003`), **AES-256-GCM no KofJS (`SECN002`)** — AES-GCM puro JS com paridade cross-target JVM↔JS e tamper detection por base64 estrito, **histogram/metrics no Native (`OBS002`)** — store asm + export Prometheus em paridade de conteúdo com o JVM, **`time.interval`/`time.cancel` no Native (`TIME001`)** — reusa o scheduler (SCHED001) via alias `jmp`, mutação por referência validada; **TIME001 fechado no JS (02/09)** — `kofTimeInterval`/`kofTimeCancel` agora comandam uma fila cooperativa `kofTimeJobs` bombeada dentro de `kofTimeSleep` (GraalJS não tem event loop; browser/Node caem no `setInterval` nativo) — `KofTimeE2ETest.jsIntervalRunsPeriodicallyUntilCancelled` (paridade ticks≥2 + freeze após cancel), **`transaction {}` no Native** — `kof_db_transaction` em asm: BEGIN/COMMIT/ROLLBACK via `kf_db_execute`, lambda via vtable (`rdi`=this p/ capturas) e ROLLBACK+re-throw no EH (`kf_throw_string` chega no handler com `%rdi` e a chain p/ o try externo), conexão default = última aberta — `nativeTransactionCommits`/`nativeTransactionRollsBackOnFailure`, **`kof.mq` no Native (`MQ001`)** — pub/sub + filas in-process em asm (store `.bss` + nodes 40B, invoke-com-arg, paridade de output com JVM/JS), **`Set<T>`/`Map<K,V>` como campo/retorno de classe no JVM** — mapper `Set`→`HashSet`/`Map`→`HashMap` + parse de método de classe c/ retorno genérico (`KofMapSetTest.setMapAsFieldAndReturn`, 3 targets), **source map V3 do KofJS** — mappings VLQ em nível de linha (função gerada → linha Kof; `KofJsSourceMapTest`).
Fechados em 0.2.6-beta (30-31/08): `spawn` Native (CONC001 — pthread), FP XMM (FLT001), JSON completo no Native (JSN001/JSN002/JSN003 — objetos/records + arrays incl. Double/Float), WebSocket/SSE JVM (RFC 6455), `kof.http` retry/circuit JVM+JS (30s window, fail-fast), `kof.cache` 3 targets (fix de clobber de registradores), SQLite nativo `.so` direto, UI Fase 7 Router (JS), `kof fmt` + `kof config gen`.
Fechados em 0.2.6-beta (27/08): pattern matching `switch case String s` + record `Point(x,y)` (JVM/Native/JS), `String?` null safety básica, `kof.http` no JS via `Java HttpClient`, `List map/filter/reduce`, large-project `import a.b.C` (`CompilerDriver.java:243`), `List.get`/`listOf`, free-list GC (`kof_free_head`), Windows SIGPIPE.
Fechados em 0.1.0: Map/Set nativo (era COL001), await com unboxing, captura em lambdas (BoxN), resultado de tarefa (`await`).

## Princípio

A semântica Kof é a mesma em todos os targets. Onde um target não suporta
uma feature ainda, o compilador emite um diagnostic explícito — nunca código
que funciona de forma diferente ou quebra silenciosamente.

**Única exceção nomeada (D-UI-SCOPE, 18/09).** A paridade absoluta vincula toda
superfície exceto as **atualizações de regra do `kof.ui`** — as regras de
renderização/estado da linguagem de UI (sinais, `when`/`each`, diff,
semântica de cancelamento). Estas são de autoria e efeito no **KofJS hoje** e
no **Kof WebASM quando `Target.WASM` existir** — **nunca** no JVM/Native.
Esta é a única exceção nomeada à regra 5; todo o resto, incl. o restante de
`kof.ui` e todo outro domínio, segue sob a lei de paridade. Ver
`docs/development/DECISIONS.pt_BR.md` §D-UI-SCOPE.

---

## Convenção de gaps por domínio (visão universal)

Todo gap de capacidade tem um **código de diagnóstico** documentado aqui e
emitido em compile-time. Domínios novos seguem o mesmo padrão dos existentes
(`SECN000`, `WEB002`, `WEB005`, `CRON001`, ...):

| Prefixo | Domínio | Exemplos |
|---------|---------|----------|
| `HTTP`/`WEB`/`DB`/`ORM`/`MQ`/`SCHED`/`TIME`/`CONC`/`SECN`/`CRON`/`MEDIA`/`GPU`/`OBS`/`PROC`/`FFI` | Sistemas atuais | `WEB001`/`WEB002`/`WEB003`/`WEB004`/`WEB005`/`WEB006` (gate web em não-JVM), `SECN000` (todo `kof.sec` em riscv64/aarch64), `SECN002` (chacha20 no Native), `SECN006` (cookies no Native), `SECN007` (OAuth resource-server em não-JVM), `CRON001` (`at(cron)` no Native; JVM/JS real), `MEDIA001`/`MEDIA003` (handles de mídia/mic em não-JVM), `GPU001` (`kof.gpu` no JS), `OBS003` (`exportSpans` no Native), `PROC001` (`kof.process` no Native), `ORM001` (`kof.orm` no Native), `RNG001` (PRNG `rng` em cross riscv64/aarch64 + ANDROID — JVM/JS/NATIVE-x86_64 real, fatias 1–2 ✅) |
| `AND` | Android | `AND002` (servidor web embutido no Android), `AND004` (android.jar ausente — warning); `AND001` fechado 31/08; `AND003` é caveat documentado (sem gate de compile-time) |
| `NATIVE` | codegen multiarch | `NATIVE002` (rótulo do trabalho riscv64/aarch64; não é diagnóstico emitido) |
| `INFRA` | infraestrutura / IaC | `INFRA00x` |
| `DATA` | data engineering / dataframe / ML | `DATA00x` |
| `SCI` | scientific computing / HPC | `SCI00x` |
| `BIO` | bioinformática | `BIO00x` |
| `SECPQ` | criptografia pós-quântica | `SECPQ` (gap de target, nunca stub) |

**Códigos reservados (definidos, não emitidos).** Um par
`gapCode`/`supportedOn` pode permanecer no fonte depois que a capacidade
landou, como andaime para um alvo futuro sem suporte. Esses literais existem,
mas **nenhum alvo os alcança hoje**, então não são gaps: `HTTP002`
(`KofHttp.supportedOn` sempre devolve `true`), `MQ001` (`KofMq.supportedOn`
sempre `true`), `SCHED001` (`KofScheduler` só gateia `at(cron)`, como
`CRON001`; `SCRIPT` aborta antes com `COMP003`), `CONC003` (o residual JS vivo
é `CONC003-JS-01`), `UUID001` (gate removido 10/09), `OBS001`, `SECN005`
(`rate_limit`/`session`/`api_key` têm suporte em todo alvo). Não os cite como
emitidos — o diagnóstico só aparece quando um alvo fica de fato sem suporte
(R6: nunca silencioso, e nunca fantasma também).

**Android é um alvo gateado de verdade.** `--target android` reusa o
`JvmBackend`, e a §278 resolveu o over-gating: `kof.db`/`kof.orm` (20/09),
`kof.security` (23/09) e `kof.gpu` (24/09) agora compilam como o JVM
(`Main.class` byte-idêntico; o runtime gpu do Android é o `JvmVkStubRuntime`
sem FFM). As recusas genuinamente intencionais seguem `AND002` (servidor
embutido) e `AND004` (android.jar ausente do `ExternalClasspath`); ficam
listadas para a matriz não mentir sobre o que o Android aceita.

Regra (R6): gap de domínio sempre tem **código + entrada nesta matriz** —
nunca stub silencioso, nunca fallback fraco, nunca "paridade parcial" sem
diagnóstico.

## Tiers de estabilidade (R5)

Cada namespace/pacote carrega um tier:

- **`stable`** — garantia de compatibilidade; promove somente com DoD completo
  (3 targets ou gap diagnosticado, E2E por target, benchmark quando plausível,
  docs + `training/` sincronizadas, suíte verde).
- **`experimental`** — pode mudar; a camada de **pacotes oficiais** nasce
  `experimental`.
- **Default (D-POLL-19/09, D4-A):** todo namespace — núcleo ou novo — nasce
  `experimental`; a promoção a `stable` é por-namespace, só com o DoD completo do R5
  (nunca em lote, nunca por idade).

**Registro (D4-A; medido 19/09; camadas = `scripts/stdlib_boundary.txt`; tiers
com gate de máquina por `scripts/check_stdlib_boundary.sh` desde 21/09 — tier
ausente/inválido, ou `stable` sem pin no `STABLE_ALLOWLIST` do gate, quebra o build).
Todo namespace é `experimental` — **zero promoções**: o DoD do R5 fecha por
namespace, nunca em lote, nunca por idade, e uma promoção ganha uma linha AQUI
(com o teste + matriz de targets que a prova) antes da palavra do tier mudar em
qualquer outro lugar:

| camada R1 | namespaces |
|---|---|
| base-stdlib | `math` `strings` `encoding` `net` `uuid` `random` `rng` `io` `concurrent` |
| platform | `time` `http` `db` `cache` `buffer` `process` `shell` `ssh` `json` `log` `orm` `config` `gpu` `mq` `validation` `observability` `tetris` `auth` `crypto` `jwt` `passwords` `secrets` `security` `Audio` `Image` `Mic` `Video` `supervisor` `workflow` `version` `ui` |

(Escopo: os 34 namespaces de membros callable de `StdCatalog.namespaces()` — fonte
única, lock `StdCatalogTest` — mais as 6 superfícies registradas no ledger R1
(`io`, `concurrent`, `supervisor`, `workflow`, `version`, `ui`) que não são
catalogadas por membro. Total: 40, todos `experimental`.)

Domínios pesados (`ml`, `bio`, `hpc`, `infra-<cloud>`) são **pacotes
oficiais** (camada 4), **nunca** stdlib base (R1).