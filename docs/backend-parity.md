[English](backend-parity.md) | [Português](backend-parity.pt_BR.md)

# Backend Parity — Kof JVM × Native × KofJS

**Last updated:** September 20, 2026
**Version:** 0.5.0-beta

> Deltas since 0.1.0: `native.risc` (riscv64) and `native.arm` (aarch64) targets separated from `native` x86_64 (**full core 02-03/09** — `NativeRiscv64E2ETest 13/13` + `NativeAarch64E2ETest 13/13` in pure asm + qemu, current state in the "Native per-arch" section below + `docs/native-multiarch.md`); Native free-list (`kof_free_head`) + `kof_gc_collect` (mark-sweep implemented 03/09; **auto-collect ON 19/09 — §260/G-6(a) closed**: trigger fires `kof_gc_collect_now` once per program, gate `spawn_count==0`, §260 history honest); MySQL wire protocol real on x86-64 (`kof_db_mysql_scramble` + `user:pass@`; full handshake/COM_QUERY/prepared + all 13 ORM faces measured against MariaDB 12.3.2, F2d1–F2d7 22/09); `mariadb://` is a `mysql://` wire alias on Native x86-64 (db-parity S1, 23/09 — `kof_db_type` reports the mysql family; cross still honest `DB001`, R7); pattern matching `switch case String s` + record destructuring `Point(x,y)` on JVM/Native/JS; basic `String?` null safety; `KofScript` top-level `let` → `KofScriptGlobals`; `KofCcompiler` (`kof c`) native-only C subset; `List map/filter/reduce` + `Box<T>`; Windows SIGPIPE fix.
> Deltas 30-31/08: real `spawn`/`await` on Native (pthread + trampoline + join + thread-safe allocator futex — CONC001); real FP in XMM (FLT001); JSON objects/records + FP arrays on Native (JSN001/JSN002/JSN003); WebSocket/SSE on JVM (`app.ws`/`sse.*`, RFC 6455); `kof.http` retry/circuit JVM+JS (30s window, fail-fast); `kof.cache` 3 targets (register clobber fix); UI Phase 7 Router (real JS, JVM no-op); native SQLite via direct `.so`; `kof fmt` + `kof config gen`.
> Deltas 18/09: **FFI JVM generalized (R3, `.18`)** — `extern` now binds any signature over the scalar set {Int, Long, Float, Double, Boolean, String} at arbitrary arity (parameters and return), a `String` return reads back the native `char*`; a single `kof_ffi(lib, name, sig, Object[])` FFM downcalls `kof_ffi`/`kof_ffi_void` replace `kof_ffi_i`/`_si`/`_dd` (`FfiE2ETest` `pow` two-double args → `1024.0`, `strstr` two strings → `world`, `srand(Int)` default `void`; `FfiE2ETest` `atol`→`labs` proves `Long` end-to-end, `FfiSignatureTest` locks the full scalar→layout mapping incl. `Float`/`Boolean`). Honest gaps preserved: struct/pointer (D6), variadics and opaque handles/out-buffers still `FFI001`. **JS parity CLOSED 18/09 (3.6, `.18`)**: the scalar ABI binds on the JS target via a host FFM bridge `KofJsFfiBridge` (`extern`→`kofFfi`→`kof_platform.ffi` `ProxyExecutable` on the GraalJS/node runner), proven byte-for-byte JVM↔JS by `FfiE2ETest` (+7 `assertJvmJsParity`); browser has no host → honest runtime degrade (R7); non-scalar signatures still `FFI002`. Native `FFI001` (§61) remains an honest per-target gap (R7).
> Deltas 18/09 (2): **FFI JS parity CLOSED (R3.6-F2/F3, `d3598c2d`)** — the JS host runner (GraalJS/node) binds the SAME scalar ABI through `KofJsFfiBridge` (`extern`→`kofFfi`→`kof_platform.ffi`); `FfiE2ETest` 16/16 asserts byte-for-byte JVM↔JS equality; browser = honest runtime error (R7, no host); non-scalar JS = `FFI002` at compile time.
> Deltas 18/09 (3): **FFI callbacks/upcalls bind on the JVM (R3.4-C1/C2)** — an `extern` with a function-typed parameter lowers to `kof_ffi` with a nested `(<ret><params>)` token and the runtime builds a real C function pointer via `Linker.upcallStub` over the Kof function value (specialized `int invoke(int,int)` interface, unreflected + `.asType`-bridged); `JvmFfiCallbackE2ETest` runs `(x,y)->x+y` through C callbacks across Int/Long/Double/mixed ABIs (`42/42/6.0/7.5`). Contract is synchronous/non-escaping (confined-arena lifetime); callback ABI is primitive-only (String/struct/pointer-in-callback and callback-as-return stay `FFI001`); JS callback parity = slice 3.4-C3 (`FFI002` on JS), browser = honest degrade (R7). Host-pin `JvmFfiCallbackTest` locks the mechanism.
> Deltas 18/09 (4): **FFI callbacks/upcalls bind on the JS host runner too (R3.4-C3.1→C3.3)** — the `KofJsRunner` `ProxyExecutable` marshals a callback by building `Linker.upcallStub` over the Kof function value; **the compiled function value is a `Lambda…` object with an `invoke` method (not a native JS arrow), so the bridge calls `fn.getMember("invoke").execute(...)`** (the C3.1 pin's `Value.execute`-on-arrow framing was corrected); `JvmFfiCallbackE2ETest.jvmAndJsCallbacksMatchByteForByte` proves byte-for-byte JVM↔JS callback parity (`42/42/6.0/7.5` Int/Long/Double/mixed); browser = honest degrade (R7); a non-bindable callback (String-in-callback) still `FFI002` on JS. JS+JVM callback parity CLOSED; remaining R3 gaps unchanged (structs/D6, variadics, opaque handles, Native §61).
> **Deltas 20/09 (11): bare `List`/`Set`/`Map` em posicao declarada agora e a colecao builtin nos 4 alvos (#443/§373, `d969bc3a`)** — o descriptor fantasma `LList;` (NoClassDefFoundError no class load) morreu no ponto de convergencia `qualifyDeep` 2b (contrato congelado #139/#150/#214; shadow do usuario preservado pelo controle do §243); `BareCollectionFieldE2ETest` 8/8 (RED 6/8) e o print `2` identico em JVM/Script/Native/JS no jar limpo; a caca Q4 abriu §374/#553 (fix ainda aberto, lane compilador).
> **Deltas 20/09 (12): §374/#553 + §371/#550 — box-by-ARG on BARE collection `add`/`set`/`send` (JVM emitter, mirrors bug-35) AND the shipped CLI cross-builds from any CWD again (classpath-first slice loader)** — `BareCollectionPrimitiveArgE2ETest` 7/7 (RED 4/4 pre-fix), `RuntimeSourceLoaderTest` 6/6 + `ShippedCliCrossSmokeTest` 2/2 (CLI subprocess from a foreign `@TempDir`, real cross-binutils+qemu). Docs lane re-measured 7/7 + 6/6 + 2/2 GREEN on the clean tip `aabd7bff` (0F/0E). Ledger §371/§374 EN+PT.
> **Deltas 20/09 (10): argumentos de `extern` seguem a conversao numerica ORDINARIA do Kof (#549/§370, `35bfaec1`)** — `f(Float)` aceita Int/Double com a MESMA conversao de um chamada kof (`fmid(1,2)` -> `1.5`, `sqrt(9)` -> `3.0`), byte-a-byte JVM==Native (re-medido pela lane docs com jar limpo, paridade nos 5 casos); nao-convertiveis travam SEM014 no call-site (`abs(2.5)`, `abs("s")`, `abs(true)`); arity errada continua SEM013. `FfiExternTypeConversionTest` 11/11 + FfiE2ETest/FfiNativeE2ETest/FfiNativeCrossE2ETest 38/38 reverificados.
> **Deltas 20/09 (9): phantom-constructor gate + cross-CLI broken by the shipped-jar CWD trap** — §362 landed (`57a0d5f0`, `ConstructorPhantomE2ETest` 7/7): `P(1,2)` in `record P(Int x)` now SEM023, arg-type mismatch SEM014 — measured on the shipped jar by the docs lane, identical on jvm/js/native builds and the legit forms (`Q(7)`, `E(9)`, `R(1, 2)`) keep compiling+running (parity cross-target holds because the gate is compile-time). AND the docs-lane sweep of fatia 2 found **§371/#550 (P0-class)**: the runtime-slice loader reads the `.java` source via a CWD-relative path, so the PRUNE NEVER WORKS FROM THE DISTRIBUTED JAR -> full runtime -> `usesDb` -> forced dynamic link -> `-lsqlite3` missing on the cross sysroot -> `kof build --target native.risc hello.kf` fails COMP001 on any machine outside the module tree (E2E suites run with cwd=module = the blind spot; this host reproduced riscv64+aarch64). Fatia-2 ABI itself re-verified good via the API path (FfiNativeCrossE2ETest 6/6 + lane probes under qemu).
> **Deltas 20/09 (8): FFI scalar binds on ALL Native targets (#431 slices 1-2, `d946e6fa`+`d5c849f3`, §369)** — `extern` with `library()` now lowers to a direct `call sym@PLT` on x86-64, riscv64 and aarch64 (the §61 escape becomes the production route: no dlopen, link-by-use — the DB001/SQLite precedent); per-class SysV/LP64/AAPCS64 marshaling with >=9-arg spill, `String`<->`char*` at payload offset 24 (NULL->NULL), `void`/`Bool`/`Float`, String return = boundary copy (`kof_ffi_from_cstr`); SEM013/SEM014 call-site validation added (a wrong-arity `extern` used to compile and SIGSEGV at runtime). ABI facts MEASURED under qemu: riscv64 glibc passes AND returns FP in `fa0..fa7` (physical f10-f17, NOT ft0); aarch64 inherits via the translator's `fa0->f0->d0` = AAPCS64. Two real exit-path bugs fixed with proof: C stdio output was LOST under the raw `_start` (no atexit — `call fflush` before exit on all 3 targets; x86 needed `%rdi=0` + re-alignment) and the aarch64 `_start` used `exit(93)` not `exit_group(94)` (any live thread — scheduler, raylib/GLFW — hung the process FOREVER; RED 20s / GREEN <1s). Proofs: `FfiNativeE2ETest` 16/16 x86-64 (InitWindow-shape golden + JVM byte-parity of the old bindings) + `FfiNativeCrossE2ETest` 6/6 riscv x aarch byte-identical under qemu. Honest remainder: struct/array (D6, variadics (3.5), callbacks on Native, opaque handles -> `FFI001`; cross >=9-spill and Float/Bool edges have no golden (no cross-cc fixture on this host). §61 CLOSED.
**Deltas 20/09 (7): cluster #278 FECHADO COMPLETO** — §361 (box no escritor, `e293c4a5`) + §365 (default nunca-escrito = `null` nos 4 alvos, `dd418419`) + §368 (gate de atributibilidade na store de campo — `String`→`Char`, `2.5`→`Int`, `5L`→`Int?` agora SEM012 nos 4 alvos, `5cd078c1`) — #278 fechado com evidencia. A face `Char?`+String-literal do adendo (6) provada phantom, NAO coercibilidade pendente: rejeicao uniforme e o contrato (locals ja rejeitavam). Matriz 4-alvos em `NullablePrimitiveFieldWriterE2ETest` + `FieldAssignabilityPhantomE2ETest` (8/8)._
**Deltas 20/09 (6): §361 PARCIAL + §365 abertos pela re-medicao da lane docs no tip `e293c4a5`** — escritor de campo nullable-primitivo boxa `Int?/Long?/Double?/Troolean/static/composto` nos 4 alvos (✅), mas a face **`Char?`** ainda morre (JVM/Script `VerifyError` no `putfield`: String cru no slot `Character`; Native: cast error na leitura pos-char; JS ✅) e a **leitura de campo nullable nunca-escrito vaza `0` no JS** (JVM/Script/Native = `null`; pre-existente, nao do fix) — §365 no ledger, rota cluster `.22`/#278.

> **Deltas 20/09 (5): rio da erasure JVM landou (`16f16081`/`c8d55a10`/`0aa6a307`, familias §355-§357)** — `TypeVariable` carrega o bound ate todo emit site, interfaces genericas baixam descritores apagados com pontes covariantes em pais E interfaces (box/unbox na fronteira de primitivo), campo `T[]`/`as T[]` apagam com honestidade, `SEM098` gateia array-primitivo-em-slot-apagado; 8 issues fechadas com prova (#399/#363/#375/#385/#366/#365/#295/#368). Gap nativo pre-existente registrado §358 (link `toString` em `T` sem bound nos cross).

> **Deltas 26/09 (8): `process.Result` inteiro (fatia E da linha 1, lane
> native-cross)** — `println(r)` e `"texto" + r` agora imprimem por CONTEUDO no
> x86-64 (`RuntimeProcessResult`) e no cross riscv64/aarch64
> (`NativeRiscvAsmProcessResult`), com o mesmo contrato §367 da JVM:
> `ProcessResult[exitCode=N, stdout=S, stderr=E]`, removendo apenas CR/LF finais
> de cada stream. `ProcessResultPrintGuard` passa a recusar somente MCU/riscv32
> freestanding. **Prova:** `ProcessResultWholePrintE2ETest` 1/1 (happy path,
> concat, exit 3, stdout+stderr e multiline/trimming; paridade byte JVM==x86-64==
> riscv64==aarch64). Dois bugs de montagem medidos no caminho: (1) literais eram
> passados em `%rsi`/`%rdx` fora da ABI de `kof_string_from_literal` (SIGSEGV
> 139; corrigido para `%rdi`=bytes, `%esi`=len); (2) o trim copiava o HEADER do
> `KofString` em vez do payload (`+24`), perdendo o conteudo.

> **Deltas 26/09 (7): `process.spawn` + ops de handle no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 1 fatia D, lane native-cross)** — nova fatia `NativeRiscvAsmProcessSpawn` (aarch64 herda pelo tradutor), porta fiel do `RuntimeProcessSpawn` x86: `clone(flags=SIGCHLD,stack=0)` pois riscv nao tem `fork`; tabela `.bss` de 64 slots (handle = indice), stdout vivo no pipe, stdin/stderr `/dev/null`, exec falho = `-1` via pipe `CLOEXEC`, reap preguiçoso (`wait4 WNOHANG`) em `alive`/`exitCode`/`kill`; `write` = no-op honesto. Gate `ExpressionProcessCallLowerer` libera `spawn` no cross; so o MCU/riscv32 freestanding segue `PROC001`. **Prova:** `ProcessSpawnCrossE2ETest` 4/4 (paridade byte JVM==riscv64==aarch64 sob qemu; `DomainGapCodesTest.processSpawnOnCross` virou no-gap). Dois bugs medidos no caminho: (1) o sentinela `Integer.MIN_VALUE` precisou de extensao de sinal 32->64 (`slli/srai`), pois no riscv o `Int` mora no registrador de 64 bits; (2) comentario `#` em linha de `.space` nao era removido pelo tradutor → `aarch64-as` rejeitava; movido para linha propria.

> **Deltas 26/09 (6): `process.spawn` + ops de handle no x86-64 (`D-FULL-PARITY-050` linha 1 fatia B, lane native-cross)** — nova fatia `RuntimeProcessSpawn`: `kof_process_spawn` (tabela persistente de 64 slots no `.bss`, handle = indice; `pipe2` + `fork` + `execvp` da libc; stdin/stderr do filho em `/dev/null`; exec falho = `-1`), `kof_spawn_read_line` (linha sem quebra, `""` em EOF), `kof_spawn_exit_code` (`Integer.MIN_VALUE` vivo, `-1` morto/killado), `kof_spawn_alive`, `kof_spawn_kill` (SIGKILL + reap + esquece o handle) e `kof_spawn_write` = no-op honesto (stdin de `/dev/null` nos dois alvos — entrada viva e regra 6). Gate `ExpressionProcessCallLowerer` libera `spawn` no x86-64; cross/MCU seguem `PROC001`. `ProcessSpawnNativeE2ETest` 4/4 (paridade byte JVM==x86-64) + `DomainGapCodesTest` repontado (x86 compila, cross PROC001). Bug medido: o filho fechava o fd de ESCRITA do pipe CLOEXEC (escrevia no fd ja fechado), entao um exec falho era lido como sucesso — fix = fechar `fail_r`.

> **Deltas 26/09 (3): `ssh.cmd`/`run`/`ok` em TODO o nativo — x86-64 + cross riscv64/aarch64 (`D-FULL-PARITY-050` linha 3 fatias A+B, lane native-cross)** — `RuntimeSsh` emite `kof_ssh_argv` (oraculo exato `[ssh,-o,BatchMode=yes,-o,ConnectTimeout=5,host,command]`) e `kof_ssh_run` (reusa `kof_process_run`); o gate `ExpressionSshCallLowerer` libera x86-64 e riscv64/aarch64 e mantem PROC001 so no MCU/riscv32. `SshE2ETest` 12/12 + `SshCrossE2ETest` 2/2 (argv == JVM byte-a-byte nos 3 nativos, host+comando = 1 elemento cada, host inalcancavel = Result honesto; JS `cmd`/`run`/`ok` provados contra o golden JVM).

> **Deltas 26/09 (5): `shell.pipeline` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 2 fatia B2, lane native-cross)** — nova peca `NativeRiscvAsmPipeline.kof_shell_pipeline(List<List<KofString>>)`: o kernel encadeia os estagios (estagio 0 stdin `/dev/null`, stdout do i vira stdin do i+1, stderr intermediario descartado, so o ULTIMO e capturado) via `clone(SIGCHLD)`+`dup3`+`execvp`, SEM threads; Result = `{stdout,stderr,exit}` do ultimo (oraculo JVM). Vazio/sem estagio/>16 = `Result` honesto. `ShellCrossE2ETest` 7/7 (2 e 3 estagios byte-paridade JVM==riscv64==aarch64) + `ShellE2ETest` 19/19. Bug medido: o argv do filho duplicava `argv[0]` (`cat` rodava `[cat,cat]`) — fix no loop (indice `j`, nao `j+1`).

> **Deltas 26/09 (4): `shell.runWith` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 2 fatia B1, lane native-cross)** — `NativeRiscvAsmShell.kof_shell_runwith(argv, cwd, env)` spawna argv-first: programa = `argv[0]`, o resto vira args, delega a `kof_process_run`. O caso herdado (`cwd` `""`/null, `env` null/vazio) e byte-paridade com o JVM; `cwd`/`env` NAO-VAZIOS nunca sao ignorados (R6) — devolvem `Result` honesto (`stdout=""`, `stderr` com mensagem, `exitCode=-1`), como a convencao do JVM para falha de spawn (env aditivo/cwd-exec no processo free-standing = fatia B2). `pipeline` segue `PROC001`. `ShellCrossE2ETest` 5/5 + `ShellE2ETest` 19/19.

> **Deltas 26/09 (2): `shell.run`/`cmd`/`ok` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 2, lane native-cross)** — `run` reusa `kof_process_run` (linha 1 fatia C), `ok` e IR puro sobre o acesso `exitCode`, `cmd` usa a peca nova `NativeRiscvAsmShell` (`kof_shell_argv` = prepend do program, o oraculo JVM exato, sem split). `pipeline`/`runWith` seguem PROC001 honesto (fatia B). Corrige tambem um pin STALE do `ShellE2ETest` que ainda exigia `process.run` cross == PROC001 depois da linha 1 fatia C. `ShellCrossE2ETest` 3/3 byte-parity JVM==riscv64==aarch64; `ShellE2ETest` 18/18. Bug medido no caminho: a peca nao trocava p/ `.section .text` e caia no `.bss` herdado -> `kof_shell_argv` nao-executavel (SIGSEGV 139, `jalr` p/ 0x72298 no `qemu -d in_asm`); fix = `.section .text`.

> **Deltas 26/09 (1): `process.run` no CROSS riscv64/aarch64 (`D-FULL-PARITY-050` linha 1 fatia C, lane native-cross)** — `run` compila e roda nos 2 alvos cross via `NativeRiscvAsmProcess` (riscv64 nao tem `fork` -> `clone(flags=SIGCHLD, stack=0)`; filho `dup3` 1/2, stdin `/dev/null`, `List`->`argv` na pilha do filho, `execvp` da libc para PATH, pai drena os 2 pipes com `ppoll` mascara `0x19` + `wait4`; exec falho = `("", msg, -1)`). `execvp` entrou no `NativeCrossLink.LIBC_SYMBOLS` (link dinamico so quando a fatia e alcancavel; o pruner descarta p/ HELLO). Buffers estaticos de 1 MiB no `.bss` da fatia — a arena GC cross segue 256 KiB (subir o heap global p/ 4 MiB quebrou `riscv/aarch64HeapExhaustionPanicsHonest`; revertido). O gate `PROC001` agora recusa SO `process.spawn` no cross. `ProcessRunCrossE2ETest` 6/6 byte-parity JVM==riscv64==aarch64; `spawn` cross = `PROC001` honesto (fatia B, outra lane).

> **Deltas 20/09 (4): D-TROOL landed (`916b9fb7`/`d61836eb`/`5f0757e8`)** — `Bool` 2-valores, `Troolean` trivalente de Kleene na maquina boxed (`&&`/`||`/`!` desugaram; backends intocados), `Bool?` do usuario rejeitado `SEM095`; lei travada `TrooleanLawE2ETest` 13/13. Linha de paridade propria ja na tabela.

> **Deltas 20/09 (3): X7-2 — DWARF agora nos CROSS (`5d9c855c`/`23b7ecf7`/`890b58bf`)** — tabelas de linha `.file/.loc` provadas no ELF riscv64/aarch64 real sob qemu (`NativeDwarfCrossTest`) + DIEs de CU/subprogram com `frame_base` por ABI; source maps JS ✅; `kof profile` honesto. Linha do tracker corrigida contra o codigo.

> **Deltas 20/09 (2): X10 FECHADA (`27826838`)** — catalogo de assinaturas do LSP em **32/32 namespaces** (`json` na tabela com trava no SEM025 real), hover + `signatureHelp` + `completionItem/resolve` de uma fonte gerada (`scripts/gen_signatures.py`), 265 membros.

> > Deltas 20/09 (1): **`shell.pipeline` is REAL on JS — the last live-pipe residual of row 2.2 closes** — host binding `KofJsProcessBridge.processPipeline`: same ProcessBuilder chain + daemon pump threads as JVM `kof_shell_pipeline` (first stage stdin `/dev/null`, rest PIPE, last exit code, pumps joined, stragglers destroyed; `no stages`/`empty stage`/spawn errors = honest `Result(-1)`). Lowerer gate narrowed to Native-only; `ShellE2ETest` 18/18 with the JS pin replaced by byte-parity goldens (2-stage `echo|wc -w`→3 and a 3-stage `echo|tr|wc` multi-pump pin). Universal tracker row 2.2 flips ✅.
> Deltas 19/09 (6): **JS face of `process.spawn` landed (F10 closes on JS) + a dead routing branch fixed at root, lane `.18`** — `spawn` + handle ops (`write`/`readLine`/`exitCode`/`kill`/`alive`) now run on JS through the `KofJsProcessBridge` host binding — parity **by construction** (same JDK, same ProcessBuilder, contract copied from the measured JVM binding: failed spawn = `-1`, EOF = `""`, alive sentinel = `Integer.MIN_VALUE`); the lowerer gate narrowed to Native-only and `DomainGapCodesTest.processSpawnOnJs` flipped from PROC001-pin to no-gap. **§355 caught on the way:** handle ops lowered to a raw `invokevirtual java/lang/Long.readLine` — the `isHandle` branch lived behind a dispatcher that never routes a `Long` receiver, so NO target ever ran them (all old pins asserted compilation only). Root fix = route it; proof = `ProcessSpawnE2ETest` 4/4 byte-parity JVM==JS. Honest quirk kept: child stdin is `/dev/null`, so public `write` is a no-op on both targets — live input piping is a rule-6 contract change, not a parity bug.
> Deltas 19/09 (5): **`kof.shell` 2.2.3 landed — `runWith(argv, cwd, env)` on JVM + JS host binding, lane `.18`** — runs argv in a chosen `cwd` (`""` inherits the process dir) with an **additive** environment (map keys override inherited ones; never a silent env wipe); spawn errors and empty argv return an honest `Result` (`stderr` set, `exitCode == -1`) on both targets — never a hang, never a silent success (R6). JVM `kof_shell_runwith` (ProcessBuilder) + JS shim → `KofJsRunner.processRunWith`; Native stays the compile-time `PROC001` it inherits from `process.run`. `ShellE2ETest` 15/15 (parity `pwd`/`printenv` goldens JVM==JS byte-for-byte, honest-failure pins, Native gap pin, SEM025 wrong-shape pin). Residual of the 2.2 row = JS `pipeline` live pipes only (separate `process.spawn` platform item). Plan `shell-plan.md` §5 2.2.3 ✅.
> Deltas 19/09 (4): **`kof.workflow` 2.1.3 bundle COMPLETED — all five faces landed, lane `.18`** — retry/deadLetter (both faces)/schedule/checkpoint (3a) shipped earlier today and **supervision (3b) lands now**: `runSupervised(dag, nome, maxReinicios)` runs the DAG as one_for_one supervised workers by DELEGATING to `kof.supervisor` (each job = a `transient` child; per-child watcher restarts only the failed child; deps = cooperative waits on volatile status flags; restart-limit exceeded = drop + transitive skip; R6 guards loud: `maxReinicios < 1` refused, `retry()` on the same dag refused — one restart policy per face). The supervisor host ships flat with the face (deduped by its marker when `kof.supervisor` is also imported). Native runs the real face (no stub — the OTP core delivers on all 4 targets, §129 ported). `WorkflowE2ETest` 20/20 (JVM==JS byte-parity + Native compile pin + double-import pin). Plan `workflow-plan.md` §5 2.1.3.
> Deltas 19/09 (3): **registry PULL landed — `kof deps` consumes the D2-A releases (1.5.3-S2)** — an `owner/repo[@ver]` line in `kofdeps` resolves the release `<repo>-<ver>.tar.gz` from the GitHub Releases API, verifies the embedded `SHA256SUMS` BEFORE installing, caches at `~/.kof/deps/kof/<owner>/<repo>/<ver>/` (idempotent; bare `latest` pins the concrete version into `kofdeps`), and `kof build`/`kof run` consume it through `Deps.classpath()`; honest REG001–REG004 (R6). `DepsRegistry.java`, `DepsRegistryTest` 6/6 on a JDK fake server (zero network in gates). See line 117.
> Deltas 19/09 (2): **`kof deploy` gains multi-target from one source + `--publish` face (X9 slices 4–5, D-POLL-19/D2-A)** — `--target jvm,native,js`/`all` packages one release per face + `<name>-<v>.deploy-manifest.json` (line 8.4); `--publish` uploads tar.gz (+manifest) to GitHub Releases (reuse on 422; honest fail without `GH_TOKEN`) — `DeployPublish.java`, `CmdDeployTest` 15+2-skip on a JDK fake server (zero network in gates). See line 116.
> Deltas 18/09 (14): **APK pipeline real, pinada e CI-provada (§299, tooling/cli lane, X9)** — `ApkToolchain.pickBuildTools` exige build-tools **≥35** (d8 com `class major 65`/Java-21; hosts antigos = honesto `DEP001` com a condição documentada em learn/32 + KOFANDROID), `keytool` roda com `-d` + CWD fixo (não escrevia o keystore fora do diretório de execução) e o split `CmdBuild 647→467` + `ApkToolchain.java` (200) fecha o gate ≤500 (§303). Prova: e2e `androidWithSdkPackagesApk` executa **sem skip** no CI ubuntu (`1b425a57`, `33363a3f` — SUCCESS) e `androidWithoutSdkIsHonestFailure` mantém R6 sem SDK.
> Deltas 19/09 (2): **`kof.workflow` MVP landed (slice 2.1.2, lane `.18`)** — pure-Kof host `dev/kof/workflow-host.kf` injected flat by `CompilerWorkflow` on `import kof.workflow` (DD-OTP-01 option A, supervisor mechanism): `job(name, () -> Bool)` + `.after(dep)` + `dag(listOf(...))` + `run() -> Report` (succeeded/failed/skipped/errors, `allOk()`, `summary()`); deterministic sequential fixpoint, failure cascades to transitive dependents, cycle rejected at run time with an actionable message; NO target gate (no runtime boundary in the layer). Q2 minimal scope; retry/checkpoint/deadLetter = 2.1.3. R1 ledger registered ("workflow platform", 34 namespaces). `WorkflowE2ETest` 7/7. Plan: `workflow-plan.md` SIGNED-OFF 19/09.
> Deltas 19/09 (1): **`kof.shell` MVP landed (slices 2.2.0–2.2.2, `34e4344f`, lane `.18`)** — `shell.cmd(program, args)` (argv list, golden anti-injection: `"a b|c && d"` stays ONE element), `shell.run(program[,args])` → `kof.process.Result` (lowers onto `kof_process_run`, JVM+JS byte-parity), `shell.ok(r)` pure IR, `shell.pipeline([[...],[...]])` = JVM real chain, JS/Native honest PROC001. R1 ledger registered (`stdlib_boundary.txt` "shell platform"). `ShellE2ETest` 11/11. Plan: `docs/shell-plan.md` SIGNED-OFF 18/09 (Q1–Q3 maintainer poll); glob/`~`/redirection stay out of v1 by design.
> Deltas 18/09 (8): **`kof deploy` JS releases are now SELF-CONTAINED (§298, tooling/cli lane)** — `CmdDeploy.jsImportClosure` copies the entry's transitive relative-import closure (`kof-runtime.mjs` + any `./kof-runtime-*.mjs`) into the release dir, hashes every shipped file in `SHA256SUMS` and adds them to the `.tar.gz`, so the `node <name>.mjs` promised by `RELEASE.md` works from a clean directory (before: `ERR_MODULE_NOT_FOUND` — the weak-green test executed nothing; it now RUNS node and asserts program output, with an honest `assumeTrue(node)` guard). Companion fix `90ea7c34`: the android APK pipeline normalizes `projDir` to an absolute path after CI ubuntu exposed a CWD-doubled relative path (keytool `FileNotFoundException`). `DEP001` stays honest for cross riscv64/aarch64 + `--publish` (D2).
> Deltas 18/09 (9): **completion domain-aware completo dos receivers-typed (X10 slice 3, docs→platform lane)** — `StdCatalog` 18→**31 namespaces**: json(encode/decode), log(debug..error), orm(11), config(8), gpu(13), mq(7), validation(27), observability(15), tetris(run) e os estáticos de mídia Image/Audio/Video/Mic — cada lista travada contra o `case`-literals do SEU dispatcher real (`log` via `isLogMethod`, mídia via `case ns -> switch(name)` aninhado, json via os literais `.equals(mc.methodName())` do validador inline); todo membro resolve no dispatcher por probe (argCount 0..8 para media/tetris; `Int[]`x`Int` para gpu). web/app-DSL + ui + ffi ficam honestamente fora (R6). (`9e4d1728`, `LspServerTest` 25/25, `StdCatalogTest` 10/10)

> Deltas 18/09 (13): **hover de símbolos do projeto no `kof lsp` (X10 slice 7, docs→platform lane)** — quando `LspHover` não conhece o nome, `LspProject.declarationLine` busca a declaração no buffer (fonte da verdade) e nos `.kf` irmãos e mostra a LINHA completa + arquivo de origem em ```kof```; desconhecido = `null` honesto (R6). Bug real exposto pela fatia: o parser de frames do teste fatiava por CHAR um `Content-Length` que é BYTES UTF-8 — payloads não-ASCII quebravam; helper refeito byte-safe. (`848b7df1`, `LspServerTest` 30/30)
> Deltas 18/09 (12): **`workspace/symbol` no `kof lsp` cobre o projeto inteiro (X10 slice 6, docs→platform lane)** — `LspProject.workspaceSymbols` une os símbolos dos buffers abertos (fonte da verdade) com os `.kf` irmãos não-abertos (dedupe por path absoluto, walker das fatias 4–5); filtro substring case-insensitive; ordenação prefixo→substring→nome→uri (convenção LSP). Lógica no `LspProject` (LspServer estaciona em 578 < 600). (`c04e16a4`, `LspServerTest` 29/29)
> Deltas 18/09 (11): **referências cruzam arquivos no `kof lsp` (X10 slice 5, docs→platform lane)** — `references()` agora varre os `.kf` irmãos do projeto além do buffer (mesmo walker da fatia 4; read-only). Varredura extraída para `LspProject` (split obrigatório: LspServer ia 603 ≥ 600 CRÍTICO → 572; estrutura-only, comportamento idêntico). Rename cross-file fica de fora: WorkspaceEdit em arquivos não-abertos = decisão de superfície (rule 6). (`f5df2362`, `LspServerTest` 28/28)
> Deltas 18/09 (10): **go-to-definition cruza arquivos no `kof lsp` (X10 slice 4, docs→platform lane)** — quando o nome não é declarado no buffer, `LspServer.definition` percorre a árvore do projeto (profundidade ≤6, ordenada, primeiro hit) e devolve o Location exato do `.kf` irmão reusando `LspSymbols.declarationRange` (a própria convenção do LSP — sem parser paralelo, sem API de AST nova); nome desconhecido = `null` honesto (R6, nunca chute). (`5837eca7`, `LspServerTest` 27/27 — 2 testes novos com `@TempDir` de dois arquivos). Nota de coordenação: o split ≤500 do `CmdBuild` (gate quebrado em 647 pelo §299) já foi consertado em `ea0ac82b` pela lane apk — o split duplicado desta lane foi descartado ANTES do push.
> Deltas 18/09 (7): **domain-aware stdlib completion no `kof lsp` (X10 slice 1, docs→platform lane)** — novo `dev.kof.compiler.StdCatalog` expõe os membros reais dos 7 namespaces roteados pelo `KofStd.staticMethod` (math, strings, encoding, net, uuid, random, rng); após `rng.` o editor recebe os 5 nomes do typer (kind Function, detail `kof.rng`) — sem parser paralelo: `functions()` é transcrição dos `case`-literals dos próprios typers e o `StdCatalogTest` trava as duas pontas contra a fonte (lista == case-literals, dispatch do KofStd == chaves do catálogo, todo membro resolve no typer). Prefixo não-stdlib = zero invenção. time/json/http/db/... ficam na fatia 2 (dispatch próprio). (`8a2c26b7`, `LspServerTest` 23/23)

> Deltas 18/09 (6): **`rng` native x86_64 face closed (X8 slice 2, docs→platform lane)** — `RuntimeRng` asm (xorshift128/splitmix32, same immediates and shifts as the JVM/JS fragments — bits identical by construction) wired into the runtime slice map; `KofRngTest.jvmNativeParityFullFace` RUNS the same `.kf` on JVM and on the native binary and asserts byte-identical stdout; `RNG001` now covers ONLY cross riscv64/aarch64 + ANDROID (compile-time honest). A caller-routing bug caught by the proof: the `random_double` special-case emitted a hardcoded `call kof_random_double` for `rng.double()` too (OS-entropy call, 0 state consumed, shifted int sequence) — now per-name. (`367af29d`, `KofRngTest` 11/11)

> Deltas 18/09 (5): **`rng` namespace — seedable deterministic PRNG (X8 slice 1, docs→platform lane)** — `rng.seed(n)`/`rng.int(bound)`/`rng.boolean()`/`rng.double()`/`rng.string(n, alpha)` on JVM+JS (xorshift128 + splitmix32, 32-bit-exact math: same seed ⇒ same bits on both backends, `KofRngTest` 8/8 incl. byte-for-byte JVM↔JS parity and an in-test Java oracle). `RNG001` is the honest compile-time gap on NATIVE/NATIVE_RISCV64/NATIVE_AARCH64/ANDROID (slice 2 = native asm). NEVER for key material — that stays `random`/`security` (OS entropy, R11).
> Deltas 01/09: **`spawn` with lambda capturing external variable** (`SpawnStmt` emitted the lambda with zero captures → `VerifyError`/wrong value; now collects via `collectCaptures`); **boolean `&&`/`||` short-circuit on JS** (backend emitted bitwise `&`/`|` which evaluates both sides; now JS `&&`/`||` for `bool`, bitwise intact); **`Channel<T>` as a function parameter** (type came out with empty package and `isChannel` required `kof.concurrent` → mapped as builtin + `JvmTypeMapper` → `LinkedBlockingQueue`); **`println`/`print` before `spawn` on Native** and the pre-existing bug **`spawn→await→spawn`** (SIGSEGV in `pthread_create` — stack arrived misaligned at the C call; aligned with `andq $-16` preserving `r15` + frame); **KofJS in a real browser** (`KofJsBrowserE2ETest` — Chrome headless + HTTP + DOM); **`SECN002` closed — AES-256-GCM in KofJS** (pure JS AES-GCM: FIPS 197 key expansion + GCM CTR/GHASH, format `aesgcm$iv$ctTag` identical to JVM/Native; tamper detected by strict base64 decode `strict=true` — cross-target JVM↔JS parity tested); **`OBS002` closed — histogram/metrics on Native** (asm store 32B name+sum+count + Prometheus export via `kof_string_concat` — content parity with JVM); **`TIME001` (Native) — `time.interval`/`time.cancel`** (same mechanism as `scheduler.every`/`cancel`, SCHED001: thread per job; alias `kof_time_interval`/`kof_time_cancel` → `jmp kof_scheduler_every`/`cancel`; mutation by reference of the capture validated); **`TIME001` closed on JS (02/09) — cooperative timer queue pumped by `time.sleep`** (GraalJS has no `setInterval`; browser/Node use the native one — `KofTimeE2ETest.jsIntervalRunsPeriodicallyUntilCancelled`); **`MQ001` closed — kof.mq on Native** (pub/sub + in-process queues in asm: `.bss` store + 40B nodes, invoke-with-arg `rdi`=fn `rsi`=msg, unsubscribe by fn object identity — output parity JVM↔Native↔JS); **`Set<T>`/`Map<K,V>` as class field/return on JVM** (the `JvmTypeMapper` mapped only `List`→`ArrayList`, so `Set`/`Map` became `Lkof/Set;`/`Lkof/Map;` → `NoClassDefFoundError`; now `HashSet`/`HashMap`; + parsing of class method with generic return `Set<Int> all(` — `KofMapSetTest.setMapAsFieldAndReturn`, 3 targets); **typed ORM Query DSL (level 3, `ORM001`)** — `User.query(db){ where; orderBy; limit }` lowered to `db.query<T>` (SQL prepared at compile-time, values as binds; `KofOrmE2ETest` 22); **KofJS source map V3** (real VLQ mappings at line level: each generated function → Kof line via `KofDebugInfo` — before it was a stub `"mappings":""`; `KofJsSourceMapTest`).
> Deltas 02/09: `Set<T>` as a declared type on JVM (`kof.Set` → HashSet); JVM null-safety narrowing fixed (`s.length`/`s.substring(...)`/`if_acmp*`); `mapOf` infers the first pair; `String? s = null` prefixed parses; `readText`/`readFile` → `String?` (Native returns `null`); `size()` throws instead of `-1`; gaps `STR001`/`STR002` documented below.
> Deltas 03/09 (main): **aarch64 native core complete** (riscv→aarch64 translation, NATIVE002 13/13); **kof.deps** (package manager MVP, Maven Central); **lifecycle** `application { onStart/onShutdown }`; **W3C observability spans** (3 targets) + metrics removed; **LOG001 JS**; **TIME001 JS**.
> The "Deltas" lines record the history per release (0.1.0→0.4.0-beta). Current build: `mvn test` **3225** (2762 kof-compiler + 50 kof-script + 7 kof-c-compiler + 406 kof-cli), **0 failures / 0 errors**, 221 skip (cross runs in the dedicated qemu job; external DB/toolchain guards + §255 sysroot) — CI Build+Tests job of `404d8be6` (20/09 ~18:14, first green on `beta-0.5.0`, reactor `Kof 0.5.0-beta`). The §252 flake and the §181/§256 residuals stay closed at code. The matrix reflects the current state of the 3 targets. DoD in `docs/development/DECISIONS.md` §D-PLAT → `docs/architecture/performance.md` §40–§41.

---

## Parity Table

| Feature | JVM | Native | KofJS | Notes |
|---------|-----|--------|-------|-------|
| Literals (int, long, float, double, string, bool, char, null, hex) | ✅ | ✅ | ✅ | hex: `0x...` |
| Variables, `var`/`val`, inference | ✅ | ✅ | ✅ | |
| Arithmetic, bitwise, unary | ✅ | ✅ | ✅ | |
| if/else | ✅ | ✅ | ✅ | |
| if-expr (`var x = if (c) a else b`) | ✅ | ✅ | ✅ | |
| while, for, do-while, break/continue | ✅ | ✅ | ✅ | |
| for-in (`for (var x in coll)`) | ✅ | ✅ | ✅ | |
| switch (`case N:`) | ✅ | ✅ | ✅ | |
| Functions (all forms, without `fun`) | ✅ | ✅ | ✅ | |
| Records (`record Point(Int x, Int y)`) | ✅ | ✅ | ✅ | toString/equals/hashCode |
| Classes, fields, methods | ✅ | ✅ | ✅ | |
| `constructor(...)` and primary `class X(...)` | ✅ | ✅ | ✅ | |
| Inheritance, `super`, override | ✅ | ✅* | ✅ | Native: `super.metodo()` = SUP001 |
| Virtual dispatch | ✅ | ✅ | ✅ | |
| Interfaces | ✅ | ✅ | ✅ | |
| Generics (erasure) — `Box<T>` primitive `T` | ✅ | ✅ | ✅ | 25/08 `substituteTypeVariable` + `kof_int_to_string` |
| `sealed` types + exhaustive `switch` (X5.1/X5.2, `SEM080`/`SEM081`) | ✅ | ✅ | ✅ | 0.5.0-beta — modifier erased in codegen; same-file closed set; `switch`-expr must cover direct subtypes. `SealedTypeE2ETest` 9/9 + matrix `sealedswitch` |
| variance `out`/`in` (declaration-site) + use-site projection `List<out T>`/`List<in T>` (X5.3/X5.4, `SEM082`) | ✅ | ✅ | ✅ | 0.5.0-beta — compile-time only (erases to `WildcardType`); `TypeVarianceE2ETest` 9/9 · `UseSiteVarianceE2ETest` 5/5 · matrix `variance`/`useproj` |
| boundary reflection `interop.schema(R)` (X6, `D-INTEROP-REFLECT`; `INTEROP001`/`INTEROP002`) | ✅ | ✅ | ✅ | 0.5.0-beta — compile-time intrinsic (frontend fold, zero runtime reflection); `List<Field>` with compiler-provided `record Field(String name, String type)`; gate `import kof.interop`; `InteropSchemaE2ETest` 17/17 · matrix `interopschema` |
| static field of an external class by class name (`Integer.MAX_VALUE`, `TimeUnit.SECONDS`; §500-B) | ✅ 26/09 — real `getstatic` (classpath/JDK reflection) | **`INTEROP003`** — compile-time refusal 26/09 (§510: before, `KofGetStatic` leaked and the asm output had nothing behind it) | **`INTEROP003`** — compile-time refusal 26/09 (§510: before, artifact died `ReferenceError: java_lang_Integer`) | SCRIPT ✅ (host-JVM reflection, golden = JVM) · ANDROID ✅ (JvmBackend) · `ExternalStaticFieldE2ETest` 11/11 · `DomainGapCodesTest.externalStaticFieldOnNonJvmBackedTargetsIsInterop003` · [§510](docs/bugs-and-gaps/known-bugs.md#510--the-500-b-external-class-static-field-face-leaked-to-jsnative-integermax_value-compiled-clean-there-and-died-at-runtime-referenceerror-java_lang_integer--the-honest-fix-is-the-compile-time-refusal-interop003-on-non-jvm-backed-targets---fixed-2609) |
| Reflection at the boundary `interop.schema(R)` (X6.0–X6.2, `D-INTEROP-REFLECT`) | ✅ | ✅ | ✅ | 0.5.0-beta — `import kof.interop`; compile-time intrinsic (zero runtime reflection, same output on the 4 targets, no `REF001`); immutable `List<Field>` + compiler-provided `record Field(String name, String type)`; `INTEROP001`/`INTEROP002`; `InteropSchemaE2ETest` 18/18 · matrix `interopschema` |
| Python engine `KofPy` (X2 fatia 1, D-COMPLETE-FIRST item 2; `INTEROP004`/`INTEROP005`/`INTEROP006`) | ✅ 26/09 — motor real: `process.spawn("python3","-u","-c",code,spec)` + RPC linha-JSON + `json.decode<T>` tipado (replay sem estado — a sessão É a fonte) | ✅ x86 (RuntimeProcess, fatia D da paridade) · cross riscv64/aarch64 = `INTEROP005` até os encoders FP pousarem ([§514](docs/bugs-and-gaps/known-bugs.md#514--cross-riscv64aarch64-never-received-the-json-fplong-element-encoders-kof_json_encode_double-absent-from-the-riscv-asm-the-translated-loop-falls-to-encode_int-for-the-new-tags-35---open-owner--lane-native)) | ✅ (JsRuntimeProcessShellOps, golden idêntico ao JVM) | SCRIPT ✅ por paridade de construção (reflexão no MESMO KofRuntime — medido 26/09) · ANDROID/MCU/RISCV32 `INTEROP005` (face não provada — R7); erros nomeados `INTEROP004`/`INTEROP006`; o §513 (json.encode Double/Long colapsados) foi achado neste caminho, FIXED com prova; `InteropPyE2ETest` 5/5 + `InteropPyScriptE2ETest` 1/1 + `JsonNativeEncodeFpE2ETest` 1/1 · [§513](docs/bugs-and-gaps/known-bugs.md#513--jsonencode-element-dispatch-collapsed-raw-doublelong-slots-to-encode_int-on-x86-deterministic-garbage-and-jvm-listbool-cast-booleaninteger-classcastexception---fixed-2609-same-commit) |
| Python engine `KofPy` (X2 fatia 1, D-COMPLETE-FIRST item 2; `INTEROP004`/`INTEROP005`/`INTEROP006`) | ✅ 26/09 — real engine: `process.spawn("python3","-u","-c",code,spec)` + RPC linha-JSON + `json.decode<T>` tipado (stateless replay — the session IS the source) | ✅ x86 (RuntimeProcess fatia D) · **cross riscv64/aarch64 `INTEROP005`** until the FP encoders land ([§514](docs/bugs-and-gaps/known-bugs.md#514--cross-riscv64aarch64-never-received-the-json-fplong-element-encoders-kof_json_encode_double-absent-from-the-riscv-asm-the-translated-loop-falls-to-encode_int-for-the-new-tags-35---open-owner--lane-native)) | ✅ (JsRuntimeProcessShellOps, byte-identical golden) | SCRIPT ✅ paridade de construção (reflexão no MESMO KofRuntime — medido 26/09) · ANDROID/MCU/RISCV32 `INTEROP005` (face não provada — R7); erros nomeados `INTEROP004`/`INTEROP006`; §513 native json achado no caminho (FIXED); `InteropPyE2ETest` 5/5 + `InteropPyScriptE2ETest` 1/1 + `JsonNativeEncodeFpE2ETest` 1/1 · [§513](docs/bugs-and-gaps/known-bugs.md#513--jsonencode-element-dispatch-collapsed-raw-doublelong-slots-to-encode_int-on-x86-deterministic-garbage-and-jvm-listbool-cast-booleaninteger-classcastexception---fixed-2609-same-commit) |
| Lambdas `(x: Int) -> expr` | ✅ | ✅ | ✅ | with captures (box `BoxN`) |
| Exceptions (throw "msg", try/catch/finally) | ✅ | ✅ | ✅ | Native: own unwinding |
| `assert(cond[, msg])` | ✅ | ✅ | ✅ | |
| `spawn` stmt / `spawn f()` / `await` / `poll` / `done` / `cancel`+`cancelled` / `selectAny` / `awaitTimeout` / `channel<T>` send/receive / `scheduler.every`+`cancel` (Handle<T>, unbox, clean exception; `at(cron)` = real 5-field UTC cron on JVM/JS, Native CRON001) | ✅ (virtual threads; channel = LinkedBlockingQueue; scheduler = ScheduledExecutor) | ✅ 31/08 (pthread_create + trampoline + pthread_join, allocator futex — CONC001; awaitTimeout = 1ms polling; channel = FIFO futex; **scheduler SCHED001** = thread per job + `usleep` ms→us + `active` flag; **cross riscv64/aarch64: clone 220 + nanosleep 101 + spinlock `amoswap.w`; CONC001 helpers 15/09 = fatia `RtB48` (done/poll/cancel/cancelled/selectAny/awaitTimeout; cancel por TID real gettid(178) no ctid do clone; supervisor ✅ 19/09 — §129 port cross)) | ✅ 03/09 (CONC003 closed — real async/await/Promise; channel = queue of pending resolvers; `cancelled()` always `0`, known limitation; scheduler = setInterval) | `KofAwaitTest` 8/8 · `KofConcurrency2Test` 41 (post-§117 + cross helpers 15/09) · `SpawnE2ETest` 8/8 |
| Strings (`+`, `==`, length, charAt, substring, contains, startsWith, endsWith, indexOf, trim, case, replace, split) | ✅ | ✅ | ✅ | `STR001` closed in UTF-16: x86_64 + JS + riscv64/aarch64 `length`/`charAt` (10–11/09, B33) + `substring`/`indexOf`/`lastIndexOf` riscv/aarch (11/09, B34 — code-unit walk faithful to x86, `kof_su_*` trampolines). **Contract residue (x86, not cross):** `"ab".substring(0,0)` → `"ab"` (`end=0` marker ="until the end" collides with explicit `end=0`; JVM: `""`) — maintainer's decision |

| Arrays (`new Int[n]`, `arr[i]`, `.length`) | ✅ | ✅ | ✅ | |
| `List<T>`, `listOf`, for-in | ✅ | ✅ | ✅ | |
| `Map<K,V>` + `mapOf` (put/get/remove/contains/size/keys/values/clear/isEmpty) | ✅ HashMap | ✅ own asm | ✅ JS Map | `KofMapSetTest` — **class field/return 01/09** (mapper `Map`→`HashMap`; parse of method with generic return) |
| `Set<T>` + `setOf` (add/contains/remove/size/clear/isEmpty) | ✅ HashSet | ✅ asm over List | ✅ JS Set | type tag on Native; **class field/return 01/09** (mapper `Set`→`HashSet`) |
| `enum` + values/valueOf/name + exhaustive switch (`SEM031`) | ✅ | ✅ | ✅ | enum→String in descriptors; `KofEnumSwitchTest` |
| JSON encode/decode (primitives, List) | ✅ | ✅ | ✅ | arrays `JSN003` closed; `Double[]`/`Float[]` on Native (JSN001, 31/08) |
| JSON objects/records (JSN002) | ✅ | ✅ 31/08 | ✅ | compile-time composition on Native |
| `kof.io` (File, Path, Directory) | ✅ | ✅ | ✅ | `readText`/`readFile` → `String?` (`null` for absence, 02/09); `size()` throws instead of `-1` |
| `readLine` (stdin) | ✅ | ✅ | ✅ | `String?` — `null` at EOF (02/09; Native previously returned `""`) |
| kof.io `readText`/`size` | ✅ | ✅ | ✅ | `STR002` historical closed 02/09: Native `readText` returns `null` (previously terminated); `size()` without `-1` sentinel |
| kof.time (`now()`, `sleep`, `interval`) | ✅ | ✅ | ✅ | `interval`/`every` JVM (ScheduledExecutor) + **Native (01/09, `time.interval` reuses the scheduler — SCHED001; mutation by reference validated; **cross 05/09** — alias `j kof_scheduler_every`/`cancel` in the riscv64/aarch64 runtime)** + **JS (TIME001 closed 02/09; §132 18/09 — `time.sleep` is now an await-point: cooperative async sleep returning a Promise, driven by the `KofJsRunner` host pump so sibling/child spawned tasks advance while a task sleeps)** — interval queue fired by that same host pump (`__kofSleepStep`) on GraalJS, native `setInterval`/`setTimeout` in browser/Node. Civil calendar + ISO date (`isLeapYear`/`daysInMonth`/`dayOfWeek`/`daysBetween`/`addDays`/`diffDays`): **TIME002 closed 11/09** — `addDays`/`diffDays` on the 5 targets (x86 `RuntimeTimeIso` + riscv64 slice B35 + translated aarch64; golden under qemu). `time.collect()` (manual GC): JVM/SCRIPT (JVM runtime) + x86 (`RuntimeGc`) + riscv64/aarch64 real; **JS real** (§426 improved 25/09 — the `kof-runtime.mjs` `kofGcCollectNow` face the backend already imported is now EXPORTED and routed to the host GC, `kof_platform.gcCollect` → `System.gc()`, the exact JVM/SCRIPT semantics; browser/hostless = honest runtime error, never a silent no-op) |
| `readLine`, `readFile`, `writeFile` | ✅ | ✅ | ✅ | |
| `kof.validation` (13 preds) | ✅ | ✅ | ✅ | `KofValidationTest` |
| `kof.math` (`clamp/abs/sign/min/max/isEven/...` Int + `sqrt/lerp/percentage/isInteger/isDecimal` Double) | ✅ | ✅ | ✅ | Int scalars on the 4 targets; **Double (S1b/S1b.1): JVM/Script/JS/x86 (SSE2 `sqrtsd`/`cvttsd2si`/`ucomisd`, raw bits via rax) + riscv64/aarch64 — MATH001 closed 11/09** (slice B36 `fsqrt.d`/`fmul.d`/`fdiv.d` + `fcvtzs`/`fcvt.d.l`; raw bits via a0..aN = x86's "raw rax" model; golden 20 vectors identical on the 5 targets under qemu). `Double as Int` aarch64 fixed along with it (translator: `fcvt.w/l.{s,d}`→`fcvtzs`, was inverted `scvtf` — §120). **`roundTo(value, decimals)` (S1b.3, DECISIONS §3) DONE 14/09 on the 5 targets** — half-away-from-zero by deterministic decimal scaling, no libm (`p=10^\|d\|` by repeated multiply; `d<0` rounds to tens/hundreds); arithmetic contract `roundTo(2.675,2)==2.68`; matrix `stdmathround` + `KofMathTest.roundToCrossArch` under qemu. `pow` = libm (x86 `pow@PLT`; riscv/aarch MATH001). Bug 94: `NaN==NaN` only in the interpreter (frozen semantics). |
| `kof.supervisor` (OTP core issue #83: `import kof.supervisor` → `supervisor(name).child(id,fabrica,politica).restartLimit(max).escalate(cb).start()/.stop(deadlineMs)/.stats()`) | ✅ | ✅ (x86 · riscv64 · aarch64) | ✅ 18/09 (§132) | Pure-Kof host injected as a virtual package (resource `dev/kof/supervisor-host.kf`, mechanism identical to `android-host`: only acts on explicit `import kof.supervisor` — zero false-positive). Observes worker failure via `try{await}catch` per child (`vigiar` loop in a dedicated thread), restarts with a **new factory** (DD-OTP-06), respects restartLimit (stops + R6 without escalate), cooperative stop `cancel`+deadline, stats(started/restarts/dropped/alive). Gate DD-OTP-11: worker fails 2x → ends on the 3rd → restarts=2/factory=3. NATIVE x86 ✅ 15/09: §129 closed (DECISIONS §2 option B — `kof_exc_chain` is now TLS per-thread and the spawn trampoline installs a per-worker handler; `await`/`selectAny` rethrow the cause). riscv64/aarch64 ✅ 19/09: §129 cross port — per-TID chain table `kof_exc_slots` + `kof_exc_slot()` (the TLS-via-`tp` route was proven ABI-unsafe and abandoned); `OTP001` gate removed, `crossGateOtp001` runs the APP under qemu on both arches. JS ✅ 18/09 §132 resolved: `time.sleep` is now an await-point (cooperative async sleep — the compiler colors the reaching method async via `computeAsyncColoring`, `kofTimeSleep` returns a Promise, and `KofJsRunner`'s host pump drives it), so a task spawned from inside another task DOES run while the poller sleeps — the worker fires and `OTP002` is lifted. `KofSupervisorE2ETest` 16/16 (JVM+Script+Native x86+JS run, limit, cross gate OTP001, no-import doesn't inject). Blocker §130 fixed along with it (false SEM024 in method body re-analyzed in the same scope). |
| `kof.observability` (health/metrics/requestId + spans) | ✅ | ✅ | ✅ | `KofObservabilityTest` 12/12 (spans JSON x86_64/riscv64/aarch64 == golden JVM — §272 closed 18/09); **`exportSpans()` (OTel OTLP/JSON) = `OBS003`: JVM/JS ✅, Native compile-time gap** |
| `kof.media` (`Image`/`Audio`/`Video`/`Mic` + `app.serveDir`) | ✅ `javax.imageio`/`javax.sound` (`KofMediaE2ETest`) | **MEDIA001** (handles) / **MEDIA003** (mic) / **WEB005** (`serveDir`) — compile-time gap | **MEDIA001** / **MEDIA003** / **WEB005** — compile-time gap | JVM-only: non-JVM targets diagnose at compile-time (R6), never a silent stub; `MEDIA003` also covers missing hardware |
| `kof.gpu` (Vulkan compute: `available`/`failReason`/`dispatchMatmul`/matvec; `gpu.*` namespace) | ✅ FFM Vulkan (real; link-by-use + `--enable-preview` only for GPU programs) | ✅ asm stubs (`available=false`, dispatch returns the CPU fallback; cross riscv64/aarch64 via `NativeRiscvAsmGpu` since 26/09) | ✅ JS/Script fallback (`JsRuntimeGpuSupport`/interpreter; `GPU001` retired 26/09, `D-FULL-PARITY-050` row 6) | FFI seed of the universal plan (§GPU): every target degrades honestly (`available=false`, dispatch `-1`/`-6`), never a silent wrong result and no longer `GPU001` |
 client | ✅ | Native HTTP/1.1 asm (socket/recv/send parse; https→throw; non-IP DNS→127.0.0.1; timeout/retry/circuit REAIS §259 17/09: ppoll-deadline+SO_RCVTIMEO, N+1 attempts, 30s fail-fast — x86/riscv/aarch) | ✅ synchronous GraalJS (`Java HttpClient` interop); Node/browser = real **asynchronous** fetch (`spawn http.* + await` → Promise; §133 — the synchronous face in pure JS is registered as HTTP004, not emitted as a code) | `KofHttpE2ETest` 8/8 (JVM+JS+Native real resp; `jsNodeSpawnAwaitHttpResolvesBody` on node); retry/circuit parity 30/08 |
| `kof.cache` (get/set/ttl/delete/clear) | ✅ | ✅ 30/08 (clobber fix `%rax`/`%rdi`) | ✅ | `KofCacheE2ETest` 5/5 x3 targets |
| `kof.mq` (pub/sub + queue) | ✅ | ✅ (01/09, pub/sub + in-process queues, asm; **05/09 full riscv64/aarch64 port** — queue by handle, pop via `kof_list_remove`, queue_size, unsubscribe by identity, scan loops without clobber) | ✅ | `KofMqE2ETest` 5/5 (JVM+Native+JS + **cross riscv64/aarch64 qemu** with output parity) |
| `kof.config` (typed) | ✅ | ✅ x86_64 (own asm); **CONF001** riscv64/aarch64 (§425 — no lookup runtime, honest refusal) | ✅ 16/09 (CONF001 closed) | full Native **x86_64** precedence (`KOF_CONFIG` > env > profile > `kof.config`); `NativeConfigE2ETest` 8. The cross asm was a silent default-echo (`li a0,0`) with `supportedOn` wrongly true → gated §425 |
| `kof.log` | ✅ (JSON + correlation ID) | ✅ (asm; UTC, no JSON) | ✅ 01/09 (console.* + level) | `KofLogE2ETest` 11 (incl. JS) + `NativeLogE2ETest` 7. **Cross (riscv64/aarch64)**: `[LEVEL] msg` without timestamp (x86_64 uses `UTC LEVEL msg`) — non-deterministic format gap, not a silent failure (level+message present) |
| `kof.process` (`process.run`/`process.spawn` — subprocess with live stdout; stdin `/dev/null`-gated — §355) | ✅ (ProcessBuilder; spawn handle ops REAL since 19/09 — they never ran on any target before §355) | `run`/`spawn`/`exit` + whole-record `Result` printing ✅ x86-64/riscv64/aarch64 26/09 (`RuntimeProcess`/`NativeRiscvAsmProcess`, `RuntimeProcessSpawn`/`NativeRiscvAsmProcessSpawn`, `RuntimeProcessResult`/`NativeRiscvAsmProcessResult`); MCU `PROC001` | ✅ (GraalJS host — `run` since 18/09, `spawn`+handle ops since 19/09 via `KofJsProcessBridge`, whole-record parity covered by `ProcessResultContentE2ETest`/`ProcessSpawnE2ETest`) | JVM/JS run real subprocesses with live handles; Native runs `run`, `spawn`+handles and prints `Result` by content on x86-64 and the riscv64/aarch64 cross (byte-parity `ProcessSpawnNativeE2ETest` 4/4, `ProcessSpawnCrossE2ETest` 4/4, `ProcessResultWholePrintE2ETest` JVM≡x86≡riscv64≡aarch64); only the freestanding MCU/riscv32 refuses the process surface at compile-time (`PROC001` — R6) |
| `kof.shell` (`shell.cmd`/`run`/`runWith`/`ok`/`pipeline` — sugar over `kof.process`, argv-as-list, never `sh -c`) | ✅ (lowers onto `kof_process_run`/`kof_shell_runwith`; `pipeline` = JVM `kof_shell_pipeline` real chain; `runWith` = cwd + additive env, honest `-1` Results) | ✅ ALL FIVE on riscv64/aarch64 (cross 26/09, `kof_process_run` + `NativeRiscvAsmShell`/`NativeRiscvAsmPipeline`); x86-64 `run`/`cmd`/`ok` ✅ + `runWith`/`pipeline` = honest PROC001 (slice B) | ✅ ALL FIVE real — `pipeline` = host chain + pump threads in `KofJsProcessBridge` (20/09; 2-stage + 3-stage byte-parity goldens) | 18–20/09, plan `shell-plan.md` SIGNED-OFF (Q1–Q3 maintainer poll), `ShellE2ETest` 19/19 + `ShellCrossE2ETest` 7/7 |
| `kof.ssh` (`ssh.cmd`/`run`/`ok` — sugar over `kof.process`, argv-as-list, never `sh -c`) | ✅ (lowers onto `kof_ssh_argv`/`kof_ssh_run` → `kof_process_run`; BatchMode + ConnectTimeout, honest Result) | ✅ x86-64 + riscv64/aarch64 26/09 (`RuntimeSsh`/`NativeRiscvAsmSsh` → `kof_process_run`, argv byte-parity); MCU/riscv32 = **PROC001** | ✅ real (byte-parity with JVM) | 21/09 x86 ✅ cross ✅ 26/09, universal plan Stage 2 row 2.3, `SshE2ETest` 10/10 + `SshCrossE2ETest` 2/2 (host+command stay one argv element each) |
| `kof.workflow` (MVP 2.1.2: `import kof.workflow` → `job(nome, () -> Bool)`, `job(...).after(dep)`, `dag(listOf(jobs...))`, `dag.run() -> Report{succeeded/failed/skipped/errors/retries, allOk(), summary()}`; bundle 2.1.3 COMPLETE 19/09: `flow.retry`/`retryFixed`, `deadLetter` (both faces), `schedule`, `checkpoint`, `runSupervised(dag, nome, max)` = one_for_one via `kof.supervisor`) | ✅ | ✅ (byte-parity with JVM) | ✅ (compile-pinned — the core layer is a sequential pure-Kof fixpoint crossing NO runtime boundary; the `runSupervised` face DOES cross it (spawn/await workers) and runs on all 4 targets — the OTP core delivers everywhere since §129; the honest `PROC001`/`CRON001`/`ORM001` gaps live in the job BODIES the user writes) | Pure-Kof host injected flat as a virtual package (resource `dev/kof/workflow-host.kf`, mechanism copied from `CompilerSupervisor`/`android-host`: only acts on explicit `import kof.workflow` — zero false-positive; user-defined `KofWf*` names suppress injection; the supervised slice also pulls the supervisor host flat, deduped by its marker). Q2 minimal surface + the FULL 2.1.3 add-on bundle; failure cascades to transitive dependents; cycle rejected at RUN time with an actionable message (§2 invariant, never silent). `WorkflowE2ETest` 20/20 exact stdout goldens. Plan `workflow-plan.md` §5. |
| `kof.security` (passwords/crypto/jwt/secrets + G9) | ✅ | ✅ x86_64 (asm) · **SECN002** chacha20 / **SECN006** cookies · **SECN000** riscv64/aarch64 (no `kof_sec_*` runtime) | ✅ (pure JS; **SECN007** resourceServer is JVM-only) | PBKDF2/SHA512/JWT/AES-GCM (asm on Native x86, pure JS in KofJS — `SECN002` **AES face** closed 01/09). `KofSecurity.supportedOn` gates per function/target; measured 17/09: `crypto.encryptChacha20` on Native → `SECN002`, `security.cookieSet` on Native → `SECN006`, `crypto.sha256` on `native.risc` → `SECN000`. See the Documented Gaps rows |
| `auth.resourceServer` (OAuth2 resource server, D-SEC layer 16) | ✅ JVM (JWKS RS/ES, iss/aud/exp, no alg confusion; `KofOAuthResourceServerTest` 4/4) | **SECN007** | **SECN007** | validates third-party JWT; integrates with `auth.authenticated`/`app.security` |
| `web.app().security()` (D-SEC C18, composite middleware) | ✅ JVM (fixed order rate-limit→CORS→headers→cookies/session→csrf→auth→RBAC; `KofWebE2ETest` 25/25) | **WEB006** | **WEB006** | `app.security([opts])`; opts `headers/cors/rateLimit/csrf/auth/roles`; auth-if-present; prod warning |
| `kof.db`/`kof.orm` | ✅ | ✅ (direct SQLite `.so`; **`transaction {}` commit/rollback 01/09** (asm EH + BEGIN/COMMIT/ROLLBACK); **MySQL wire protocol** — handshake+scramble+auth-switch+COM_QUERY+resultset 31/08 + **binary prepared statements 03/09**); **`kof.orm` x86 SQL-puro CLOSED 20/09 (F1+F3a)** — `deleteAll`/`create`/`migrate`/`count`/`count`-com-`where` em asm `kof_orm_*` (fatias `RuntimeOrm1/2/3`) com DDL byte-idêntico ao host (golden mede o `sql` salvo em `sqlite_master`: `PRIMARY KEY AUTOINCREMENT` + `UNIQUE`) e binds via `prepare_v2`/`bind_*` (injection-proof); **F2a `save` row-object CLOSED 20/09** — INSERT-sem-pk + generated key (`last_insert_rowid`) devolvendo NOVA instância (cópia `kof_alloc`+header+slots), UPDATE-por-pk devolvendo a MESMA instância, miss→INSERT-all (upsert), binds pelo typeCode do schema (`RuntimeOrm4` + `RuntimeOrmSchema`/`RuntimeOrmBind`, JVM==Native byte); **F2b `find` row-object CLOSED 20/09** — `SELECT` por pk, miss→`null` como o host, record construído no runtime (resolver `kof_orm_ctors` className→vtable/typeId/size; colunas casadas por NOME, leitura por typeCode, bool por tipo dinâmico da coluna (§397 pós-fix: INTEGER/FLOAT `!=0`, TEXT literal "true") — key chega KofString pelo coerce do call-site, medido), JVM==Native byte; **F2c `all` row-object→List CLOSED 21/09** (`RuntimeOrm6`: mesmo loop de campos do find + `kof_list_new`/`kof_list_add` por linha, empty list ≠ null, oracle JVM medido ANTES da asm, JVM==Native byte); **F2c2 `where`/`where_op` CLOSED 21/09** (`RuntimeOrm7`: um corpo, dois globls; 7º arg `className` lido da stack na entry — S7f/SysV; whitelist do op idêntica ao host com throw medido `ORM operator not allowed: <op>`; GC-safe — A/B empilhados durante o alloc do concat, scan conservativo); **F2c3 `page`/`delete`/`saveAll` CLOSED 21/09 — row-object x86-64 COMPLETE** (`RuntimeOrm8/9/10`: page = `LIMIT ? OFFSET ?` bindados com trunc int32 = `intValue()` do host, KofString do coerce = atoi superset honesto documentado; delete = PK do schema via `parse_schema` + key bind (classificador do Orm7), `true` sempre no DONE como `execute1 >= 0` do host — miss deleta 0 linhas e retorna true, medido no oracle; saveAll = loop `kof_list_size`/`kof_list_get` → `kof_orm_save`, patched instance descartada como no host); provas: `KofOrmE2ETest` `pageDeleteSaveAllNativeEndToEndMatchesJvm` + `pageEdgesDeleteMissSaveAllEmptyNativeMatchesJvm` byte JVM==Native (batch, offset past end, miss, empty list), honest pin moved to cross riscv64; **F2d1–F2d7 runtime-MySQL x86-64 CLOSED 22/09 — 13/13 `kof_orm_*` faces over the wire** (delete_all/count/delete/find/all/where/where_op/page/saveAll/save/count_where/create/migrate; byte JVM==Native proof per face, `KofOrmE2ETest` 60/0F; CLIENT_FOUND_ROWS + backtick dialect + ERR→`mysql: <msg>`); the riscv64/aarch64 cross ORM became REAL face by face (compile-time `ORM001` removed as each landed) — **DB-3 cross slices A-F 22-23/09: F1a `delete_all`/`count` (peça `RtB50`) + F1d `create` (peça `RtB51`, port de `RuntimeOrm2` — parser do schema + DDL + `sqlite3_exec`) + F1c `migrate` (peça `RtB52`, `kof_migrations` + idempotência + INSERT bindado) + F2c3 `delete` (peça `RtB54`, port de `RuntimeOrm9` + `RuntimeOrmSchema` — PK pelo parser, true no DONE incl. miss) + **F2a `save` (peça `RtB55` + helpers globais `RtB55Helpers`, 23/09 — INSERT gerado/UPDATE/upsert: pk 0/null → INSERT sem a PK + `last_insert_rowid` + nova instância patchada, pk≠0 → UPDATE → mesma instância, 0 linhas → upsert; o parser de schema virou o global `kof_orm_parse_schema`; armadilha aarch64 `s10`→`x16` caller-saved corrigida movendo o índice de loop p/ slot de pilha) + **F2c3 `saveAll` (peça `RtB56`, 23/09 — loop `kof_list_size`/`kof_list_get`→`kof_orm_save`, instância patchada descartada como o host, lista vazia true, GC-safe por slots) REAIS no riscv64/aarch64** sobre o SQLite `RtB46/RtB47`; aarch64 herda via tradutor; `KofOrmE2ETest.crossNativeF1aDeleteAllCountMatchX86Oracle` + `crossNativeF1dCreateMatchesX86Oracle` + `crossNativeF1cMigrateMatchesX86Oracle` byte-parity com o oráculo x86-64 (DDL lido do `sqlite_master`: AUTOINCREMENT/UNIQUE/VARCHAR/tipos; unique-em-generated suprimido, entidade sem generated, id ruim→throw; migrate idempotente, inválido→false sem registrar) + **F3a `count_where` (peça `RtB53`, 22/09): `SELECT COUNT(*) FROM "t" WHERE "f" = ?` com bind por tag da caixa §284 (Int/Long/Bool/Double/Float), KofString/null, shape inválida → throw `ORM001`; `crossNativeF3aCountWhereMatchesX86Oracle` byte-parity x86-64 + riscv64/aarch64 + perna JVM; **§447 FECHADO 22/09** — o literal Bool boxeia `kof_box_bool` no Native (antes o `valueOf` do wrapper caía no dispatch de String.valueOf e ligava TEXTO `"true"`; resíduo não-Bool no §447)** + **F2b `find` (peça `RtB57` + `RtB57Helpers` + per-program `NativeRiscvOrmCtors`, 23/09 — SELECT por pk, miss→null, helper `kof_orm_read_field` §397) + F2c1 `all` (peça `RtB58`, 23/09 — SELECT * acumulado em `kof_list_new`/`kof_list_add`, lista vazia ≠ null) REAIS no riscv64/aarch64; + **F2c2 `where`/`where_op` (peça `RtB59`, 23/09 — whitelist do op idêntica ao host com throw `ORM operator not allowed: <op>`, bind do value pelo classificador do Orm7, one body/two globls, 7º arg em a6 no call-site riscv) + F2c3 `page` (peça `RtB60`, 23/09 — `SELECT * FROM "t" LIMIT ? OFFSET ?` bindados, coerção `intValue()` do host: int direto, long/double/float truncam p/ int32, KofString = atoi, null = 0) REAIS no riscv64/aarch64; `KofOrmE2ETest.crossNativeF2c2WhereMatchesX86Oracle` + `crossNativeF2c3PageMatchesX86Oracle` byte-parity com o oráculo x86-64 (op `>=`/`LIKE`/`!=`, op inválido→throw, vazio→lista vazia; page offset além/limit 0→vazia, Double 2.9→2, campo sem coluna→throw) — **o row-object de leitura do ORM cross está COMPLETO (13/13 faces), nenhuma face `ORM001`**; §449 x86 resolvido (resolver multi-entidade + `Double` `movsd` em `RuntimeOrm5/6/7/8` + paridade do `bind_key`)** | ✅ untyped (16/09); `query<T>` = `DB002` CLOSED 18/09; ORM CLOSED 18/09 (`ORM001`) | **Query DSL `User.query(db){ where; orderBy; limit }` (level 3) 01/09** — lowers to `db.query` (JVM E2E H2); native MySQL `nativeMysqlWireProtocol` + `nativeMysqlPreparedBinary` (binary for binds) + `nativeMariadbAliasWireProtocol` (`mariadb://` = `mysql://` alias, 23/09); `nativeTransaction{Commits,RollsBack}`. **riscv64/aarch64 SQLite CLOSED 15/09** (link-by-use `libsqlite3` via `NativeCrossLink.needsSqlite` + runtime `kof_db_*` slices `RtB46/RtB47` — connect/execute/query/bind/close/transaction, `KofDbE2ETest` crossNativeSqliteRoundtrip under qemu both arches; ORM cross F1a+F1c+F1d+F3a+F2c3+F2a+F2b+F2c1+F2c2+F2c3 REAIS (`RtB50`/`RtB51`/`RtB52`/`RtB53`/`RtB54`/`RtB55`+`RtB55Helpers`/`RtB56`/`RtB57`+`RtB57Helpers`/`RtB58`/`RtB59`/`RtB60`, 22–23/09) — row-object de leitura COMPLETO, nenhuma face `ORM001`; MySQL wire x86-64 real (ORM faces closed 22/09) e **ORM sobre o wire MySQL REAL no cross riscv64/aarch64 desde 24/09 (S5.5 `RtB76`–`RtB81` — 13/13 faces byte-idênticas JVM/x86/cross), fechando as linhas 15/16 do `PARITY-GAPS`**). **JS CLOSED 16/09 (DB001)**: untyped `connect`/`connect2`/`close`/`execute`/`query`/`transaction` delegate to `kof_platform.db*` on the GraalJS host (same JVM/classpath as the JDBC path — `KofJsRunner`, bridge `KofJsDbBridge`; `KofDbE2ETest.js{ExecuteAndQueryRowsAsJson,TransactionCommits,TransactionRollsBackOnFailure,NestedTransactionDoesNotCommitOuterScope}` output byte-parity with JVM). **`db.query<T>` typed = `DB002` compile-time** (JS emits no JVM bytecode on the host classpath — `Class.forName` has nothing to load; R6: loud, never silent). ORM on JS CLOSED 18/09 (`ORM001` — `KofJsOrmBridge`). |
| `web.app()` + TLS `listenSecure` | ✅ self-signed (G12) + **own PKCS#8 PEM cert** (`listenSecure(port, certPem, keyPem)`, D-SEC 14/09) | HTTP/1.1 server (accept/route/lambda/body) — **TLS = residual WEB002** | ✅ 03/09 (real GraalJS `HttpServer` — `bc577aa`) | `KofWebTlsTest` 7/7 (JVM, incl. own cert) · `KofWebE2ETest` 25/25 · `KofWebNativeE2ETest` 4/4 |
| `web.app()` WebSocket `app.ws` + SSE `sse.*` | ✅ 30/08 (RFC 6455 + frame codec/mask) | **WEB004** (ws) / **WEB003** (sse) — not ported to Native | **SSE ✅ 16/09 HANDLER-SCOPED** no host GraalJS (`app.sse` + `send/event/close/isOpen` + `sse()`, framing/headers same as JVM; push after-return/multi-client = WEB003 residual, pump single-thread); **ws** = `WEB004` compile-time gate 16/09 (before: silent `kofWebStub` 0, R6); context-fns with runtime stay ✅ | `KofWebWsE2ETest` 11/11 · `KofWebSseE2ETest` 7/7 · `KofWebJsE2ETest` sse 3 · `KofWsFrameTest` 7/7 · `KofWebHardeningTest` 6/6 |
| `status(code, body)` / `headerSet` | ✅ 27/08 | WEB001 (Native only supports `body()` context; `status`/`headerSet` emit WEB001 at compile-time — only `kof_web_body` in `contextNativeSupported`, 16/09) | ✅ 03/09 (JS HttpServer) | `KofWebE2ETest` 9/9 |
| UI Phase 7 Router (`go/replace/back/forward/param/current/depth`) | no-op | — | ✅ 31/08 | `RouterE2ETest` |
| `switch` pattern matching `case String s` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `switch` record destructuring `Point(x,y)` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `switch` as expression `case X -> v` (SYN001) | ✅ | ✅ (x86_64/riscv64/aarch64) | ✅ (nested ternaries) | 03/09 — `default` required or enum exhaustiveness (`SEM032`); `KofSwitchExprE2ETest` 19 + riscv64/aarch64 14/14 |
| `String?` basic null safety | ✅ | ✅ | ✅ | 0.2.6-beta (`Type?`) |
| `Troolean` three-state logic (Kleene; `Bool` never nullable — `Bool?` → `SEM095`) | ✅ | ✅ (x86_64; cross via desugar front-end, CI guard) | ✅ | 0.4.0-beta — D-TROOL 19/09, `TrooleanLawE2ETest` |
| nullable-primitive FIELD write (`b.n = 42` em `Int?/Long?/...`) — #278/§361 | ✅ (`e293c4a5`; FULL apos §368) | ✅ (`5cd078c1`; matriz 4-alvos do teste) | ✅ escrita (`0aa6a307`); ✅ leitura nunca-escrita = `null` (era `0` — §365 fix `JsClassEmitter.insertFieldDefaults`+`KofInterpreterMembers.defaultValue`; matriz 4-alvos `NullablePrimitiveFieldWriterE2ETest` 9/9 matrix361c) | §361 PARTIAL (char face String-literal ainda deve RED); §365 FECHADO 20/09 |
| `List map/filter/reduce` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `Box<T>` generic | ✅ | ✅ | ✅ | `substituteTypeVariable` |
| `KofScript` top-level `var`/`val` → `KofScriptGlobals` (direct execution via `KofInterpreter`) | ✅ | ✅ | ✅ | `KofScript` 0.3.0 |
| `KofCcompiler` (`kof c`) C subset | — | ✅ x86_64 native-only | — | `kof_c`, while/if/deref &/* |
| C FFI (`extern "<lib>"`, R3) | **Partial** — any signature over {Int, Long, Float, Double, Boolean, String} free arity/void/String-return + callbacks with primitive AND String arguments, sync/non-escaping (C2→C3.4); **JVM struct/array ✅ 3.8b (20–21/09)**: `record` by value arg **and** return + scalar `T[]`→`ptr` + out-buffer `Buffer(U8)` INOUT (`buffer.alloc`/`Buffer.bytes()`; `FfiStructE2ETest` 10/10, `FfiArrayE2ETest` 5/5, `BufferE2ETest` 4/4, `BufferFfiE2ETest` 4/4); remaining (String/struct callback return, JS struct bridge) → `FFI001`/`FFI002` | **Partial** — the scalar ABI binds **DIRECT on all 3 native archs** (x86-64/riscv64/aarch64, #431 slices 1–2 ✅ 20/09, §369): link-by-use of `library()` + `call sym@PLT` (no dlopen — §61 escaped), ≥9-arg spill, `String`↔char* (payload off 24, NULL→NULL), `void`/`Bool`/`Float`, String return = boundary copy; riscv×aarch goldens **byte-identical under qemu**; **x86-64 struct param + return by value (register path *and* sret > 16 B) ✅ 3.7 fatias 1–2b (21/09)** — `FfiStructLayout` classifies via `AbiLayout`, the call-site packs each eightbyte into its destination register (param) or saves the return eightbytes and materialises the `record` (return); the sret path passes the hidden pointer in `%rdi` (reserving one INTEGER register) and copies each field from its C offset; **cross (riscv64/aarch64) struct param by value, INTEGER cells ≤ 16 B, ✅ 3.7 fatia 4 (22/09)** — the caller packs each eightbyte of the Kof record into its integer register (`a0`/`a1`; AAPCS64 `x0`/`x1` via the translator), golden measured under qemu on both archs (`FfiCrossStructParamE2ETest`); **x86-64 scalar array `T[]`→`ptr` (D6-2, 3.7 steps 1–2, 22/09) ✅** — `Long[]`/`Double[]`/`Int[]`/`Float[]`/`Bool[]` bind with copy-in per call (`kof_ffi_pack_array`, element size in `%rsi`, no write-back, JVM byte-parity; `FfiNativeArrayE2ETest`), while `String[]` and cross arrays → `FFI001`; `FfiStructE2ETest` `structParamByValueNativeRegisterPath` + `structReturnByValueNativeRegisterPath` + `structReturnSretNative` JVM==Native; memory-path struct param (> 16 B/BYREF)/callbacks/**missing `library()`** → `FFI001` at the decl line (§350) | **Partial** — host runner binds the SAME scalar ABI + callbacks byte-for-byte (`KofJsFfiBridge`/`upcallStub`); non-scalar → `FFI002`; browser = honest runtime degrade (R7, no host) | `FfiE2ETest` 16/16 JVM↔JS byte-for-byte; `JvmFfiCallbackE2ETest` 42/42/6.0/7.5; `KofJsFfiBridgeTest` 8/8; `FfiNativeE2ETest` 16/16 (x86-64, 20/09) + `FfiNativeCrossE2ETest` 6/6 (riscv64+aarch64 qemu, 20/09) |
| `native.risc` (riscv64) / `native.arm` (aarch64) | — | **full core (02-03/09)**: plumbing + codegen/pure-asm runtimes + qemu — `NativeRiscv64E2ETest 13/13` + `NativeAarch64E2ETest 13/13` (core: classes/arrays/List/switch/try-catch/pattern/Strings/recursion) — SUPERSEDED by the 12/09 re-audit; per-arch state indexed in the "Native per-arch" section below — `docs/native-multiarch.md` | — | target separation 0.2.0 |
| `kof fmt` (real parser formatter, idempotent) | ✅ 31/08 | ✅ | ✅ | `KofFormatter` (2c3e794) |
| **KofJS in a real browser** (`kof.ui` rendering DOM via ES Modules) | — | — | ✅ 01/09 (`KofJsBrowserE2ETest` — Chrome headless + HTTP + DOM capture; skips if Chrome absent) | ESM via local HTTP (modules don't load via `file://`); `KofJsRunner` serves `appDir` on `127.0.0.1` |
| Android (Phase 1: `kof build --target android` → Maven project + APK, Activity host in Kof) | ✅ (JVM bytecode) | — | — | `AND00x` gaps at compile-time; `kof.db`/`kof.orm` are **JVM-parity since 20/09** (D-DB-GAPS DB-2 — `supportedOn` gates include `ANDROID`, `Main.class` byte-identical to JVM); `kof.security` is **`ANDROID`-parity since 23/09** (`D-TECHDEBT-23/09` "port the stacks" — `KofSecurity.supportedOn` treats `ANDROID` like `JVM`, JCA shims, `Main.class` byte-identical); `kof.gpu` is **`ANDROID`-parity since 24/09** (§278 — Android compiles like the JVM, `Main.class` byte-identical; runtime = `JvmVkStubRuntime`, FFM-free on ART, `available()=false` + CPU fallback) |

### Native per-arch (x86_64 · riscv64 · aarch64) — multiarch face (4), 19/09

The **Native** column above folds three architectures; the per-arch state was
buried in cell prose (and in the stale `native.risc`/`native.arm` row, whose
"advanced parity pending" was superseded by the 12/09 re-audit of
`docs/native-multiarch.md`). This section indexes the ALREADY
MEASURED states — no new claims; every row cites existing evidence.
riscv64 and aarch64 share one reality: aarch64 inherits 100% of the riscv
slices through `NativeAarch64Translator` (rule 5), so their columns are equal
unless a dedicated note says otherwise.

| Domain | x86_64 | riscv64 | aarch64 | Evidence |
|---|---|---|---|---|
| Core language (classes/arrays/List/strings/FP/switch/try-catch/pattern/recursion, multi-dim §113, UTF-16 search §43/§102/§111) | ✅ | ✅ | ✅ | `NativeE2ETest`, `NativeRiscv64E2ETest`/`NativeAarch64E2ETest` under qemu (CI `cross-native`) |
| GC mark-sweep + AUTO-COLLECT | ✅ trigger ON 19/09 (G-6(a): once/program, gate `spawn_count==0`; MT = old behavior, worker-stack face catalogued) | ✅ tick-collect at alloc (G-4/B44) | ✅ inherits G-4 (G-5) | `KofGcE2ETest` 4/4 incl. in-repo cap test; `NativeRiscvGcSweepTest` runs both arches |
| `println(<collection>)` §107 — scalar elements | ✅ | ✅ | ✅ | three byte-identical goldens vs the measured JVM oracle (x86 `f3b3821c` + cross B39) |
| … record / nested collections | ✅ recursive `.rodata` descriptor 19/09 | ✅ 19/09 (face (4): same descriptor grammar in slice `B39` — 8=vtable `toString` via `jalr`, 9/10=nested, null→"null") | ✅ (translator; new `lhu`→`ldrh`) | `NativeE2ETest.execCollectionPrintRecordNestedJvmGolden` + `nativeCollectionPrintRecordNestedMatchesJvmGolden` on BOTH cross arches (46/46 each under qemu, golden = the measured JVM oracle; concat face needed a `valueOf(record)` vtable branch in `NativeRiscvCrossOps`) |
| Map/Set + erasure boxes (§284/§284-map) | ✅ | ✅ (`RtB49`/`RtB46`) | ✅ (translator) | `NativeErasureBoxE2ETest` 6/6 (md5 equal across the 3), `KofMapSetTest` |
| FP→string Double/Float (FLT001) | ✅ | ✅ (`RtB45`, libc on demand) | ✅ | `nativeValueOfDoubleFloatMatchesJvmGolden` both arches |
| spawn / CONC001 helpers | ✅ (pthread) | ✅ (clone 220 + futex + `RtB48`) | ✅ | `SpawnE2ETest`, `KofConcurrency2Test.crossNative*` |
| `channel<T>` send/receive | ✅ (object queue) | ✅ 23/09 (FIFO futex ported — slice `RtB61`, §423: `kof_channel_new/send/receive`, `amoswap.w` lock + futex 128/129 + 1 ms sleep on empty receive) | ✅ 23/09 (same source through the translator) | `KofConcurrency2Test.channelNative` (x86) + `channelWithSpawnCrossArch` (both arches), `BareCollectionPrimitiveArgE2ETest` (bare `1` + typed `11` run under qemu on both cross) |
| `kof.io` File/Path/Directory | ✅ | ✅ x86_64 real | 🟡 partial — `exists`/`isFile`/`isDirectory` + `readText`/`writeText`/`appendText` + `delete`/`create`/`createDirectories`/`mkdirs`/`size` + `readBytes`/`writeBytes`/`appendBytes`/`list`/`readRange`/`name`/`path_fileName`/`path_parent`/`path_extension`/`path_isAbsolute`/`path_resolve`/`path_normalize`/`path_toAbsolute`/`dir delete` (recursive) ✅ cross (`NativeRiscvAsmIoStat`/`IoText`/`IoFs`/`IoMkdirs`/`IoSize`/`IoBytes`/`IoDirList`/`IoReadRange`/`IoPath`/`IoResolve`/`IoNormalize`/`IoToAbsolute`/`IoDirDelete`, row 13) and x86-64 (`RuntimeIo3.kof_io_dir_delete`); metadata (`modifiedTime`/`isSymlink`) ✅ cross (`NativeRiscvAsmIoMeta`) + x86-64 (`RuntimeIoMeta`); `move_to` ✅ cross (`NativeRiscvAsmIoMove`) + x86-64 (`RuntimeIoMove`); `copy_to` ✅ cross (`NativeRiscvAsmIoCopy`) + x86-64 (`RuntimeIoCopy`) → full `kof.io` parity, `NAT006` closed (§497 FIXED) | `NativeIoStatCrossTest` + `NativeIoTextCrossTest` + `NativeIoFsCrossTest` + `NativeIoMkdirsCrossTest` + `NativeIoSizeCrossTest` + `NativeIoBytesCrossTest` + `NativeIoDirListCrossTest` + `NativeIoReadRangeCrossTest` + `NativeIoPathCrossTest` + `NativeIoResolveCrossTest` + `NativeIoNormalizeCrossTest` + `NativeIoToAbsoluteCrossTest` + `NativeIoDirDeleteCrossTest` + `NativeIoMetadataE2ETest` + `NativeIoMoveCrossTest` + `NativeIoCopyCrossTest` + `DomainGapCodesTest.ioOnCrossIsNat006`/`ioStatOnCrossHasNoGap` |
| `kof.web` T1 (listen/route) | ✅ | ✅ x86_64 real | 🟡 NAT007 — `NativeWebRuntime` is x86_64-only (§427; honest refusal) | `DomainGapCodesTest.webT1OnCrossIsNat007` + `ioAndWebT1OnX86AndJsHaveNoGap` |
| supervisor | ✅ | ✅ (×3 — §129 cross port 19/09) | ✅ | `KofSupervisorE2ETest` |
| scheduler `every`/`at` | ✅ | ✅ | ✅ | `KofTimeE2ETest`; `at(cron)` = CRON001 on ALL natives (not per-arch) |
| math (MATH001 closed; `pow`) | ✅ (`pow` via libm) | ✅ FP; `pow` refuses MATH001 | ✅ FP; `pow` refuses MATH001 | `KofMathTest`, `KofMath.supportedOn` |
| db (SQLite) | ✅ SQLite + MySQL/MariaDB wire | ✅ SQLite (`RtB46`/`RtB47`); MySQL/MariaDB x86-only (`mariadb://` = mysql wire alias, S1 23/09) | ✅ SQLite | `KofDbE2ETest` incl. `crossNativeSqliteRoundtrip` + `nativeMariadbAliasWireProtocol`; `NativeCrossLink` |
| security | ✅ asm (SECN002/006 faces) | 🔴 SECN000 | 🔴 SECN000 | `KofSecurity.supportedOn` |
| rng | ✅ (X8 fatia 2) | 🔴 RNG001 | 🔴 RNG001 | `KofRngTest` |
| http / kof.net / JSON scalar | ✅ | ✅ | ✅ | `riscvHttpGetPostStatus`, cross JSON goldens |
| `json.decode<Record>` | — (not measured here) | 🔴 JSN004 (pure asm) | 🔴 JSN004 | cross refusal list in the 12/09 re-audit |
| `process` / `kof.ui` | PROC001 / no port | identical refusals | identical refusals | compile-time refusals are target-wide, not per-arch (C-FFI broke this rule 20/09: #431 binds the scalar ABI per-arch on Native) |
| `kof.config` typed | ✅ | ✅ x86_64 real (precedence `KOF_CONFIG` > env > profile) | 🟡 CONF001 — riscv64/aarch64 stub echoed defaults, now an honest refusal at lowering (§425; `DomainGapCodesTest.configOnCrossIsConf001`) | `KofConfig.supportedOn` false for the two cross targets |

## Documented Gaps (not masked)

| Gap | Diagnostic | Status |
|-----|-----------|--------|
| **App model (D-APP)** — matrix `APP001–003` | `APP001` (backend-only build with `web/` present → frontend not compiled; honest R6), `APP002` (✅ FIXED 23/09, #598: `kof serve` reads `[server] port` from `kof.toml` and forwards it to the app as `KOF_SERVER_PORT` when the env var is unset — user env wins; proof `ServeManifestPortE2ETest` 2/2), `APP003` (`kof new` — invalid type/flag, existing manifest, partial dir: refuses with diagnostic, never overwrites) | ✅ `CmdNew` 14/09 (D-APP I1, DECISIONS.md §D-APP): `kof new <dir> [--type mono\|backend\|frontend\|full-stack]` — skeletons are born compilable (proof `CmdNewTest` 8/8, incl. real JVM compile of the backend/full-stack skeletons); I3 `--fat` ✅ DONE 14/09 (`kof build --fat` → executable `kof-app.jar`, `CmdBuildFatTest` 4/4, honest refusal outside JVM) |
| spawn/await on Native | ✅ 31/08 (CONC001 closed — pthread_create + trampoline + pthread_join + thread-safe allocator futex; implicit join) | |
| `kof deploy` cross (riscv64/aarch64) | — | **packaged since X9 fatia 6 (20/09)** — the cross ELF (`Default/Main`) goes through the SAME release pipeline as native x86 (artifact 0755 + RELEASE.md + SHA256SUMS + `.tar.gz`); a missing cross toolchain is an HONEST failure naming the tool (R6: it really tried — `DEP001` blanket refusal removed, `binary not found` only after the attempt). Proven: stub `as`/`ld` via `KOF_CROSS_PREFIX` on any host (`CmdDeployTest#crossReleasePackagesWithStubToolchain`, `crossDeployWithoutToolchainFailsHonestly`) + the real binutils/qemu in the CI cross job. `--publish` face ✅ 19/09 (D-POLL-19/D2-A): uploads the packaged `.tar.gz` (per face) + multi-deploy manifest to a **GitHub Releases** tag `<name>-<version>` (422 = reuse the tag; token `GH_TOKEN`; no token / endpoint down = honest failure AFTER the local package exists — R6). **Pull face ✅ 19/09 (1.5.3-S2):** `kof deps add owner/repo[@ver]` + `resolve` download the release tarball, verify `SHA256SUMS` before install, cache under `~/.kof/deps/kof/` and feed build/run — REG001–004 honest (`DepsRegistryTest` 6/6). |
| spawn/await on JS | ✅ 03/09 (CONC003 closed — real async/await/Promise; stmt + spawn-expr + await/poll/cancel/selectAny/blocking channel) | `cancelled()` always `0` (no thread-local for the current task); only task-lambdas can be async (`CONC003-JS-01`) |
| web on Native/JS (server, TLS, ws/sse) | ✅ base server (03/09): Native accept/route/lambda/body (`NativeWebRuntime.java`) + JS GraalJS HttpServer (`bc577aa`). JS: SSE handler-scoped ✅ 16/09. Residual (per feature): TLS `WEB002`, ws `WEB004`, sse `WEB003` (Native; JS post-return push/multi-client), rest `WEB001` (path params/keepalive) | `WEB002`/`WEB004`/`WEB003`/`WEB001` |
| kof.http on Native | ✅ HTTP/1.1 asm (`NativeHttpRuntime.java`, 03/09) | https + real DNS remain gaps; **`timeout`/`retry`/`circuit` are REAL on the 4 native targets since 17/09** (§259 CLOSED: non-blocking connect + `poll` deadline + `SO_RCVTIMEO`/`SO_SNDTIMEO` → `throw "kof.http: timeout"`; retry on exception/`>=500`; circuit fail-fast 30s half-open — riscv64 ported, aarch64 inherits; `KofHttpNativeResilienceCrossTest` 4/4 under qemu, messages identical to x86/JVM). `HTTP002` is the only HTTP gap code *defined* — it is **not emitted today**: `KofHttp.supportedOn` always returns `true`, so the `HTTP002` branch in `ExpressionHttpCallLowerer` is dead and `KofHttp.gapCode()` has no callers (§259). Reserved for when a native HTTP target becomes genuinely unsupported |
| kof.db/orm on JS | **`kof.db` untyped CLOSED 16/09** (bridge on GraalJS host) — `db.query<T>` typed CLOSED 18/09 (`DB002` — guest-side bind via `__kof_decode_<T>`); **`kof.orm` CLOSED 18/09** (`ORM001` — `KofJsOrmRuntime`/`KofJsOrmBridge`, same SQL as `JvmOrmRuntime`, byte-parity E2E) | done |
| `kof.media` (Image/Audio/Video/Mic + `app.serveDir`) on Native/JS | `MEDIA001` (handles), `MEDIA003` (mic), `WEB005` (`serveDir`) | JVM-only: media handles emit the gap in `ExpressionUiMediaCallLowerer` (`MEDIA001`/`MEDIA003`); `app.serveDir` emits `WEB005` in the web gate (`KofWeb.gapCode` + `ExpressionBuiltinInstanceCalls.lowerWeb`) — compile-time, honest R6, never a silent stub. Measured 17/09: before the fix the web gate's catch-all emitted `WEB001` while the docs promised `WEB005` (phantom code; the only mapping lived in the dead `KofMedia.appServeDir`). `KofMediaE2ETest` covers `MEDIA003` (missing hardware) + `WEB005` on Native/JS |
| `kof.gpu` on JS | **retired 26/09** | Was `GPU001` (measured 18/09). Now supported: `KofGpu.supportedOn` is true for JS/Script and the runtime degrades honestly via `JsRuntimeGpuSupport` (`available=false`, dispatch `-1`/`-6`) — proven by `KofGpuCrossTest#gpuJsFallback`; `GPU001` is no longer emitted (`StdParityGapAuditTest#gpuUngatedOnAllTargets`) |
| `kof.observability.exportSpans()` (OTel OTLP/JSON) on Native | `OBS003` | JVM-first (R7): `KofObservability.supportedOn` is false for the Native targets and `ExpressionMethodCallLowerer` emits `OBS003` at compile-time (measured: `--target native` → `observability.exportSpans: not available on the NATIVE driver.target yet (OBS003)`); the JVM/JS payload is OTLP/JSON `resourceSpans` (golden measured on the JVM oracle + GraalJS) |
| `kof.security` on riscv64/aarch64 | `SECN000` | The pure-asm riscv64/aarch64 runtime has **no** `kof_sec_*` primitive: `KofSecurity.supportedOn` returns false for every function on those targets (measured 17/09: `crypto.sha256("x")` with `--target native.risc` → `crypto.sha256: not available on the NATIVE_RISCV64 driver.target yet (SECN000)`). Without the gate the call broke at link with an undefined reference (R6) |
| `kof.config` on riscv64/aarch64 | `CONF001` | The cross asm had no `kof_config_*` lookup runtime (the stubs echoed the default argument / `0` / `false`) while `KofConfig.supportedOn` returned `true` for every target — the `CONF001` branch was dead. §425: `supportedOn` is now false for `NATIVE_RISCV64`/`NATIVE_AARCH64`, so `ExpressionConfigCallLowerer` emits `config.<method>: not available on the NATIVE_RISCV64 driver.target yet (CONF001)` at compile time (measured 21/09 — `DomainGapCodesTest.configOnCrossIsConf001`), never wrong values on the cross. JVM/x86_64/JS keep the real implementation |
| `String` regex methods on JS + Native | `STR003` | `matches`/`replaceAll`/`replaceFirst`/`compareToIgnoreCase` are JVM-only: the typer accepts them everywhere (`StringMethodRegistry`) but JS fell through to a nonexistent `String.prototype` member (runtime `TypeError`; `replaceAll` had literal-vs-regex divergence) and Native was mangled into `java_lang_String_<m>` (cryptic `ld` failure). §424 (21/09): `StringTargetGaps` refuses on JS/Native with a clean compile-time `STR003` (`DomainGapCodesTest.stringIncompleteMethodsOnJsAndNativeAreStr003` + `stringIncompleteOnJvmHasNoGap`); JVM keeps the real implementation. D-FULL-PARITY-050 row 11 (24/09): `toCharArray` left the gate — ported to JS + x86 (`RuntimeStringToCharArray`) + cross (`NativeRiscvAsmStrToCharArray`), UTF-16 code units, proof `KofStringsTest#toCharArrayJvmJsNative` + `NativeStringToCharArrayCrossTest` |
| `kof.io` (File/Path/Directory) on riscv64/aarch64 | `NAT006`(partial) | The cross runtime carries the `kof_io_strlen`/`kof_io_make_string` internals plus, since D-FULL-PARITY-050 row 13, the stat faces (`kof_io_stat_mode`/`kof_io_file_exists`/`is_file`/`is_dir` via `NativeRiscvAsmIoStat`), the text faces (`kof_io_read_text`/`write_text`/`append_text` via `NativeRiscvAsmIoText`) the fs mutations (`kof_io_delete`, `kof_io_dir_create` via `NativeRiscvAsmIoFs`) the recursive mkdir (`kof_io_dir_create_dirs` via `NativeRiscvAsmIoMkdirs`) size (`kof_io_file_size` via `NativeRiscvAsmIoSize`, throws on miss like x86 — the JVM/x86 message divergence is §494) the bytes faces (`kof_io_read_bytes`/`write_bytes`/`append_bytes` via `NativeRiscvAsmIoBytes`) dir listing (`kof_io_dir_list` via `NativeRiscvAsmIoDirList`; opens O_RDONLY because `O_DIRECTORY` is arch-divergent) `readRange` (`kof_io_read_range`/`read_range_path` via `NativeRiscvAsmIoReadRange`, `pread64`) and the pure path faces (`kof_io_file_name`, `kof_io_path_file_name`/`parent`/`extension`/`is_absolute` via `NativeRiscvAsmIoPath`, no syscall) and `Path.resolve` (`kof_io_path_resolve` via `NativeRiscvAsmIoResolve`, string concat) and `Path.normalize` (`kof_io_path_normalize` via `NativeRiscvAsmIoNormalize`, `./..`//` collapsed, root preserved) and `Path.toAbsolute` (`kof_io_path_to_absolute` via `NativeRiscvAsmIoToAbsolute`, `getcwd` + resolve). `Directory.delete()` is recursive on the cross (`NativeRiscvAsmIoDirDelete`) and on x86-64 (`RuntimeIo3`, `getdents64`+`unlink`+`rmdir`). `modifiedTime`/`isSymlink` are ported on the cross (`NativeRiscvAsmIoMeta`) and x86-64 (`RuntimeIoMeta`). `move_to` and `copy_to` are ported (`NativeRiscvAsmIoMove`/`RuntimeIoMove`, `NativeRiscvAsmIoCopy`/`RuntimeIoCopy`); the `kof.io` face set is complete on x86-64 and the cross (`NAT006` closed; §497 FIXED). §427 (21/09): `ExpressionBuiltinInstanceCalls.lowerIo` refuses the not-yet-ported faces at compile time (`kof.io: '<method>' is not available on the riscv64/aarch64 native targets yet (NAT006)`); x86_64/JVM/JS keep the real runtime |
| `kof.web` T1 (listen/route) on riscv64/aarch64 | `NAT007` | `NativeWebRuntime` (`kof_web_listen`/`kof_web_route`) is emitted only on the x86_64 path; the cross allowing it via `KofWeb.isNativeTarget` produced a loud link break. §427 (21/09): the lowering refuses at compile time (`web T1 (listen/route): not available on the riscv64/aarch64 native targets yet (NAT007)`); x86_64 and JS keep the T1 server |
| `kof.security` chacha20 on Native x86_64 | `SECN002` | `crypto.encryptChacha20`/`decryptChacha20` are JVM/JS only (`KofSecurity.supportedOn`); measured 17/09: `--target native` → `crypto.encryptChacha20: not available on the NATIVE driver.target yet (SECN002)`. The AES-GCM face of `SECN002` is closed on Native (01/09) — this is the chacha20 face of the same code |
| `kof.security` cookies on Native | `SECN006` | `security.cookieSet`/`cookieSetOpts`/`cookieGet` are JVM/JS only (D-SEC C11); measured 17/09: `--target native` → `security.cookieSet: not available on the NATIVE driver.target yet (SECN006)` |
| `Secret` value type (`secrets.of`/`secrets.secret`) | `SECN008` | D-SECRETS face 1 (Stage 5/tracker 3.6): JVM-first (R7). JS/Native/Script/Android refuse at compile-time — measured 21/09 (`SecretE2ETest`): `secrets.of("x")` on JS/NATIVE/NATIVE_RISCV64/NATIVE_AARCH64/ANDROID → `SECN008`. The type is reachable only through the gated constructors, so the instance faces (`reveal`/`redacted`) are unreachable where unsupported; never a silent stub (R6) |
| `KeyHandle` (`secrets.keyFromHex/keyFromPem/keyFromKeystore`, `rotate`) | `SECN008` | D-SECRETS P3 (Stage 5/tracker 3.6): JVM-first (R7). Same gated-constructor argument as `Secret` — measured 21/09 (`KeyHandleE2ETest`): `secrets.keyFromHex("...")` on JS/NATIVE/NATIVE_RISCV64/NATIVE_AARCH64/ANDROID → `SECN008`. `crypto`/`jwt` `KeyHandle` overloads are JVM-only; the raw key bytes are never exposed, and `rotate()` revokes the old handle (`SECN010`) |
| `kof.process` on Native | `PROC001` (MCU/riscv32 only) | `process.run`, `process.spawn`+handle ops and whole-record `Result` printing are REAL on x86-64/riscv64/aarch64 (26/09; `ProcessSpawnNativeE2ETest` 4/4, `ProcessSpawnCrossE2ETest` 4/4 and `ProcessResultWholePrintE2ETest` JVM≡x86≡riscv64≡aarch64). Only the freestanding MCU/riscv32 refuses the process surface at compile-time; the host targets access `.stdout`/`.stderr`/`.exitCode` and print/concat the record by content. JVM (`ProcessBuilder`) and JS (GraalJS host) run both |
| `kof.shell` `pipeline` on JS/Native | `PROC001` | `pipeline` needs live stdout→stdin piping: JVM real (`kof_shell_pipeline`, ProcessBuilder chain + pump threads); JS/Native refuse at compile-time with `PROC001` — never a raw `ReferenceError` (§235 discipline; `ShellE2ETest.pipelineOnJsIsHonestProc001` pins it). `cmd`/`run`/`runWith`/`ok` are real on JVM+JS. JS CLOSED 20/09: the chain itself landed as a host binding (`KofJsProcessBridge.processPipeline` — pump threads; the `spawn` unit did not wait: pipeline pipes its own stages internally, exactly like the JVM binding — public `write` still `/dev/null`-gated, rule 6), pinned by `ShellE2ETest` parity goldens (16/16). Native CLOSED 26/09 (row 2 slice B2): the cross (`NativeRiscvAsmPipeline`) chains stages with real OS pipes (no threads — `clone`+`dup3`+`execvp`), byte-parity with the JVM; only x86-64 `pipeline`/`runWith` remain `PROC001` (slice B). |
| `kof.ssh` on Native | `PROC001` (MCU/riscv32 only) | `ssh.cmd`/`run`/`ok` are REAL on x86-64 (`RuntimeSsh`) AND riscv64/aarch64 (`NativeRiscvAsmSsh`), both reusing `kof_process_run` with the exact JVM argv oracle (byte-parity measured 26/09). Only the freestanding MCU/riscv32 remain a compile-time `PROC001`. JVM/JS lower onto the same process layer. |
| `extern` non-bindable ABI on any target | `FFI001` / `FFI002` | **20/09 (#431, §369):** Native now BINDS the scalar set on all 3 archs WITHOUT FFM — `library()` link-by-use + `call sym@PLT` (the §61 escape is the production route). Non-bindable on any native arch (struct **return**/memory-path struct/array/pointer args, callbacks, missing `library()`) → `FFI001` **at the decl line** (§350), never a silent stub, never a link-break (R6); **x86-64 struct param + return by value (register path *and* sret > 16 B) now BIND (3.7 fatias 1–2b, 21/09)**; **cross (riscv64/aarch64) struct return (fatia 3, 22/09) and struct param by value with INTEGER cells ≤ 16 B (fatia 4, 22/09) also BIND** — the rest of the struct surface (memory-path struct param > 16 B/BYREF, cross float/HFA, `T[]`/`Buffer`) stays `FFI001`. JVM binds the scalar set **plus the 3.8b struct/array/out-buffer slice** (`record` by value arg/return + scalar `T[]`→`ptr` + `Buffer(U8)` INOUT, `FfiStructE2ETest` 10/10, `FfiArrayE2ETest` 5/5, `BufferFfiE2ETest` 4/4); the remaining non-bindable on JVM (String/struct callback return) and all non-scalar on the JS host stay honest (measured 21/09). |
| JSON Float/Double | ✅ 31/08 (JSN001 closed — XMM + fraction/exponent parser) | |
| FP→string on riscv64/aarch64 | ✅ 15/09 (FLT001 closed — slice `RtB45`) | was `FLT001`: the pure-asm runtime had no libc (`%g`/`snprintf` absent). Now the cross port of `RuntimeDtoa` (`kof_dtoa_format`/`kof_double_to_string`/`kof_float_to_string`) consumes `snprintf`/`strtod` through the **on-demand dynamic link** (`NativeCrossLink`: a program that prints FP links `-lc`, the rest stays static). Byte-identical JDK `Double.toString`/`Float.toString` on riscv64 **and** aarch64 (aarch inherits via the translator, which now maps the `fa0..fa7` ABI aliases to `d0..d7`); `println(3.14)`/`"v=" + 3.14`/`listOf(1.5)` no longer diagnose. Proof `NativeRiscvDtoaTest` (JVM oracle table, both arches) + `Native{Riscv64,Aarch64}E2ETest#nativeValueOfDoubleFloatMatchesJvmGolden` |
| JSON objects/records on Native | ✅ 31/08 (JSN002 closed — compile-time composition) | |
| GC mark-sweep on Native | ✅ | real `kof_gc_sweep` (bit1 flag) + **auto-collect disabled** for requiring safe-points (`status.md` #1) — `KofGcE2ETest` 3/3 |
| Full native MySQL | ✅ | **wire protocol** + **binary prepared statements (03/09)** — `NativeDbPrepared.java`, `KofDbE2ETest` 12/12 |
| Native riscv64/aarch64 codegen | `NATIVE002` | **riscv64 full core (02/09) + aarch64 full core (03/09)**: `NativeRiscv64E2ETest 20/20` + `NativeAarch64E2ETest 20/20` — runtimes + codegen in **pure asm** (raw syscalls, **no C**; `as`+static `ld` with `--no-relax` (gp=0 without C runtime); aarch64 via `translateRiscvToAarch64`) + qemu: println, var, if/else, arithmetic, **classes (virtual dispatch/fields), arrays, List, switch, try/catch/throw, pattern matching (switch String s/instanceof/as), String methods (charAt/substring/contains/startsWith/endsWith/indexOf/concat/toInt + trim/toUpperCase/toLowerCase/replace char+String/lastIndexOf/equalsIgnoreCase/split), recursion, JSON (encode/decode record+lists), HTTP (get/post/put/patch/delete/options/status + headers — asm-generic socket=198/connect=203/write=64/read=63/close=57, same table on both), spawn/await (`clone(220)`+`futex` — qemu-riscv64 8.2.2 has no clone3; shared heap → atomic bump `kof_alloc` `amoadd.d`/`ldadd`)**. Full x86 parity (DB/UI/net) pending on both. **See `docs/native-multiarch.md`** |
| Static field on Native | ✅ FIXED 07/09 (bug 41, Native lane) | History: `NativeBackend:629-630` was an empty stub for `KofGetStatic`/`KofPutStatic` (printed uninitialized memory). Fix: `ClassLayout` excludes statics from the instance layout + `collectStaticFields`/`emitStaticData` emit with real offset. Proof: `NativeE2ETest.nativeStaticFields` (green in the 12/09 gate, 64/64) |
| `record.hashCode()` on JS/Native | ✅ FIXED (bug 42; JS `1ecfb3d` + Native `CompilerRecordSupport.buildRecordHashCodeMethod`) | Historical symptom: JS `TypeError: hashCode is not a function`; Native `ld: undefined reference to P_hashCode`. Synthetic record `hashCode` (accumulates `31*h + field`) emitted on the 3; `==`/`toString`/`hashCode` parity on the 4 targets. Live proof: `ConformanceMatrixTest.recordhash` (cell WITHOUT exclusion — runs JVM+Native+Script+JS; 11/11 green 12/09). |
| `String.length`/`charAt` on Native (UTF-8 vs UTF-16) | ✅ 5 targets (bug 43; cf. STR001) | Native counted UTF-8 bytes (`"café".length`=5, `charAt(3)`=195); JVM counts UTF-16 code units (4, 233). x86_64 fixed 10/09 (`RuntimeStringOps`/`RuntimeStringSearch` + `.Lkof_substr_walk`); JS 10/09; riscv64/aarch64 11/09 (in the MERGE: slices `RtB34`/`RtB35` — `kof_string_length`/`char_at`/`substring` + `index_of`/`last_index_of` UTF-16, including the `-from` variants (`*_2`, §102 cross) and `starts_with2`). Proof: golden JVM==x86==riscv==aarch (`NativeRiscv64/Aarch64E2ETest`+matrix, `assumeToolchain`). **Contract `substring(0,0)` CLOSED 11/09 (§111):** "until the end" sentinel changed from `0` to `-1` at the x86 AND cross call site — `"ab".substring(0,0)` = `""` (JDK) on the natives. |
| `String.compareTo`/`hashCode`/`equals` on Native | ✅ 5 targets (bug 97, 10–11/09) | Declared in the reference + accepted by the typer, but the natives didn't emit them (`undefined reference java_lang_String_compareTo`/`_hashCode`). x86_64 10/09 (`RuntimeStringCompare`, UTF-16 code units; `.equals` routed to `kof_string_equals` with isString guard); JS 10/09 (`JsCallEmitter` + `kofStringCompareTo`/`kofHashCode`); riscv64/aarch64 11/09 in the MERGE (slice `RtB36`: `String_equals`/`String_compareTo`/`String_hashCode` — code-unit algorithm ported from x86, via `.Lu5_next`; aarch inherits from the translator). **Residue: `Object.equals` (receiver `Object`) still link-fails — virtual dispatch on Native, gap open for the Native lane.** |
| `println(double)` on Native | ✅ 4 targets (bug 44 10/09 + inf/nan spelling 11/09 + §180 shortest-round-trip 15/09 + **cross 15/09** — slice `RtB45` via dynamic-link `snprintf`/`strtod`) | JDK `Double.toString`/`Float.toString` via `RuntimeDtoa` (`kof_dtoa_format`/`kof_double_to_string`/`kof_float_to_string`): bounded `%.*e`+`strtod` shortest round-trip + Java reformat (`1.0E7`, `0.001`), Float with its own form, `Infinity`/`-Infinity`/`NaN`. `RuntimePrintNum` (unboxed `print`), `RuntimeJsonEncode` (NaN/±Inf → `null`) and `RuntimeCollectionToString` delegate to it; `RuntimeStringConv` keeps only int/char/long/bool. Proof `ConformanceMatrixTest.doubleprint` (Native included)+`infinityprint`+`floatprint` + `NativeRiscvDtoaTest` (cross oracle) |
| Heterogeneous `println(if/switch-expr)` on Native | 🟡 PARTIAL 15/09 (fatia 1, `97d08e54`) | §205: the `Object`-join of a heterogeneous if/switch had no Native print dispatch → SIGSEGV (exit 139). Fatia 1 lowers the DIRECT print branch-by-branch (each branch by its own type — the same rule the JVM already applies in the branch box): cells `ifexpr-heterogeneous-direct`/`-else`/`-multi`/`-multi-default` byte-identical JVM×Native. Residual (via-`var`/`as Object`) = the polymorphic boxed ABI (§104b-ii), slice 2. The R6 diagnostic gate was REJECTED by the maintainer — rule 5 (same output on every target) is law. |
| `finally` with `return` in try | ✅ 4 targets (bug 45, DD-01 option 4a, 13/09) | `FinallyFrame` in the IR: `ReturnStmt` with active frame → store `#retVal` + jump `returnFinallyLabel`; epilogue runs the finally and returns (chains nested try); JS rebuilds native try/finally + return-only epilogue. Before: JVM/Native/interp discarded the finally, JS returned `undefined`. Proof `CoreRegressionE2ETest.finallyReturnJvm`+`finallyReturnJs`; suite 1627/0 |
| `json.decode<List<Record>>` | `JSN004 (Native)` — honest gap, bug 48 (09/09) | Interpreter ✅ FIXED 07/09 (`KofInterpreterRuntime` intercepts `kof_json_decode_object_list`); JVM/JS ok. **Native:** no longer a silent link-fail — `ExpressionJsonCallLowerer` (branch `isNative()`, :131-145) emits the **diagnostic `JSN004`** ("List<Record>/List<Class> not supported on the Native target yet; use JVM/JS") instead of calling the nonexistent `kof_json_decode_object_list` (R6: diagnosed gap, never mute stub/link fail). `decode<Record>` at the top ok on the 4 (fix 07/09) |
| nested `try` on KofJS | ✅ FIXED 07/09 (bug 49) | JS `COMP002: try expected KofTryEnd` — `JsControlFlowParser` only processes finally with a `Throwable` catch-all and consumes the done label only if it isn't a nested-try endLabel (`MethodCtx.isTryEndLabel`); JVM/Native/Script/JS run |
| `throw` inside `catch` (re-throw) on KofJS | ✅ FIXED 08/09 (bug 52) | JS `unexpected KofCatchStart` (catch body ends in KofThrow) — resolved as a side effect of the bug 45 fix (`c727fee`); 4 targets now agree (`ConformanceMatrixTest.catchrethrow` without exclusion) |
| channel send/recv INSIDE `spawn` | ✅ FIXED 09/09 (bug 50) | History: Native SIGSEGV 139 — the `call usleep` in the empty receive clobbered `%rsi` (=&lock), leaving `futex` with the wrong uaddr (never set to &lock). Fix: aligns rdi/rsi to the WAKE pattern + restores &lock in rsi for the next cmpxchg, at both points. Proof: `KofConcurrency2Test.channelWithSpawnNative` (`v=42`, green in the 12/09 gate; before, the suite only had `channelNative` WITHOUT spawn) |
| `app.delete(...)` → COMP002 frame crash (GitHub #29) | ✅ FIXED 07/09 (bug 54) | `KofIo.instanceMethod` `case "delete"` without `argCount==0` collides with web route → extra `KofPop` over `kof_web_route` (void) → ASM `Frame.merge` underflow. Fix: `argCount == 0` guard. `get/post/put/patch/delete/options` compile and run (`KofWebE2ETest.deleteRouteCompilesAndResponds`) |
| Web handler ending in `return null` responds 404 (GitHub #28) | ✅ FIXED 07/09 (bug 53) | `ExpressionTyper` `LambdaExpr` case only scanned top-level `ReturnStmt` → nested `return` in `if` ignored → `invoke()` void discarded the value. Fix: recursive scan (`firstReturnValueType`). `return null` remains a documented 404 (`KofWebE2ETest.handlerReturningNullAsLastPathStillRespondsValue`) |
| concurrent `spawn` → CME in the interpreter (statics) | ✅ FIXED 07/09 (bug 55) | `staticFields`/`initialized` HashMap shared between virtual threads → CME in `computeIfAbsent` + `<clinit>` race (loser via empty statics). Fix: ConcurrentHashMap + per-class lock (JVM semantics: clinit once, others wait). Probe 120/120 + `concurrentAwaitReturnsOwnTaskResult` |
| `String.split` + `parts.get`/`.size` → ClassFormatError `Illegal class name ""` (GitHub #30) | ✅ FIXED 07/09 (bug 56) | `ArrayType` receiver in the generic fallback → `KofCall`/`KofLoadField` with owner ArrayType → empty internalName in the constant pool. Fix: `.get(i)` → AALOAD, `.size/.length` → ARRAYLENGTH + typer (component/Int). Shared frontend, JVM emit. `CoreRegressionE2ETest.stringSplitArrayAccess` (JVM+JS) |
| `await` on declared `Object`/`Handle<T>` handle → VerifyError/ClassNotFoundException (GitHub #31) | ✅ FIXED 07/09 (bug 57) | `Handle` wasn't mapped (only `Channel`) → nonexistent `LHandle;` descriptor; and `emitWideningIfNeeded` didn't unbox an erased value (Object/Unknown) to a primitive slot. Fix: `Handle<T>`→`CompletableFuture` (Type.of + JvmTypeMapper) + checkcast + unbox in widening. `CoreRegressionE2ETest.awaitOn{HandleThroughObject,TypedHandle}Param` (JVM) |
| `json.decode` of record with `List<Record>` field → raw maps / ClassCastException (GitHub #34) | ✅ FIXED 07/09 (bug 58) | field emitted without Signature attribute (erased generic) → `kof_json_bind` only via `Class<?>` (erasure) and returned the raw list. Fix: `toGenericSignature` on fields/record components + recursive bind by `getGenericType`. `List<List<T>>` → JSN004 (honest gap). `CoreRegressionE2ETest.jsonDecodeRecordWithListOfRecords` (JVM) |
| `serveDir` `GET /ui/` (trailing slash) → 404 (GitHub #35.2) | ✅ FIXED 07/09 (JVM web lane) | `kof_web_static_match` only resolved index.html for `path.equals(prefix)` (no slash); `/ui/` fell into rel="" → non-file → 404. Fix: trailing slash (and root `/`) → `index.html`. `KofMediaE2ETest.serveDirTrailingSlashServesIndex` |
| `http.<verb>` with 2+ headers as separate args → crash/SEM025 (GitHub #32) | ✅ FIXED 07/09 (bug 60) | API only matched arity 1-3 (headers = 1 String `\n`); 4+ args → null → SEM025 (crash already contained by `65e2dc0`). Fix: variadic headers — `staticCall` accepts `>=2`/`>=3`, lowering merges extras with `concat(acc,"\n",h)`. `KofHttpE2ETest.multipleHeadersAsVariadicArgs` (JVM; JS/Native use the same lowering) |
| Native riscv64/aarch64 REGRESSION: `println` → `undefined reference kof_static_java_lang_System_out` (59 tests) | ✅ FIXED 09/09 (bug 59, Native lane) | History: since `62423bf` (bug 41 fix), the riscv/aarch `.s` referenced the symbol but didn't define it in `.data` (only x86 defined it) → 59 link-fails. Fix: `emitRiscv`/`emitAarch64` now call `collectStaticFields()`/`emitStaticData`. Proof: `NativeRiscv64E2ETest` + `NativeAarch64E2ETest` **39/39 + 39/39 green WITHOUT skip** in the 12/09 gate on this host (compiles → binary exists → runs on qemu → rc=0) |
| `spawn`/`await` (deterministic, order by await) | OK on the 4 | `spawn fn(a)` + `await` + `spawn fn` ×2 + same-thread channel: identical output on the 4 targets (locked in `ConformanceMatrixTest` batch 3) |

Closed in 0.2.6-beta (01/09): **`kof.log` on JS (LOG001)** — console.* + `KOF_LOG_LEVEL`, Vulkan block in the JVM runtime conditional on use of `kof.vk` (capability/link-by-use, R2) + `--enable-preview` only for Vulkan programs (FFM preview API in JDK 21), `spawn` with lambda capturing external variable (JVM+Native), boolean `&&`/`||` with short-circuit on JS (bitwise intact), `Channel<T>` as a function parameter (JVM/Native/JS), `println`/`print` before `spawn` and `spawn→await→spawn` on Native without SIGSEGV (stack alignment in `pthread_create`), KofJS `kof.ui` rendering in a real browser (Chrome headless E2E), LSP `references`+`rename`, W3C tracing `traceId`/`spanId` (3 targets), `moduleRoot` by LCA (P1-4), typed column validation in the ORM (P3-10, `ORM003`), **AES-256-GCM in KofJS (`SECN002`)** — pure JS AES-GCM with cross-target JVM↔JS parity and tamper detection by strict base64, **histogram/metrics on Native (`OBS002`)** — asm store + Prometheus export in content parity with the JVM, **`time.interval`/`time.cancel` on Native (`TIME001`)** — reuses the scheduler (SCHED001) via `jmp` alias, mutation by reference validated; **TIME001 closed on JS (02/09)** — `kofTimeInterval`/`kofTimeCancel` now drive a cooperative queue `kofTimeJobs` pumped inside `kofTimeSleep` (GraalJS has no event loop; browser/Node fall back to native `setInterval`) — `KofTimeE2ETest.jsIntervalRunsPeriodicallyUntilCancelled` (parity ticks≥2 + freeze after cancel), **`transaction {}` on Native** — `kof_db_transaction` in asm: BEGIN/COMMIT/ROLLBACK via `kf_db_execute`, lambda via vtable (`rdi`=this for captures) and ROLLBACK+re-throw in the EH (`kf_throw_string` reaches the handler with `%rdi` and the chain to the outer try), default connection = last opened — `nativeTransactionCommits`/`nativeTransactionRollsBackOnFailure`, **`kof.mq` on Native (`MQ001`)** — pub/sub + in-process queues in asm (`.bss` store + 40B nodes, invoke-with-arg, output parity with JVM/JS), **`Set<T>`/`Map<K,V>` as class field/return on JVM** — mapper `Set`→`HashSet`/`Map`→`HashMap` + parsing of class method with generic return (`KofMapSetTest.setMapAsFieldAndReturn`, 3 targets), **KofJS source map V3** — VLQ mappings at line level (generated function → Kof line; `KofJsSourceMapTest`).
Closed in 0.2.6-beta (30-31/08): `spawn` Native (CONC001 — pthread), FP XMM (FLT001), complete JSON on Native (JSN001/JSN002/JSN003 — objects/records + arrays incl. Double/Float), WebSocket/SSE JVM (RFC 6455), `kof.http` retry/circuit JVM+JS (30s window, fail-fast), `kof.cache` 3 targets (register clobber fix), native SQLite direct `.so`, UI Phase 7 Router (JS), `kof fmt` + `kof config gen`.
Closed in 0.2.6-beta (27/08): pattern matching `switch case String s` + record `Point(x,y)` (JVM/Native/JS), basic `String?` null safety, `kof.http` on JS via `Java HttpClient`, `List map/filter/reduce`, large-project `import a.b.C` (`CompilerDriver.java:243`), `List.get`/`listOf`, free-list GC (`kof_free_head`), Windows SIGPIPE.
Closed in 0.1.0: native Map/Set (was COL001), await with unboxing, capture in lambdas (BoxN), task result (`await`).

## Principle

Kof semantics are the same on all targets. Where a target does not yet support
a feature, the compiler emits an explicit diagnostic — never code that works
differently or breaks silently.

**Sole named exception (D-UI-SCOPE, 18/09).** Absolute parity binds every
surface except **`kof.ui` rule updates** — the UI language's rendering/state
rules (signals, `when`/`each`, diff, cancellation semantics). Those are authored
and effective on **KofJS today** and on **Kof WebASM when `Target.WASM`
exists** — **never** on JVM/Native. This is the single named exception to
rule 5; everything else, including the rest of `kof.ui` and every other
domain, stays under the parity law. See `docs/development/DECISIONS.md`
§D-UI-SCOPE.

---

## Gap convention per domain (universal view)

Every capability gap has a **diagnostic code** documented here and emitted at
compile-time. New domains follow the same pattern as the existing ones
(`SECN000`, `WEB002`, `WEB005`, `CRON001`, ...):

| Prefix | Domain | Examples |
|---------|---------|----------|
| `HTTP`/`WEB`/`DB`/`ORM`/`MQ`/`SCHED`/`TIME`/`CONC`/`SECN`/`CRON`/`MEDIA`/`GPU`/`OBS`/`PROC`/`FFI` | Current systems | `WEB001`/`WEB002`/`WEB003`/`WEB004`/`WEB005`/`WEB006` (web gate on non-JVM), `SECN000` (all `kof.sec` on riscv64/aarch64), `SECN002` (chacha20 on Native), `SECN006` (cookies on Native), `SECN007` (OAuth resource-server on non-JVM), `CRON001` (`at(cron)` on Native; JVM/JS real), `MEDIA001`/`MEDIA003` (media handles/mic on non-JVM), `OBS003` (`exportSpans` on Native), `PROC001` (`kof.process` on Native), `ORM001` (`kof.orm` on Native **cross riscv64/aarch64 only** — x86-64 real since 22/09: 13/13 faces over SQLite + MySQL wire), `RNG001` (`rng` PRNG on cross riscv64/aarch64 + ANDROID — JVM/JS/NATIVE-x86_64 real, slices 1–2 ✅) |
| `AND` | Android | `AND002` (embedded web server on Android), `AND004` (android.jar missing — warning); `AND001` closed 31/08; `AND003` is a documented caveat (no compile-time gate) |
| `NATIVE` | multiarch codegen | `NATIVE002` (label for the riscv64/aarch64 work; not an emitted diagnostic) |
| `INFRA` | infrastructure / IaC | `INFRA00x` |
| `DATA` | data engineering / dataframe / ML | `DATA00x` |
| `SCI` | scientific computing / HPC | `SCI00x` |
| `BIO` | bioinformatics | `BIO00x` |
| `SECPQ` | post-quantum cryptography | `SECPQ` (target gap, never a stub) |

**Reserved codes (defined, not emitted).** A `gapCode`/`supportedOn` pair may
stay in the source after the capability landed, as scaffolding for a future
unsupported target. Those literals exist but **no target reaches them today**,
so they are not gaps: `HTTP002` (`KofHttp.supportedOn` always returns `true`),
`MQ001` (`KofMq.supportedOn` always `true`), `SCHED001` (`KofScheduler` gates
only `at(cron)`, as `CRON001`; `SCRIPT` aborts earlier with `COMP003`),
`CONC003` (the live JS residual is `CONC003-JS-01`), `UUID001` (gate removed
10/09), `OBS001`, `SECN005` (`rate_limit`/`session`/`api_key` are supported on
every target). Do not cite them as emitted — a diagnostic appears only when a
target is genuinely unsupported (R6: never silent, and never a phantom either).

**Android is a real gated target.** `--target android` reuses `JvmBackend`,
and §278 resolved the over-gating: `kof.db`/`kof.orm` (20/09), `kof.security`
(23/09) and `kof.gpu` (24/09) now compile like the JVM (`Main.class`
byte-identical; the Android gpu runtime is the FFM-free `JvmVkStubRuntime`).
The genuinely intended refusals remain `AND002` (embedded server) and `AND004`
(android.jar missing from `ExternalClasspath`); they are listed so the matrix
does not lie about what Android accepts.

Rule (R6): a domain gap always has a **code + entry in this matrix** — never a
silent stub, never a weak fallback, never "partial parity" without a
diagnostic.

## Stability tiers (R5)

Each namespace/package carries a tier:

- **`stable`** — compatibility guarantee; promotes only with complete DoD
  (3 targets or diagnosed gap, E2E per target, benchmark when plausible,
  docs + `training/` synchronized, green suite).
- **`experimental`** — may change; the **official packages** layer is born
  `experimental`.
- **Default (D-POLL-19/09, D4-A):** every namespace — core or new — is born
  `experimental`; promotion to `stable` is per-namespace, only with the full R5 DoD
  (never in bulk, never by age).

**Registry (D4-A; measured 19/09; layers = `scripts/stdlib_boundary.txt`; tiers
machine-gated by `scripts/check_stdlib_boundary.sh` since 21/09 — a missing/invalid
tier, or a `stable` not pinned in the gate's `STABLE_ALLOWLIST`, fails the build).
Every namespace is `experimental` — **zero promotions**: the R5 DoD closes per
namespace, never in bulk, never by age, and a promotion gets a row HERE (with the
test + target matrix that proves it) before the tier word changes anywhere:

| R1 layer | namespaces |
|---|---|
| base-stdlib | `math` `strings` `encoding` `net` `uuid` `random` `rng` `io` `concurrent` |
| platform | `time` `http` `db` `cache` `buffer` `process` `shell` `ssh` `json` `log` `orm` `config` `gpu` `mq` `validation` `observability` `tetris` `auth` `crypto` `jwt` `passwords` `secrets` `security` `Audio` `Image` `Mic` `Video` `supervisor` `workflow` `version` `ui` |

(Scope: the 34 callable member namespaces of `StdCatalog.namespaces()` — single
source, lock `StdCatalogTest` — plus the 6 surfaces registered in the R1 ledger
(`io`, `concurrent`, `supervisor`, `workflow`, `version`, `ui`) that are not
member-cataloged. Total: 40, all `experimental`.)

Heavy domains (`ml`, `bio`, `hpc`, `infra-<cloud>`) are **official packages**
(layer 4), **never** base stdlib (R1).
